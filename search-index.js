// Auto-generated by build_search_index.py -- do not hand-edit.
// Regenerate after changing any subsection title/body text.
window.SEARCH_INDEX = [{"page":"index.html","pageLabel":"Week 1","id":"d1-1","title":"The Three Ways (Flow, Feedback, Continual Learning)","body":"This mental model, from Gene Kim's The Phoenix Project and Accelerate, underpins everything in DevOps. The First Way — Flow (left to right) Work moves from development to production with no long queues, no invisible work, and small batch sizes instead of giant releases. The goal is to shorten the time between a developer writing code and that code delivering value to a user. Key practices: Small, frequent commits rather than large, infrequent releases Automated testing so the pipeline doesn't stall waiting for manual QA Limiting work in progress so tasks finish rather than pile up Making work visible — everyone can see what's in progress, blocked, or done The Second Way — Feedback (right to left) Signals move from production back to development, fast. A failing test, a production alert, a customer complaint — the shorter the path back to the person who can fix it, the cheaper the fix. A bug caught in a developer's IDE costs minutes; the same bug caught in production costs hours, customer trust, and possibly money. Automated monitoring and alerting on production systems Fast test suites that run on every commit Blameless post-incident reviews that produce systemic fixes Telemetry everywhere — you can't improve what you can't measure The Third Way — Continual Learning and Experimentation A culture of experimentation, where every incident becomes shared knowledge rather than blame. Teams run game days (simulated failures), conduct chaos engineering, and turn every post-mortem into a concrete improvement. Every improvement becomes the new baseline you build on. Blameless post-mortems with action items that actually get implemented Allocated time for improvement work (not just feature work) Sharing knowledge across teams through documentation and internal talks Experimentation — new tools, approaches, architectures in low-risk settings first"},{"page":"index.html","pageLabel":"Week 1","id":"d1-2","title":"DORA Metrics","body":"Four measurements from the DevOps Research and Assessment group, validated across thousands of organizations. They split into two speed metrics and two stability metrics. MetricWhat it measuresEliteHighMediumLow Deployment frequencyHow often you ship to productionOn demand (multiple/day)Weekly–monthlyMonthly–semi-annually< once / 6 months Lead time for changesTime from commit to production< 1 hour1 day – 1 week1 week – 1 month> 6 months Change failure rate% of deployments causing a failure0–15%16–30%16–30%16–30% Time to restore serviceHow fast you recover from an incident< 1 hour< 1 day1 day – 1 week> 6 months Critical insightSpeed and stability are not a trade-off. Elite teams are good at both simultaneously. Shipping faster doesn't mean more failures — it means smaller changes that are easier to debug and roll back. How to measure each one practically Deployment frequency: count of production deploys from Jenkins/pipeline build history Lead time: timestamp of PR merge to timestamp of production deploy Change failure rate: count of rollbacks or hotfixes divided by total deploys Time to restore: duration from incident alert to service recovery, from incident tickets"},{"page":"index.html","pageLabel":"Week 1","id":"d1-3","title":"SLIs, SLOs, and Error Budgets","body":"SLI — Service Level Indicator A measured quantity that describes some aspect of service health, e.g. \"99.95% of HTTP requests returned a 2xx/3xx status\", \"p95 latency was 220ms\", \"99.99% of scheduled cron jobs completed successfully\". SLO — Service Level Objective The target for an SLI, agreed with the team and (ideally) the business, e.g. \"99.9% of requests succeed over a rolling 30-day window\" or \"p95 latency stays below 300ms\". Error budget The gap between 100% and your SLO. A 99.9% SLO leaves a 0.1% error budget — roughly 43 minutes of downtime per month. This budget is \"spent\" on risk: deploys, experiments, maintenance. How error budgets drive decisions Budget remaining → ship features, take risks, experiment Budget almost exhausted → slow down, focus on reliability, skip risky deploys Budget exceeded → freeze feature work, all effort goes to stability until it recovers This turns \"reliability\" from a vague feeling into a number the engineering team and the business can make decisions with — and it prevents the trap where ops says \"we can never deploy, it might break things.\" The error budget explicitly permits a certain amount of breakage."},{"page":"index.html","pageLabel":"Week 1","id":"d1-4","title":"Toil vs Engineering Work","body":"ToilManual, repetitive, automatable work that scales linearly with the system and has no lasting value — SSH-ing in to restart a service, copying config to every server by hand, checking disk usage each morning, manually creating IAM users via the console. Engineering workReduces toil or adds durable value — a script that restarts a crashed service automatically, a CI/CD pipeline that deploys config changes, CloudWatch alarms for disk usage, Terraform that provisions IAM users from config. Google's SRE guidance: cap toil at 50% of a team's time. If you're doing the same manual step three times, it should become a script or pipeline stage by the fourth time. The practical test: \"If the system doubles in size, does this task also double?\" If yes, it's toil. If no (because it's automated), it's engineering."},{"page":"index.html","pageLabel":"Week 1","id":"d1-5","title":"Scrum and Kanban Applied to a Platform Backlog","body":"Infrastructure work splits into two fundamentally different streams: Planned work (~60%): build the VPC, set up CI/CD, migrate the database to RDS, write Terraform modules — predictable, estimable, fits sprint-style planning. Unplanned work (~40%): production is down, a dev team needs a new IAM role, a security patch dropped today, disk is 95% full — arrives any time, can't wait for the next sprint. Pure Scrum breaksScrum assumes most work is planned and committed to a sprint. When 40% arrives unpredictably, sprint commitments are constantly blown and the process becomes a burden. Pure Kanban's costContinuous flow handles interrupts naturally, but without sprint commitments, long-term project work gets endlessly deprioritized behind whoever is shouting loudest. The hybrid most DevOps teams actually use Scrum-style 1-week sprints for planned project work (sprint goal: \"VPC and IAM roles are built and documented\") Kanban board running alongside for operational requests and incidents, with its own WIP limits Sprint capacity planned at 60% to absorb interrupt-driven work — pull extra backlog tasks if no incidents come One standup covers both streams: \"Here's my sprint task. I also picked up an incident yesterday, here's the status.\" WIP limits: a maximum number of items per board stage — e.g. 2 per person \"In Progress.\" Uncomfortable, but the first two finish faster; context-switching between three half-done incidents is slower than finishing two sequentially. Cycle time replaces velocity as the metric: how long does a task take from \"Ready\" to \"Done\"? Growing cycle time means something is blocking flow. Definition of Done for infrastructure Works end-to-end — the VPC actually routes traffic, the IAM role actually grants access Reproducible — built via code, not clicked in the console Secure — least-privilege, no open access, encrypted Documented — a teammate can understand it without asking you Explains trade-offs — you can articulate what you chose and what you rejected"},{"page":"index.html","pageLabel":"Week 1","id":"d1-6","title":"Shift-Left Testing and Security","body":"\"Shift left\" means moving testing and security checks earlier in the pipeline — towards the developer's commit, away from the end. Code → Build → Deploy to staging → Manual QA → Security audit → Deploy to prod ↑ bug found here costs days Code → Lint + unit tests → SAST scan → Build → Deploy to staging → Prod ↑ bug found here costs minutes Concrete shift-left practices Pre-commit hooks: linting, formatting, basic checks before the commit even enters Git Unit tests on every commit: CI fails immediately if tests break SAST: tools like Bandit (Python), Semgrep, or Checkov (Terraform) scan for security issues in code, on the PR Dependency scanning: Dependabot or Snyk check for known vulnerabilities in libraries before deploy Policy-as-code: Open Policy Agent (OPA) enforces rules like \"no S3 buckets without encryption\" at the PR level The principle: the earlier you catch a problem, the cheaper it is to fix. A SQL injection caught by SAST during code review costs 15 minutes. The same vulnerability caught in a production penetration test costs days of remediation, an incident report, and possibly a breach."},{"page":"index.html","pageLabel":"Week 1","id":"d1-7","title":"Trunk-Based Development","body":"The model: everyone commits to (or near) a single main branch frequently, using short-lived feature branches that live for hours to at most a couple of days. main: ──●──●──●──●──●──●──●──●──●── (always deployable) \\ / \\ / \\ / feature-a: ●──● (merged in hours) feature-b: ●──●──● (merged in 1-2 days max) Rejected alt.main: ──●────────────────●────── (only release merges) develop: ──●──●──●──●──●──●──●──●── (integration branch) release/1.2: ●──●──● (release branch) feature-xyz: ●──●──●──●──●──●──● (weeks-long branches) Why trunk-based wins for DevOps Short-lived branches mean fewer merge conflicts (hours of drift, not weeks) main is always in a deployable state — any commit can be released Enables small-batch, high-frequency deployment (daily or multiple times a day) Forces code to be backward-compatible (it's going to main alongside other work) How incomplete work ships safely: feature flags. Code is deployed \"dark\" — it exists in production but is behind a flag that's off. Enable it for internal users first, then 1%, then 10%, then 100%. If something breaks, disable the flag — no redeploy needed. Manager's likely challenge: \"But Gitflow gives you a clear release branch — how do you handle hotfixes in trunk-based?\" Your defense: a hotfix is just another fast merge to main, deployed immediately. No cherry-picking across branches, no \"which branch has the fix\" confusion. Feature flags mean the fix ships in minutes, not hours."},{"page":"index.html","pageLabel":"Week 1","id":"d1-8","title":"Modern Change Management","body":"Traditional — slowA Change Advisory Board (CAB) meets weekly, reviews every proposed change, approves or rejects. Changes queue for the next meeting. Deployment frequency: at best weekly, often monthly. Modern approach — fast and safe Small changes: small diffs are easier to review, understand, and roll back Peer review: a pull request reviewed by a teammate, not a committee meeting Automated gates: the CI pipeline enforces tests, security scans, and policy checks — passing the pipeline means meeting the quality bar Fast rollback path: the safety net is not a meeting; it's the ability to revert in minutes This doesn't mean no governance — it means governance is encoded in the pipeline rather than performed in a meeting. The pipeline is stricter than a CAB (checks every change, not a sample) and faster (seconds, not days)."},{"page":"index.html","pageLabel":"Week 1","id":"d2-1","title":"Deployment Strategies — In Depth","body":"2.1.1 Recreate Mechanism[v1 running] → [v1 stopped — DOWNTIME] → [v2 starting] → [v2 running] Failure modeIf v2 fails to start (bad config, crash loop, missing dependency), nothing is running. Recovery means redeploying v1 — another outage cycle. Total outage duration is unpredictable. CostCheapest — you only ever run one set of infrastructure. No duplicate environments, no traffic splitting, no weighted routing. Use whenInternal tools with a maintenance window, batch processing jobs, dev/staging environments where downtime doesn't matter. Never for user-facing production services. 2.1.2 Rolling MechanismStep 1: [v2] [v1] [v1] [v1] ← 1 replaced, 3 still serving on v1 Step 2: [v2] [v2] [v1] [v1] ← 2 replaced Step 3: [v2] [v2] [v2] [v1] ← 3 replaced Step 4: [v2] [v2] [v2] [v2] ← done (~10 minutes total) Failure — mixed versionsBoth v1 and v2 serve traffic simultaneously. The load balancer distributes randomly. If v2 changes an API response format, or expects a DB column v1 doesn't create, you get inconsistent behaviour. Failure — slow rollbackIf v2 is bad, you do another rolling deploy back to v1 — which takes just as long. You've been serving bad responses the whole time. CostLow — never more than N instances total (briefly N-1 mid-swap while one drains). GateHealth check passed (binary: up or down). No metrics analysis, no baseline comparison. Automatic and fast. Use whenStateless services where v1 and v2 are backward-compatible. Read-heavy APIs where a mixed-version window is tolerable. 2.1.3 Blue/Green MechanismBefore: Load balancer → Blue (v1) [4 instances] Green (v2) [4 instances, idle] Switch: Load balancer → Green (v2) [4 instances] Blue (v1) [4 instances, idle] After: Terminate Blue once confident Green is stable (hours/days later) Failure — full blast radius100% of traffic hits v2 instantly on switch. No gradual ramp. A subtle bug appearing only at full load or with real data hits every user at once. Rollback is fast, but the blast radius before you notice is 100%. CostHighest — double infrastructure during the switchover window. At scale (40 servers), you run 80 during deploy. Blue must stay alive for confidence (hours or a full day). Use whenYou need instant, clean rollback. Databases and stateful services where a slow rollback is unaffordable. Frontend assets (new S3 path, switch CloudFront origin — zero mixed versions). Key advantage over rolling: no mixed-version window. Traffic goes entirely to v1 or entirely to v2, never both — matters for services with strict API contract requirements. 2.1.4 Canary MechanismStage 1: [v2: 5%] [v1: 95%] ← watch for 30 min, compare metrics Stage 2: [v2: 25%] [v1: 75%] ← watch for 30 min at higher load Stage 3: [v2: 100%] ← done (~2 hours total) Failure — monitoring blindnessCanary is only as good as your observability. If your dashboard can't detect a 5% error increase (noisy baseline), the bug slips through and you promote to 25%. The gate requires meaningful monitoring. Failure — data corruptionIf the canary writes corrupt data for 5% of users, rolling back the traffic doesn't undo the data damage. CostLow to moderate — instances are replaced in place (like rolling), so server count stays the same. The extra cost is monitoring/observability tooling that makes traffic-split decisions meaningful. GateMetrics comparison — is v2's error rate, p99 latency, and business metrics comparable to v1's baseline? Analysis, not just a health check — often human judgment or automated anomaly detection, with deliberate 15–60 min pauses between steps. Use whenAnything where the blast radius of a bad deploy is expensive — payment services, order processing, authentication. You're trading deployment speed for safety. Difference from rolling: same in-place infrastructure approach, but fundamentally different in speed and decision logic. Rolling replaces automatically in ~10 minutes with only a health-check gate. Canary takes ~2 hours with deliberate pauses and metrics comparison. Rolling asks \"is it alive?\"; canary asks \"is it behaving correctly?\" 2.1.5 Shadow (Dark Launch) MechanismProduction traffic is duplicated — the real request goes to v1 (which serves the actual response), and a copy goes to v2 (which processes it but its response is discarded). You compare v2's outputs and performance against v1's. Failure — write side effectsWorks well for reads (compare search results, recommendations). Dangerous for writes — if v2 processes a duplicated payment request, you've double-charged someone. You must mock all write side effects or only shadow read paths. Failure — resource doublingDoubles compute and network load. Downstream dependencies (databases, external APIs) receive double the traffic and may not be provisioned for it. CostHigh — two full environments and double the compute for every request. Use whenMajor refactors needing proof the new system produces identical results. ML model deployment (shadow the new model, compare predictions). Database migration (compare query results between old and new databases)."},{"page":"index.html","pageLabel":"Week 1","id":"d2-2","title":"Feature Flags as a Deployment Tool","body":"Feature flags decouple deployment (code goes to production) from release (users see the feature): # In your Django view def checkout_view(request): if feature_flags.is_enabled('new_checkout_flow', user=request.user): return new_checkout(request) # new code, deployed but dark else: return old_checkout(request) # existing code, all users see this You deploy this to 100% of instances via a simple rolling deploy. Nobody sees the new checkout yet. Then you flip the flag for 1% of users — a canary release with no infrastructure change. Ramp to 10%, 50%, 100%. If it breaks, flip it off — instant, no redeploy. Why it mattersFeature flags let you use simple, cheap rolling deploys for most changes while still getting canary-like safety for risky features. You don't need blue/green infrastructure to get blue/green-like control. The cost of feature flags Code complexity — branches everywhere, multiple paths to maintain Stale flags — \"temporary\" flags accumulate if not cleaned up Testing burden — you need to test both flag-on and flag-off paths Discipline required — every flag needs an owner, a purpose, and an expiry date Flag types Release flags: temporary, control rollout of a new feature, removed after full rollout Ops flags: semi-permanent, disable expensive features under load (\"circuit breakers\") Experiment flags: A/B testing, comparing user behaviour between variants Permission flags: long-lived, control feature access by user tier (free vs paid)"},{"page":"index.html","pageLabel":"Week 1","id":"d2-3","title":"Backward-Compatible Schema Migrations","body":"The rule: never make a database change that breaks the currently running version of your app. During a rolling deploy, v1 and v2 run simultaneously against the same database. If v2's migration drops a column v1 still reads, v1 crashes. The expand-and-contract pattern — three separate deploys Deploy 1 — Expand Add the new column (e.g. full_name) alongside the old ones. App code dual-writes to both, reads from old columns. v1 still works — it ignores the new column. Safe to roll back: redeploy v1, the new column sits there unused. Deploy 2 — Migrate Backfill full_name for all existing rows. Switch app code to read from the new column. Continue dual-writing. If this fails, old columns still have valid data — safe to roll back. Deploy 3 — Contract (days or weeks later) Stop writing to the old columns, drop them from the schema. This is the point of no return — after this you can't roll back to the old app version. In your Django experience, you might run makemigrations and migrate in a single deploy. In production DevOps, the migration and the app deploy are separate events with days between them. Avoid DROP COLUMN in the same deploy that stops reading from it RENAME COLUMN (old code can't find the column under the new name) ALTER COLUMN to a narrower type (e.g. VARCHAR(255) → VARCHAR(50) — longer existing data gets truncated) Adding a NOT NULL constraint without a default (existing rows fail the constraint)"},{"page":"index.html","pageLabel":"Week 1","id":"d2-4","title":"Why Rollback Is Usually a Lie","body":"Everyone says \"we can just roll back.\" Here's why it's almost never that simple: Scenario 1 — Schema has moved forward v2 ran a migration adding a NOT NULL column. v1's code doesn't write to it. Rolling back means new rows violate the constraint — writes crash. Scenario 2 — Data format has changed v2 started writing addresses in a new JSON structure. v1 expects the old structure and can't parse what v2 wrote while it was live. Scenario 3 — External state has changed v2 sent emails, processed payments, published events. You can roll back your code, but you can't un-send emails or un-charge cards. Scenario 4 — Dependent services have moved forward v2 of service A started sending a new field that service B now depends on. Rolling back service A breaks service B. What real rollback requires A reverse migration script that safely undoes schema changes A data migration that converts v2-written data back to v1's format Identification of every external side effect and a remediation plan for each Testing the rollback path before you need it (which almost nobody does) Honest framingRollback is a forward fix that happens to use the previous version's code. Treat it as a deployment, not an undo button. Plan for it explicitly, test it, and document what it won't fix."},{"page":"index.html","pageLabel":"Week 1","id":"d2-5","title":"Pipeline Anatomy","body":"Build once, promote everywhere (correct) Build (image:abc123) → Dev (abc123) → Staging (abc123) → Prod (abc123) ↑ SAME artifact The CI server builds an image (or JAR, or zip) once, tags it with the commit hash, pushes it to a registry (ECR). That exact, immutable artifact is deployed to dev, promoted to staging, promoted to prod. What changes between environments is configuration, injected at runtime — env vars, secrets from Secrets Manager, connection strings from Parameter Store. The artifact itself is byte-for-byte identical. Rebuild per stage (anti-pattern) Build #1 (image:aaa111) → Dev Build #2 (image:bbb222) → Prod ↑ DIFFERENT artifact — never tested Why it's dangerousBuilds are not perfectly reproducible. A pip install might pull a newer patch version. An apt-get update might include a security patch that changes behaviour. A timestamp or random seed might differ. You've deployed something to production that's never been tested anywhere, even though the source is identical. What may legitimately differ between environments AspectMust be sameMay differ Container image / artifact✓ Deployment pipeline and process✓ Configuration mechanism (env vars from Secrets Manager)✓ Instance size (t3.small vs m5.xlarge)✓ Replica count (1 vs 4)✓ Database (shared dev RDS vs Multi-AZ prod RDS)✓ External integrations (Stripe sandbox vs live)✓ Data (synthetic vs anonymized copy vs real)✓ Monitoring depth (basic vs full + PagerDuty)✓ The rule: the deployment mechanism and the artifact must be identical. Scale, cost, data, and external integration endpoints may differ — those are economics and safety, not correctness. Environment tiers and their purpose Dev Auto-deploys on merge to main. Fast integration feedback. Minimal resources, shared database, synthetic data. If it breaks, nobody outside the team notices. Staging Deploys on promotion from dev (manual trigger or automated after dev smoke tests pass). Catches configuration and integration bugs that only appear at near-production configuration. Same secrets-path structure and deployment process as prod, similar (smaller) infrastructure, sandboxed external services. Production Deploys after manual approval gate from staging. Real users, real data, real money. Full monitoring, PagerDuty alerts, Multi-AZ databases, auto-scaling enabled. Why three tiers, not two: staging catches problems that only appear at prod-like configuration — wrong env vars, misconfigured secrets, connection pool limits, IAM mismatches. Without staging, your first prod-like test is production itself."},{"page":"index.html","pageLabel":"Week 1","id":"d2-6","title":"Multi-Region Architecture","body":"Active-Active Both regions serve live traffic simultaneously. Users route to the closest region by latency (Route 53 latency-based routing). Route 53 (latency-based routing) ├── ap-south-1 — ACTIVE (serving traffic, read-write DB) └── eu-west-1 — ACTIVE (serving traffic, read-write DB) ↕ bi-directional data replication Advantages Near-zero RTO — if one region fails, the other is already serving traffic; DNS just stops sending traffic there Lower latency for geographically distributed users Full utilization of both regions (no idle infrastructure) Challenges Data conflicts: simultaneous updates to the same record in different regions need conflict resolution (last-write-wins, application merge logic, CRDTs) Cost: double infrastructure, all the time (not just during deploys) Complexity: every service must be region-aware, every write must replicate, every cache synchronized or independently warmed Active-Passive One region serves all traffic. A second sits on standby with a read replica and the ability to be promoted on failure. Route 53 (failover routing) ├── ap-south-1 — ACTIVE (all traffic, primary read-write DB) └── eu-west-1 — STANDBY (no traffic, read replica, promoted on failure) → one-way async replication Advantages Simpler — no data conflict resolution needed Cheaper — standby runs minimal infrastructure (DB replica, maybe a cold ASG) Sufficient for most businesses that can tolerate minutes of downtime Challenges Failover time: promoting standby, warming instances, updating DNS takes 15–30 minutes typically Standby rot: untested failover may not work when needed — run failover drills quarterly Data loss: async replication means the standby is always slightly behind — the gap is your RPO Traffic steering (Route 53 routing policies) Latency-based: routes to whichever region has the lowest latency. Used in active-active. Failover: primary/secondary record with a health check; if primary fails, DNS resolves to secondary. Used in active-passive. Geolocation: routes by country/continent — data residency compliance (EU users → EU region). Weighted: sends X% to one region, Y% to another — gradual region migration. RTO and RPO These are business decisions that you implement technically. RTO — Recovery Time Objective Maximum acceptable downtime. \"We cannot be down more than 30 minutes\" → RTO 30 minutes. Drives your DR architecture choice. RPO — Recovery Point Objective Maximum acceptable data loss, in time. \"We can lose at most 5 minutes of data\" → RPO 5 minutes. Drives your replication strategy. TargetArchitectureReplicationCost RTO 4hr, RPO 24hrBackups in another region, restore on failureDaily backup snapshotsLow RTO 30min, RPO 5minActive-passive, async replicationContinuous async (seconds of lag)Moderate RTO ~0, RPO ~0Active-active, sync replicationSynchronous (adds write latency)Very high The honest business conversation: \"You want zero downtime and zero data loss. That costs 2.5x your current infrastructure budget and adds 40ms latency to every write. Are you sure you don't mean 30 minutes and 5 minutes?\" Usually, they do."},{"page":"index.html","pageLabel":"Week 1","id":"d3-1","title":"AWS Global Infrastructure","body":"Regions A region (e.g. ap-south-1 for Mumbai, us-east-1 for N. Virginia) is a cluster of data centres in a geographic area. Your data stays in the region you choose unless you explicitly replicate it. Not every region has every service, and pricing varies. For the ramp plan: ap-south-1 (Mumbai) is close to Dhaka, has all major services, and is cheaper than US regions. us-east-1 gets new services first and has the most documentation. Availability Zones Each region has 2–6 AZs — physically separate buildings with independent power, cooling, and networking, connected by low-latency (<1ms) redundant fiber. If AZ-a loses power, AZ-b and AZ-c keep serving. Critical detailAZ naming is randomized per account. Your ap-south-1a might be a different physical data centre than another account's ap-south-1a — AWS does this to prevent everyone piling into the \"first\" AZ. Use AZ IDs (aps1-az1) for cross-account coordination. Service scope ScopeServicesImplication GlobalIAM, Route 53, CloudFront, WAF (global), S3 (namespace)Created once, works in all regions RegionalVPC, S3 (data), Lambda, RDS, ECS, Secrets Manager, ALBCreated per region — a VPC in Mumbai doesn't exist in London AZ-scopedEC2 instances, EBS volumes, Subnets, NAT GatewaysTied to a specific building — an EBS volume in AZ-a can't attach to an instance in AZ-b Why this matters: when an EC2 instance in AZ-a dies and Auto Scaling launches a replacement in AZ-b, the old EBS volume is stranded. Stateful data should go in RDS or S3 (regional services), not local EBS."},{"page":"index.html","pageLabel":"Week 1","id":"d3-2","title":"Shared Responsibility Model","body":"LayerYour responsibilityAWS's responsibility Application code✓ IAM policies✓ Security group / firewall rules✓ Encryption settings✓ OS patching (EC2)✓ OS patching (Fargate, Lambda, RDS)✓ Network configuration (VPC, subnets)✓ Physical data centre security✓ Hypervisor / host OS✓ Network infrastructure between AZs✓ The spectrum: more managed service = less your responsibility. EC2: you manage everything from the OS up ECS Fargate: AWS manages the host OS; you manage the container Lambda: AWS manages everything except your function code and IAM RDS: AWS patches the engine; you manage access and encryption If your RDS database gets breached because the password was admin123, that's on you. If the underlying hardware fails, that's on AWS."},{"page":"index.html","pageLabel":"Week 1","id":"d3-3","title":"Service Quotas","body":"Default limits that will bite you during the ramp plan: ServiceDefault limitRisk VPCs per region5Shared account with teammate, easy to hit Elastic IPs per region5NAT Gateways each need one EC2 On-Demand vCPU limitVaries by familyAuto Scaling silently fails if you hit this S3 buckets per account100Rarely hit during learning IAM roles per account1,000Not an issue now, critical at enterprise scale Check your limits: aws service-quotas list-service-quotas --service-code ec2 Watch outHitting a quota doesn't always produce a clear error. Auto Scaling may silently fail to launch instances during a traffic spike."},{"page":"index.html","pageLabel":"Week 1","id":"d3-4","title":"Tagging Strategy","body":"Set a convention on day one: Environment = dev | staging | prod Project = ecommerce-platform Owner = fahad CostCenter = ramp-plan-2026 ManagedBy = terraform | manual Why tags matter Cost tracking: see exactly how much dev costs vs staging in the billing console IAM scoping: write policies like \"this role can only manage resources tagged Environment: dev\" Automation: scripts can find and stop all instances tagged Environment: dev every evening to save money Accountability: \"who left this running?\" → check the Owner tag"},{"page":"index.html","pageLabel":"Week 1","id":"d3-5","title":"Budget Alarm Setup","body":"$60 per engineer. Set alerts at 50% and 80%. Top cost traps during learning Forgetting to stop EC2 instances overnight (~$0.10/hr × 12 hours = $1.20/night) NAT Gateways running 24/7 ($0.045/hr = ~$32/month each) RDS instances left running on weekends Forgotten Elastic IPs not attached to a running instance ($0.005/hr each) Cost-saving habit: at the end of each day, check the billing console or run: aws ce get-cost-and-usage \\ --time-period Start=2026-08-27,End=2026-08-31 \\ --granularity DAILY \\ --metrics \"UnblendedCost\" \\ --profile sandbox"},{"page":"index.html","pageLabel":"Week 1","id":"d3-6","title":"AWS CLI with Named Profiles","body":"# Configure aws configure --profile sandbox # Enter access key, secret key, region (ap-south-1), output format (json) # Verify identity aws sts get-caller-identity --profile sandbox # Shows: Account ID, ARN, User ID — the \"whoami\" of AWS # Set a default to avoid typing --profile every time export AWS_PROFILE=sandbox Debugging tipget-caller-identity is your best debugging friend. Whenever something returns AccessDenied, run it first — it tells you who AWS thinks you are. How long does export AWS_PROFILE last? export AWS_PROFILE=sandbox only applies to the current shell session — every aws command you run afterward in that same terminal uses it automatically, with no --profile flag needed. Close the terminal or open a new tab, and it's gone; that new session falls back to the CLI's default profile again. # Make it permanent — every new terminal, not just this session echo 'export AWS_PROFILE=sandbox' >> ~/.zshrc # zsh echo 'export AWS_PROFILE=sandbox' >> ~/.bashrc # bash # Apply it to the terminal you already have open, without restarting source ~/.zshrc OverrideSetting AWS_PROFILE doesn't lock you in — passing --profile other-name on any single command still overrides the environment variable for just that one call. Useful when you're mostly working in one account but need a one-off command against another."},{"page":"index.html","pageLabel":"Week 1","id":"d3-7","title":"IAM — Identity and Access Management","body":"Principals: Users, Groups, and Roles IAM User A permanent identity with long-lived credentials (access key + secret, or console password). Used for human console login. Prefer roles wherever possible — long-lived credentials can be leaked. IAM Group A collection of users. Policies attached to the group are inherited by every member. Never attach policies directly to a user — put the user in a group, attach the policy to the group. Keeps onboarding/offboarding clean. IAM Role A temporary identity that anything can assume — an EC2 instance, a Lambda function, an ECS task, a user from another account. No permanent credentials; issues short-lived tokens (typically 1 hour) via sts:AssumeRole. The preferred way to grant access to services. Policy documents — reading them line by line { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Sid\": \"AllowReadOnlyS3\", \"Effect\": \"Allow\", \"Action\": [ \"s3:GetObject\", \"s3:ListBucket\" ], \"Resource\": [ \"arn:aws:s3:::my-app-bucket\", \"arn:aws:s3:::my-app-bucket/*\" ], \"Condition\": { \"StringEquals\": { \"aws:RequestedRegion\": \"ap-south-1\" } } } ] } Read it aloud: \"Allow getting objects and listing contents, but only in this specific bucket, and only when the request comes from ap-south-1.\" The five fields Effect: Allow or Deny. Deny always wins over Allow. Action: the specific API call(s). Never use * in production. Resource: which specific thing, by ARN. * means all resources — avoid it. Condition: optional constraints — IP range, MFA required, region, tag values. Principal (resource-based policies only): who is granted access. Identity-based vs resource-based policies Identity-based policies attach to a user, group, or role — \"What can this identity do?\" Resource-based policies attach to a resource (S3 bucket policy, SQS queue policy) — \"Who can access this resource?\" They include a Principal field. AWS evaluates both together. For cross-account access you typically need both: a resource policy on the target allowing the external account, and an identity policy on the external role allowing the action. Policy evaluation logic When an API call is made, AWS evaluates in order: Explicit deny check: if any policy anywhere says Deny for this action, it's denied. Game over. Always wins. SCP check (AWS Organizations): organization-level guardrails. If the SCP doesn't allow it, denied. Permission boundary check: the maximum ceiling for this role. Outside the boundary → denied. Allow check: is there at least one identity-based or resource-based policy that explicitly Allows? If yes, allowed; if no, implicit deny. 90% of debugging Default deny: no Allow → denied Explicit Deny always wins, even over Allow Allow must be explicit for the specific action on the specific resource Trust policies vs permission policies Every role has two halves. Trust policy — \"Who can assume this role?\" { \"Version\": \"2012-10-17\", \"Statement\": [{ \"Effect\": \"Allow\", \"Principal\": { \"Service\": \"ecs-tasks.amazonaws.com\" }, \"Action\": \"sts:AssumeRole\" }] } This says: \"ECS tasks can assume this role.\" No human, no Lambda, no EC2 can. The permission policy is \"What can this role do once assumed?\" — the standard document with Actions and Resources. For cross-account, the trust policy says \"Principal\": { \"AWS\": \"arn:aws:iam::OTHER_ACCOUNT:role/auditor\" } — the auditor role from the other account can assume this role. Permission boundaries A policy that sets the maximum a role can ever do, regardless of what other policies are attached: Boundary says: \"At most S3 and CloudWatch\" Identity policy says: \"Allow s3:*, ec2:*, cloudwatch:*\" Effective permissions: s3:* and cloudwatch:* only (ec2:* is outside the boundary) Useful when you let developers create their own roles but want to guarantee they can never exceed a defined scope. Instance profiles and IMDSv2 An instance profile is the wrapper that attaches a role to an EC2 instance. The instance gets temporary credentials by calling the Instance Metadata Service. IMDSv1 — insecurecurl http://169.254.169.254/latest/meta-data/iam/security-credentials/my-role # Returns credentials — no authentication required # This is how the 2019 Capital One breach happened (SSRF → metadata → credentials) IMDSv2 — currentTOKEN=$(curl -X PUT \"http://169.254.169.254/latest/api/token\" \\ -H \"X-aws-ec2-metadata-token-ttl-seconds: 21600\") curl -H \"X-aws-ec2-metadata-token: $TOKEN\" \\ http://169.254.169.254/latest/meta-data/iam/security-credentials/my-role # Requires PUT (SSRF attacks typically can't do PUT) + token Always enforce IMDSv2: --metadata-options \"HttpTokens=required,HttpEndpoint=enabled\" Debugging AccessDenied with CloudTrail Run aws sts get-caller-identity — confirm who you are Check CloudTrail event history for the denied API call The event shows: userIdentity, eventName, resources, errorCode, errorMessage Ask four questions: Does my identity policy Allow this exact action on this exact resource? Does the resource policy (if any) allow my principal? Is there an explicit Deny anywhere overriding the Allow? Am I failing a Condition? (wrong region, no MFA, wrong tag, wrong source IP)"},{"page":"index.html","pageLabel":"Week 1","id":"d3-8","title":"The Three IAM Roles for Assignment A1","body":"Role 1 — Application Role (ECS Tasks) { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Sid\": \"ReadAppAssets\", \"Effect\": \"Allow\", \"Action\": [\"s3:GetObject\"], \"Resource\": \"arn:aws:s3:::ecommerce-assets-prod/*\" }, { \"Sid\": \"ReadSecrets\", \"Effect\": \"Allow\", \"Action\": [\"secretsmanager:GetSecretValue\"], \"Resource\": \"arn:aws:secretsmanager:ap-south-1:*:secret:ecommerce/prod/*\" }, { \"Sid\": \"WriteAppLogs\", \"Effect\": \"Allow\", \"Action\": [\"logs:CreateLogStream\", \"logs:PutLogEvents\"], \"Resource\": \"arn:aws:logs:ap-south-1:*:log-group:/ecs/ecommerce-prod:*\" } ] } JustificationThe app reads assets from S3, reads database credentials from Secrets Manager, and writes its own logs. It does NOT get PutObject, DeleteObject, any IAM actions, or any EC2/infra actions. If this container is compromised, the attacker can read assets and secrets for this one service — they cannot modify infrastructure, delete data, or escalate privileges. Role 2 — Operator Role (Human Engineers) { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Sid\": \"ManageCompute\", \"Effect\": \"Allow\", \"Action\": [ \"ec2:Describe*\", \"ec2:StartInstances\", \"ec2:StopInstances\", \"ecs:UpdateService\", \"ecs:DescribeServices\", \"ecs:RegisterTaskDefinition\" ], \"Resource\": \"*\", \"Condition\": { \"StringEquals\": { \"aws:RequestedRegion\": \"ap-south-1\" } } }, { \"Sid\": \"ManageSecrets\", \"Effect\": \"Allow\", \"Action\": [\"secretsmanager:GetSecretValue\", \"secretsmanager:PutSecretValue\"], \"Resource\": \"arn:aws:secretsmanager:ap-south-1:*:secret:ecommerce/*\" }, { \"Sid\": \"DenyIAMChanges\", \"Effect\": \"Deny\", \"Action\": \"iam:*\", \"Resource\": \"*\" } ] } JustificationOperators deploy (update ECS services, register task definitions), manage instances, and rotate secrets. The explicit Deny on iam:* prevents operators from modifying permissions. The region condition prevents accidental cross-region operations. Role 3 — Read-Only Auditor { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Sid\": \"ReadEverything\", \"Effect\": \"Allow\", \"Action\": [ \"ec2:Describe*\", \"s3:GetObject\", \"s3:ListBucket\", \"rds:Describe*\", \"ecs:Describe*\", \"ecs:List*\", \"logs:GetLogEvents\", \"logs:DescribeLogGroups\", \"cloudtrail:LookupEvents\", \"iam:Get*\", \"iam:List*\" ], \"Resource\": \"*\" }, { \"Sid\": \"DenyAllWrites\", \"Effect\": \"Deny\", \"Action\": [ \"ec2:Run*\", \"ec2:Terminate*\", \"ec2:Create*\", \"ec2:Delete*\", \"s3:PutObject\", \"s3:DeleteObject\", \"iam:Create*\", \"iam:Delete*\", \"iam:Put*\", \"iam:Attach*\" ], \"Resource\": \"*\" } ] } JustificationAuditors see everything (logs, configs, IAM, CloudTrail) to verify compliance. Explicit Deny on all mutations ensures that even if someone attaches an additional policy, the role still can't modify anything."},{"page":"index.html","pageLabel":"Week 1","id":"d3-9","title":"ARNs — Amazon Resource Names","body":"An ARN is the unique address of any resource in all of AWS — like a postal address for your house, but for AWS resources. Every ARN follows the same pattern: arn:aws:s3:::my-app-bucket │ │ │ │ │ └── Resource name (the bucket itself) │ │ │ │ └── Account ID (empty for S3 — buckets are globally unique) │ │ │ └── Region (empty for S3 — it's a global namespace) │ │ └── Service (s3) │ └── Partition (aws = standard, aws-cn = China, aws-us-gov = GovCloud) └── Prefix (always \"arn\") The full format is: arn:aws:service:region:account-id:resource Real examples from your account ResourceARN Your S3 bucketarn:aws:s3:::my-app-bucket An object inside itarn:aws:s3:::my-app-bucket/images/logo.png Your EC2 instancearn:aws:ec2:ap-south-1:111122223333:instance/i-0123456789abcdef0 An IAM rolearn:aws:iam::111122223333:role/my-app-role An RDS databasearn:aws:rds:ap-south-1:111122223333:db:my-database Some fields are empty for global services: S3 No region, no account ID — bucket names are globally unique across all of AWS, so nobody else can have the same bucket name as you. IAM No region — IAM is global, your roles work in every region. EC2, RDS All fields filled — they exist in a specific region, in a specific account. Why ARNs matter for you Remember the IAM policies from 3.7? The Resource field uses ARNs to say exactly which thing the policy applies to: { \"Effect\": \"Allow\", \"Action\": \"s3:GetObject\", \"Resource\": \"arn:aws:s3:::my-app-bucket/*\" } This says \"allow reading objects, but only from this specific bucket.\" The /* at the end means \"all objects inside it.\" Without the ARN, AWS wouldn't know which bucket you're talking about — there could be millions of S3 buckets across all AWS accounts. Finding a resource's ARN Usually shown at the top of its detail page in the console, or via CLI: # Your EC2 instance's ARN aws ec2 describe-instances \\ --instance-ids i-0123456789abcdef0 \\ --query \"Reservations[].Instances[].{ARN:InstanceId}\" \\ --output text"},{"page":"index.html","pageLabel":"Week 1","id":"d3-10","title":"Condition Keys in Practice — Restricting by Location","body":"Say the goal is \"only allow access from Bangladesh.\" You'd use the aws:SourceIp condition with Bangladesh's public IP ranges: { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Sid\": \"AllowOnlyFromBangladesh\", \"Effect\": \"Deny\", \"Action\": \"*\", \"Resource\": \"*\", \"Condition\": { \"NotIpAddress\": { \"aws:SourceIp\": [ \"103.0.0.0/8\", \"114.128.0.0/10\", \"202.4.96.0/19\", \"103.48.16.0/20\" ] } } } ] } Read it aloud: \"Deny everything if the request is NOT coming from these IP ranges.\" Why Deny + NotIpAddressExplicit Deny always wins. An Allow-based rule could be bypassed by attaching another policy that allows access from anywhere. A Deny-based rule can't be overridden — it blocks non-BD traffic no matter what other policies exist. Country-level filteringBangladesh doesn't have one clean CIDR block. IP ranges are scattered across many blocks assigned to ISPs like Grameenphone, Banglalink, Robi, etc. The example above is simplified — a real implementation would need dozens of CIDR ranges that change over time. A better approach for \"only from Bangladesh\" Instead of tracking IP ranges, AWS has cleaner options. Option 1 — VPN, the production answer. All access goes through a VPN; no public IP filtering needed. If you're on the VPN, you're authorized — if not, you can't reach anything: Your laptop (Dhaka) → VPN → AWS VPC (private network) Option 2 — restrict to specific known IPs, more practical than \"all of Bangladesh\": your office network and home ISP, two known, stable IPs instead of an entire country's worth of ranges. { \"Condition\": { \"IpAddress\": { \"aws:SourceIp\": [ \"103.123.45.0/24\", \"114.56.78.0/24\" ] } } } Option 3 — region restriction, if the real goal is \"keep my resources in the nearest region\" rather than \"control who connects\": { \"Condition\": { \"StringEquals\": { \"aws:RequestedRegion\": \"ap-south-1\" } } } This prevents anyone — even you — from accidentally creating resources in us-east-1 or eu-west-1. It doesn't restrict who can access; it restricts where resources can exist. Other useful conditions you'll use in the ramp plan ConditionWhat it doesExample use aws:SourceIpRestrict by IP addressOffice network only aws:RequestedRegionRestrict by regionOnly ap-south-1 aws:MultiFactorAuthPresentRequire MFASensitive operations need 2FA aws:PrincipalTagCheck user's tagsOnly team=devops can access ec2:ResourceTagCheck resource's tagsCan only manage resources tagged Environment=dev aws:CurrentTimeRestrict by timeNo deploys outside business hours Worth noting: a policy that says \"operators can deploy only between 10am and 6pm Bangladesh time,\" using aws:CurrentTime, is a real pattern for reducing late-night risky changes."},{"page":"index.html","pageLabel":"Week 1","id":"d3-11","title":"IAM Actions Reference","body":"Every distinct Action string used across the three IAM roles in 3.8, grouped by service — what each one does, and whether it shows up as an Allow or an explicit Deny. ServiceActionWhat it doesEffectUsed in S3s3:GetObjectRead an objectAllow3.7, Role 1 S3s3:ListBucketList a bucket's contentsAllow3.7, Role 3 S3s3:PutObjectWrite an objectDenyRole 3 S3s3:DeleteObjectDelete an objectDenyRole 3 Secrets Managersecretsmanager:GetSecretValueRead a secretAllowRole 1, Role 2 Secrets Managersecretsmanager:PutSecretValueWrite / rotate a secretAllowRole 2 CloudWatch Logslogs:CreateLogStreamOpen a new log streamAllowRole 1 CloudWatch Logslogs:PutLogEventsWrite log linesAllowRole 1 CloudWatch Logslogs:GetLogEventsRead log linesAllowRole 3 CloudWatch Logslogs:DescribeLogGroupsList log groupsAllowRole 3 EC2ec2:Describe*Any read-only Describe callAllowRole 2, Role 3 EC2ec2:StartInstancesStart a stopped instanceAllowRole 2 EC2ec2:StopInstancesStop a running instanceAllowRole 2 EC2ec2:Run*Launch new instances (RunInstances)DenyRole 3 EC2ec2:Terminate*Terminate instancesDenyRole 3 EC2ec2:Create*Create EC2 resources (volumes, snapshots, etc.)DenyRole 3 EC2ec2:Delete*Delete EC2 resourcesDenyRole 3 ECSecs:Describe*Any read-only Describe callAllowRole 3 ECSecs:List*Any read-only List callAllowRole 3 ECSecs:DescribeServicesRead service statusAllowRole 2 ECSecs:UpdateServiceRoll out a new task definition to a serviceAllowRole 2 ECSecs:RegisterTaskDefinitionRegister a new task definition revisionAllowRole 2 IAMiam:*Every IAM action, no exceptionsDenyRole 2 IAMiam:Get*Read a single IAM resourceAllowRole 3 IAMiam:List*List IAM resourcesAllowRole 3 IAMiam:Create*Create IAM resources (users, roles, policies)DenyRole 3 IAMiam:Delete*Delete IAM resourcesDenyRole 3 IAMiam:Put*Write inline policies / configDenyRole 3 IAMiam:Attach*Attach a managed policy to a principalDenyRole 3 RDSrds:Describe*Any read-only Describe callAllowRole 3 CloudTrailcloudtrail:LookupEventsSearch the audit logAllowRole 3 STSsts:AssumeRoleAssume a role and get temporary credentialsAllow3.7, Trust policies Reading tipA trailing * is a wildcard within that action namespace — ec2:Describe* covers DescribeInstances, DescribeVolumes, every Describe call EC2 has, present or future. iam:* is the widest wildcard here: every action in the entire IAM service."},{"page":"index.html","pageLabel":"Week 1","id":"d4-1","title":"What a VPC Is","body":"A VPC (Virtual Private Cloud) is your own isolated network within an AWS region — think of it as your private data centre network in the cloud. Nothing can enter or leave your VPC unless you explicitly allow it. When you create a VPC, you define a CIDR block — the total pool of private IP addresses available to everything inside it. VPC — public and private subnets Internet Internet Gateway VPC 10.0.0.0/16 — fahad-devops-vpc Public — 10.0.1.0/24 ALB · NAT Gateway Private — 10.0.10.0/24 EC2 · RDS Public subnet — has a route to the Internet Gateway Private subnet — no direct internet route The real build (4.8) uses 3 tiers × 2 AZs = 6 subnets. See 4.9 for the full picture."},{"page":"index.html","pageLabel":"Week 1","id":"d4-2","title":"CIDR Planning on Paper","body":"CIDR (Classless Inter-Domain Routing) notation defines a range of IP addresses. Before the mental math, here's what's actually happening under the hood. An IP address is just 32 bits (ones and zeros) You see 10.0.0.0, but the computer sees: 10 . 0 . 0 . 0 ↓ ↓ ↓ ↓ 00001010.00000000.00000000.00000000 Each of the 4 numbers (called octets) is 8 bits. 4 × 8 = 32 bits total. The range of each octet is 0–255 because 8 bits can represent 2⁸ = 256 values (00000000 to 11111111). The / number = how many bits are locked The number after / tells you: how many bits from the left are fixed (the network part). The remaining bits are yours to use for individual addresses (the host part). The / number = how many bits are LOCKED from the left /16 00001010.00000000 . xxxxxxxx.xxxxxxxx 16 bits locked (10.0) 16 bits free → 2¹⁶ = 65,536 /24 00001010.00000000.00000000 . xxxxxxxx 24 bits locked (10.0.0) 8 bits free → 2⁸ = 256 /28 00001010.00000000.00000000.0000 xxxx 28 bits locked 4 bits free → 2⁴ = 16 = Network part (locked — same for all addresses in this range) = Host part (free — each combination = one unique address) The formula Total addresses = 2 ^ (32 − the / number) Quick mental shortcut Bigger / number = fewer addresses (more bits locked) · Smaller / number = more addresses How the calculation works Each free bit can be either 0 or 1 — two choices. With multiple free bits, you multiply the choices together: /16 → 32 − 16 = 16 free bits 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 └──────────────────────── 16 times ────────────────────────────────┘ = 2¹⁶ = 65,536 addresses /24 → 32 − 24 = 8 free bits 2 × 2 × 2 × 2 × 2 × 2 × 2 × 2 └────────── 8 times ───────────┘ = 2⁸ = 256 addresses /28 → 32 − 28 = 4 free bits 2 × 2 × 2 × 2 └─ 4 times ──┘ = 2⁴ = 16 addresses Think of it like a combination lock. A 4-digit lock where each digit is 0–9 has 10 × 10 × 10 × 10 = 10,000 combinations. Same idea here — but each \"digit\" is a bit (only 0 or 1), so it's 2 instead of 10, multiplied by itself for each free bit. Walking through /24 step by step 10.0.1.0/24 means: 10 . 0 . 1 . 0 Fixed Fixed Fixed Free (this octet can be anything 0-255) 24 bits are locked = the first 3 octets (10.0.1) never change. The last octet (8 bits) is free, so it ranges from 0 to 255: First address: 10.0.1.0 10.0.1.1 10.0.1.2 ... 10.0.1.254 Last address: 10.0.1.255 Total: 256 addresses Now /16 10.0.0.0/16 means: 10 . 0 . 0 . 0 Fixed Fixed Free Free (last 2 octets can be anything) 16 bits locked = first 2 octets (10.0) never change. The last 2 octets are free: First address: 10.0.0.0 10.0.0.1 ... 10.0.0.255 10.0.1.0 ← third octet increments 10.0.1.1 ... 10.0.255.254 Last address: 10.0.255.255 Total: 256 × 256 = 65,536 addresses And /28 10.0.0.0/28 means 28 bits are locked — that's 3 full octets (24 bits) plus 4 more bits into the last octet. Only 4 bits are free: Last octet in binary: 0000xxxx ↑ only these 4 bits change xxxx can be: 0000 (0) to 1111 (15) First address: 10.0.0.0 Last address: 10.0.0.15 Total: 2⁴ = 16 addresses The cheat sheet you'll actually use Every CIDR from /16 to /32. The \"third octet range\" column is for mid-octet splits — the tricky ones. Pin this somewhere visible. CIDRFree bitsTotal addressesUsable (AWS)3rd octet: free bits3rd octet rangeUsed for /161665,53665,5318 (fully free)0 – 255VPC level /171532,76832,76370 – 127Half a VPC /181416,38416,37960 – 63Large subnet /19138,1928,18750 – 31Large subnet /20124,0964,09140 – 15Large subnet /21112,0482,04330 – 7Medium subnet /22101,0241,01920 – 3Medium subnet /23951250710 – 1Medium subnet /2482562510 (locked)—Standard subnet (most common) /2571281234th octet split: 0 – 127Small subnet /26664594th octet split: 0 – 63Small subnet /27532274th octet split: 0 – 31Small subnet /28416114th octet split: 0 – 15AWS minimum subnet /293834th octet split: 0 – 7Tiny (only 3 usable!) /3024—4th octet split: 0 – 3Point-to-point link /3112—4th octet split: 0 – 1Point-to-point link (RFC 3021) /3201—Single IPSecurity group rules, host routes PatternEach step down in the CIDR number doubles the addresses. /24 = 256, /23 = 512, /22 = 1,024. Each step up halves them. /24 = 256, /25 = 128, /26 = 64. Mid-octet formulaFor /17 to /23 (third octet splits): spill = /number − 16. Free bits in 3rd octet = 8 − spill. Max value = 2^free − 1.For /25 to /31 (fourth octet splits): same idea but spill = /number − 24. AWS reserves 5 IPs in every subnet .0 — network address .1 — VPC router .2 — DNS server .3 — reserved for future use .255 — broadcast (not supported in VPC, but reserved) So a /24 subnet (256 addresses) actually has 251 usable. How this applies to your VPC on Day 4 VPC: 10.0.0.0/16 ← your entire network: 65,536 addresses │ ├── 10.0.1.0/24 ← public subnet AZ-a: 256 addresses (251 usable) ├── 10.0.2.0/24 ← public subnet AZ-b: 256 addresses ├── 10.0.10.0/24 ← private app AZ-a: 256 addresses ├── 10.0.11.0/24 ← private app AZ-b: 256 addresses ├── 10.0.20.0/24 ← private DB AZ-a: 256 addresses └── 10.0.21.0/24 ← private DB AZ-b: 256 addresses 6 subnets × 256 = 1,536 used out of 65,536 available Plenty of room to add more subnets later The VPC is /16 (big pool), and you carve it into /24 subnets (smaller pools) — like dividing a large office floor into rooms: the floor is the VPC, each room is a subnet. Why this layout Gaps between ranges (1–2, 10–11, 20–21) leave room for future subnets Two AZs for fault tolerance (if AZ-a dies, AZ-b keeps running) Three tiers (public, app, database) for network-level isolation The 10.0.x.0 numbering makes it easy to identify the tier at a glance Practice CIDR & Subnetting Quiz Q1 How many total IP addresses are in 10.0.0.0/24? A128 B256 C512 D1,024 ✓ Correct — B) 256 32 − 24 = 8 free bits. 2⁸ = 256 addresses. A) 128 — This would be 2⁷, which is a /25 (32 − 7 = 25), not /24. B) 256 — ✓ Correct. 32 − 24 = 8 free bits → 2⁸ = 256. C) 512 — This would be 2⁹, which is a /23 (32 − 9 = 23). D) 1,024 — This would be 2¹⁰, which is a /22. Q2 How many usable IP addresses in a /24 subnet on AWS? A256 B254 C251 D250 ✓ Correct — C) 251 256 total − 5 reserved by AWS = 251 usable. A) 256 — This is the total count, not usable. AWS reserves 5 IPs in every subnet. B) 254 — This would be correct in traditional networking (just .0 and .255 reserved). But AWS reserves 5: .0 (network), .1 (router), .2 (DNS), .3 (future), .255 (broadcast). C) 251 — ✓ Correct. 256 − 5 = 251 usable. D) 250 — Off by one. Only 5 are reserved, not 6. Q3 What is the IP range of 10.0.5.0/24? A10.0.5.0 – 10.0.5.255 B10.0.0.0 – 10.0.5.255 C10.0.5.0 – 10.0.6.0 D10.0.5.0 – 10.0.5.128 ✓ Correct — A) 10.0.5.0 – 10.0.5.255 /24 locks the first 3 octets (10.0.5). Only the last octet is free (0–255). A) 10.0.5.0 – 10.0.5.255 — ✓ Correct. /24 = first 3 octets locked, last octet free (0–255). B) 10.0.0.0 – 10.0.5.255 — Wrong. This starts at 10.0.0.0, but the CIDR starts at 10.0.5.0. The third octet is locked at 5, not ranging 0–5. C) 10.0.5.0 – 10.0.6.0 — Wrong. This crosses into the next subnet (10.0.6.x). A /24 stays within one value of the third octet. D) 10.0.5.0 – 10.0.5.128 — Wrong. This is only half the range. A /24 goes up to .255, not .128. A range ending at .128 would be closer to a /25. Q4 How many addresses in a /20 subnet? A1,024 B2,048 C4,096 D8,192 ✓ Correct — C) 4,096 32 − 20 = 12 free bits. 2¹² = 4,096. A) 1,024 — This is 2¹⁰ = /22 (32 − 10 = 22). B) 2,048 — This is 2¹¹ = /21 (32 − 11 = 21). C) 4,096 — ✓ Correct. 2¹² = /20. D) 8,192 — This is 2¹³ = /19 (32 − 13 = 19). Q5 Which CIDR gives you exactly 16 addresses? A/26 B/27 C/28 D/30 ✓ Correct — C) /28 16 = 2⁴. You need 4 free bits. 32 − 4 = 28. A) /26 — 32 − 26 = 6 free bits → 2⁶ = 64 addresses. Too many. B) /27 — 32 − 27 = 5 free bits → 2⁵ = 32 addresses. Still too many. C) /28 — ✓ Correct. 32 − 28 = 4 free bits → 2⁴ = 16. D) /30 — 32 − 30 = 2 free bits → 2² = 4 addresses. Too few. Q6 You have 10.0.0.0/16 as your VPC. Can 10.0.3.0/24 be a subnet inside it? AYes — 10.0.3.0 falls within the 10.0.0.0/16 range BNo — /24 is larger than /16 CNo — the third octet must be 0 DYes — but only if it's in the same AZ ✓ Correct — A) Yes This is purely a math question: does the subnet's range fit inside the VPC's range? A) Yes — falls within range — ✓ Correct. VPC range is 10.0.0.0–10.0.255.255. Subnet range is 10.0.3.0–10.0.3.255. It fits. B) No — /24 is larger than /16 — Backwards. Larger / number = smaller subnet. /24 (256 IPs) is smaller than /16 (65,536 IPs), so it fits inside. C) No — third octet must be 0 — Wrong. The /16 VPC covers ALL values of the third octet (0–255). 10.0.3.x, 10.0.99.x, 10.0.200.x all fit. D) Yes — but only if same AZ — The AZ part is wrong. CIDR validity is pure math — the IPs either fit or they don't, regardless of which AZ you place the subnet in. You choose the AZ after the CIDR is validated. Q7 Can these two subnets coexist in the same VPC?10.0.1.0/24 and 10.0.1.128/25 AYes — they're different CIDR blocks BNo — they overlap (10.0.1.128–255 is in both) CYes — /25 is smaller so it fits inside /24 DNo — you can't use /25 in AWS ✓ Correct — B) No, they overlap Write out both ranges — the overlap becomes obvious. A) Yes — different CIDRs — Different CIDR notation doesn't mean non-overlapping. 10.0.1.0/24 covers .0–.255; 10.0.1.128/25 covers .128–.255. The .128–.255 range exists in both — that's an overlap. AWS rejects this. B) No — they overlap — ✓ Correct. /24 = 10.0.1.0–10.0.1.255. /25 = 10.0.1.128–10.0.1.255. The second range is entirely inside the first. C) Yes — /25 fits inside /24 — \"Fits inside\" is exactly the problem! Two subnets cannot overlap — each IP address must belong to exactly one subnet. The /25 is a subset of the /24, so they conflict. D) No — can't use /25 — You can use /25 in AWS. The minimum is /28. The problem is the overlap, not the prefix length. Q8 You need a subnet for 100 EC2 instances on AWS. What's the smallest CIDR that fits? A/25 (128 addresses, 123 usable) B/24 (256 addresses, 251 usable) C/26 (64 addresses, 59 usable) D/27 (32 addresses, 27 usable) ✓ Correct — A) /25 You need 100 instances + 5 AWS reserved = 105 minimum. Find the smallest power of 2 that's ≥ 105. A) /25 — 128 total, 123 usable — ✓ Correct. 123 usable ≥ 100 needed. This is the smallest CIDR that works. B) /24 — 256 total, 251 usable — Works, but wastes 151 addresses. /25 is sufficient and more efficient. C) /26 — 64 total, 59 usable — Too small. 59 usable < 100 needed. You'd run out of IPs. D) /27 — 32 total, 27 usable — Way too small. Only 27 usable IPs for 100 instances. Q9 What is the IP range of 172.16.0.0/20? A172.16.0.0 – 172.16.0.255 B172.16.0.0 – 172.16.15.255 C172.16.0.0 – 172.16.31.255 D172.16.0.0 – 172.16.255.255 ✓ Correct — B) 172.16.0.0 – 172.16.15.255 /20 locks 20 bits. The first 2 octets use 16 bits, leaving 4 more locked bits in the third octet. The third octet is split: upper 4 bits locked, lower 4 bits free. A) 172.16.0.0 – 172.16.0.255 — This is a /24 (only 256 addresses). /20 is much larger — 4,096 addresses. The third octet isn't locked at 0; it can range 0–15. B) 172.16.0.0 – 172.16.15.255 — ✓ Correct. 4 free bits in the third octet → 2⁴ − 1 = 15 → range 0–15. Fourth octet fully free (0–255). Total: 16 × 256 = 4,096. C) 172.16.0.0 – 172.16.31.255 — This would be /19. In /19: 32 − 19 = 13 free bits. Third octet has 5 free bits → 2⁵ − 1 = 31. Close, but one bit off from /20. D) 172.16.0.0 – 172.16.255.255 — This is a /16 (entire third and fourth octets free). /20 locks 4 bits of the third octet, so it can only go up to 15, not 255. Q10 Your VPC is 10.0.0.0/16. How many /24 subnets can it hold? A16 B64 C256 D512 ✓ Correct — C) 256 Divide the VPC's total by the subnet's size: 65,536 ÷ 256 = 256. A) 16 — This would be correct if each subnet were /20 (4,096 addresses). 65,536 ÷ 4,096 = 16. B) 64 — This would be /24-sized subnets in a /18 VPC. 16,384 ÷ 256 = 64. C) 256 — ✓ Correct. /16 = 65,536 addresses. /24 = 256 addresses. 65,536 ÷ 256 = 256. The third octet (0–255) gives exactly 256 possible /24 blocks. D) 512 — This would be /25-sized subnets (128 each) in a /16 VPC. 65,536 ÷ 128 = 512. Reset Quiz Deep Practice CIDR — Building from Basics to Mid-Octet Splits Q1 How many total bits are in an IPv4 address? A16 bits B32 bits C64 bits D128 bits ✓ Correct — B) 32 bits An IP address is 4 octets × 8 bits = 32 bits. This never changes for IPv4. A) 16 bits — This is only 2 octets. An IPv4 address has 4 octets. B) 32 bits — ✓ Correct. 4 octets × 8 bits = 32. C) 64 bits — This would be 8 octets. IPv4 only has 4. D) 128 bits — This is the size of an IPv6 address, not IPv4. Q2 In 10.0.0.0/24, what does the /24 mean? AThe number of servers allowed BThe total number of addresses CHow many bits are locked from the left (the network part) DThe cost tier of the subnet ✓ Correct — C) Bits locked from the left The / number tells you how many of the 32 bits are fixed (the network part). The remaining bits are free for host addresses. A) Number of servers — No, the / has nothing to do with server count directly. It defines the address range size. B) Total addresses — Close idea, but the / number isn't the count itself. It's the locked bits. You calculate addresses from it: 2^(32 − /number). C) Bits locked from the left — ✓ Correct. /24 = 24 bits locked, 8 bits free. D) Cost tier — No, CIDR has nothing to do with pricing. Q3 If 24 bits are locked, how many bits are FREE? A8 B24 C16 D4 ✓ Correct — A) 8 32 total − 24 locked = 8 free bits. This subtraction is the first step of every CIDR calculation. A) 8 — ✓ Correct. 32 − 24 = 8. B) 24 — That's the locked bits, not the free bits. You subtracted the wrong way. C) 16 — This would be 32 − 16, which is a /16, not /24. D) 4 — This would be 32 − 28, which is a /28. Q4 If you have 3 free bits, how many addresses? (2 × 2 × 2 = ?) A6 B8 C9 D12 ✓ Correct — B) 8 Each bit has 2 choices (0 or 1). You multiply: 2 × 2 × 2 = 8. A) 6 — You might have added: 2 + 2 + 2 = 6. But bits multiply, not add. B) 8 — ✓ Correct. 2 × 2 × 2 = 2³ = 8. C) 9 — You might be thinking 3² = 9. But it's 2³, not 3². D) 12 — No standard operation on 2 and 3 gives 12 here. Q5 How many addresses in a /26? (Two steps: free bits, then 2^free) A26 B32 C64 D128 ✓ Correct — C) 64 Step 1: 32 − 26 = 6 free bits. Step 2: 2⁶ = 64. A) 26 — That's the / number itself, not the address count. Don't confuse the two. B) 32 — This is 2⁵ = /27. One bit off. C) 64 — ✓ Correct. 2⁶ = 2×2×2×2×2×2 = 64. D) 128 — This is 2⁷ = /25. You subtracted one too many. Q6 How many addresses in a /25? A128 B64 C256 D32 ✓ Correct — A) 128 32 − 25 = 7 free bits. 2⁷ = 128. A) 128 — ✓ Correct. 2⁷ = 128. B) 64 — This is 2⁶ = /26. One bit too few. C) 256 — This is 2⁸ = /24. One bit too many. D) 32 — This is 2⁵ = /27. Two bits too few. Q7 Which is SMALLER: /24 or /28? A/28 is bigger — it has more addresses B/28 is smaller — it has fewer addresses (16 vs 256) CThey're the same size DIt depends on the region ✓ Correct — B) /28 is smaller Bigger / number = more locked bits = fewer free bits = fewer addresses = smaller subnet. A) /28 is bigger — This is the most common CIDR mistake. The / number and the size go in opposite directions. /28 locks more bits, so fewer addresses. B) /28 is smaller — ✓ Correct. /28 = 2⁴ = 16 addresses. /24 = 2⁸ = 256 addresses. C) Same size — No, different / numbers always give different sizes. D) Depends on region — CIDR is pure math. Region doesn't affect it. Q8 What is the range of 10.0.5.0/24? A10.0.5.0 – 10.0.5.255 B10.0.0.0 – 10.0.5.255 C10.0.5.0 – 10.0.255.255 D10.5.0.0 – 10.5.255.255 ✓ Correct — A) 10.0.5.0 – 10.0.5.255 /24 locks 3 octets. The third octet is 5 and stays 5. Only the last octet is free: 0–255. A) 10.0.5.0 – 10.0.5.255 — ✓ Correct. Three octets locked (10.0.5), last octet free (0–255). B) 10.0.0.0 – 10.0.5.255 — Wrong start address. The CIDR starts at 10.0.5.0, not 10.0.0.0. The third octet is locked at 5. C) 10.0.5.0 – 10.0.255.255 — This frees the third octet (5–255). That would be a /16 starting at 10.0.5.0, not a /24. D) 10.5.0.0 – 10.5.255.255 — The 5 moved to the second octet. The original has 5 in the third octet position. Q9 What is the range of 10.0.0.0/16? A10.0.0.0 – 10.0.0.255 B10.0.0.0 – 10.0.16.255 C10.0.0.0 – 10.0.255.255 D10.0.0.0 – 10.255.255.255 ✓ Correct — C) 10.0.0.0 – 10.0.255.255 /16 locks 2 octets (10.0). The third AND fourth octets are both free (each 0–255). A) 10.0.0.0 – 10.0.0.255 — This is a /24 (only last octet free). /16 frees two octets. B) 10.0.0.0 – 10.0.16.255 — The \"16\" doesn't go into the IP range. /16 means 16 bits locked, which frees the entire third and fourth octets. C) 10.0.0.0 – 10.0.255.255 — ✓ Correct. Two octets locked (10.0), two free (0–255 each). D) 10.0.0.0 – 10.255.255.255 — This frees three octets, which would be a /8, not /16. Q10 /16 locks 16 bits. How many full octets is that? A1 full octet (the first) B2 full octets (the first two) C3 full octets D16 octets ✓ Correct — B) 2 full octets 16 bits ÷ 8 bits per octet = exactly 2 octets. This is a clean octet boundary — no splitting. A) 1 octet — 1 octet = 8 bits. That's a /8, not /16. B) 2 octets — ✓ Correct. 16 ÷ 8 = 2. Clean boundary. C) 3 octets — 3 octets = 24 bits. That's a /24. D) 16 octets — Octets and bits are different. 16 bits = 2 octets, not 16 octets. Q11 /20 locks 20 bits. The first 2 octets use 16 bits. Where do the remaining 4 locked bits go? AExactly 2 full octets, nothing more B2 full octets + 4 bits into the third octet C2 full octets + the entire third octet D20 octets ✓ Correct — B) 2 octets + 4 bits into the third 20 − 16 = 4 extra bits that spill into the third octet. The third octet is now SPLIT: 4 bits locked, 4 bits free. This is what makes mid-octet CIDRs like /20 tricky. A) Exactly 2 octets — That would only be 16 bits. We have 20 to lock — 4 more bits need to go somewhere. B) 2 octets + 4 bits into the third — ✓ Correct. 16 + 4 = 20. The third octet is split in half. C) 2 octets + entire third — The entire third octet would be 8 more bits = 24 total. That's a /24, not /20. D) 20 octets — An IPv4 address only has 4 octets total. Q12 In a /20, the third octet has 4 locked bits and 4 free bits. What values can the third octet be? AOnly 0 (it's fully locked) B0 – 3 (4 values) C0 – 15 (16 values) D0 – 255 (fully free) ✓ Correct — C) 0 – 15 4 free bits = 2⁴ = 16 possible values. Range: 0 to 15. A) Only 0 — It's not fully locked — 4 bits are free. Zero free bits would mean a value of only 0. B) 0 – 3 — 4 values = 2² = 2 free bits. But we have 4 free bits, not 2. Don't confuse the number of free bits (4) with the number of values from 2 free bits (4). C) 0 – 15 — ✓ Correct. 4 free bits → 2⁴ = 16 values → 0 through 15. D) 0 – 255 — That would mean all 8 bits are free, which is a /16. In /20, only 4 of the 8 bits in the third octet are free. Q13 Now put it together: what is the range of 172.16.0.0/20? A172.16.0.0 – 172.16.0.255 B172.16.0.0 – 172.16.15.255 C172.16.0.0 – 172.16.31.255 D172.16.0.0 – 172.16.255.255 ✓ Correct — B) 172.16.0.0 – 172.16.15.255 Combine everything from the previous questions: first 2 octets locked (172.16). Third octet: 0–15 (4 free bits). Fourth octet: 0–255 (fully free). A) 172.16.0.0 – 172.16.0.255 — This is a /24 (256 addresses). It only uses the fourth octet. /20 is much bigger — 4,096 addresses spread across 16 values of the third octet. B) 172.16.0.0 – 172.16.15.255 — ✓ Correct. Third octet: 0–15 (from Q12). Fourth octet: 0–255. Total: 16 × 256 = 4,096. C) 172.16.0.0 – 172.16.31.255 — 31 = 2⁵ − 1 = 5 free bits in third octet. That's /19 (19 locked = 16 + 3), not /20 (16 + 4). One extra free bit. D) 172.16.0.0 – 172.16.255.255 — 255 means the entire third octet is free = /16. The /20 locks 4 bits of the third octet, limiting it to 0–15. Q14 Same method — what is the range of 192.168.0.0/19? A192.168.0.0 – 192.168.15.255 B192.168.0.0 – 192.168.19.255 C192.168.0.0 – 192.168.31.255 D192.168.0.0 – 192.168.63.255 ✓ Correct — C) 192.168.0.0 – 192.168.31.255 /19: 19 − 16 = 3 bits locked in third octet. 8 − 3 = 5 free bits. 2⁵ = 32 values = 0–31. A) ...15.255 — 15 = 2⁴ − 1 = 4 free bits in third octet. That's /20, not /19. B) ...19.255 — 19 is the / number, not the max of the third octet. Don't put the / number into the range. C) ...31.255 — ✓ Correct. 5 free bits → 2⁵ = 32 → max value = 31. D) ...63.255 — 63 = 2⁶ − 1 = 6 free bits. That's /18 (16 + 2 locked in third), not /19. Q15 What is the range and size of 10.1.0.0/21? A10.1.0.0 – 10.1.0.255 (256 addresses) B10.1.0.0 – 10.1.3.255 (1,024 addresses) C10.1.0.0 – 10.1.15.255 (4,096 addresses) D10.1.0.0 – 10.1.7.255 (2,048 addresses) ✓ Correct — D) 10.1.0.0 – 10.1.7.255 (2,048) /21: 21 − 16 = 5 bits locked in third octet. 8 − 5 = 3 free bits. 2³ = 8 values = 0–7. Fourth octet fully free. Total: 8 × 256 = 2,048. A) ...0.255 (256) — This is a /24. Only the fourth octet is free. /21 has 3 free bits in the third octet too. B) ...3.255 (1,024) — 3 = 2² − 1 = 2 free bits in third octet = /22 (16 + 6). Close, but one bit off from /21. C) ...15.255 (4,096) — 15 = 2⁴ − 1 = 4 free bits = /20. /21 has one fewer free bit. D) ...7.255 (2,048) — ✓ Correct. 3 free bits → 2³ = 8 → max value 7. Total: 8 × 256 = 2,048. Reset Quiz"},{"page":"index.html","pageLabel":"Week 1","id":"d4-3","title":"Subnet Types: Public vs Private","body":"Public subnet Has a route to the Internet Gateway. Instances can have public IPs and be reached from the internet (if the security group allows it). Used for load balancers, bastion hosts, NAT gateways. Private subnet No direct route to the internet. Instances can't be reached from outside; they access the internet through a NAT Gateway in a public subnet. Used for application servers, databases, anything that shouldn't be directly internet-accessible. Golden ruleDatabases and application servers go in private subnets. Only load balancers and NAT gateways go in public subnets."},{"page":"index.html","pageLabel":"Week 1","id":"d4-4","title":"Route Tables","body":"Every subnet is associated with a route table that determines where traffic goes. Public subnet route table Destination Target 10.0.0.0/16 local ← traffic within the VPC stays in the VPC 0.0.0.0/0 igw-xxxxxxxx ← everything else goes to the Internet Gateway Private subnet route table Destination Target 10.0.0.0/16 local ← traffic within the VPC stays in the VPC 0.0.0.0/0 nat-xxxxxxxx ← everything else goes through NAT Gateway The difference: public subnets route to the Internet Gateway (direct, bidirectional). Private subnets route through a NAT Gateway (outbound only — the internet can't initiate connections in)."},{"page":"index.html","pageLabel":"Week 1","id":"d4-5","title":"Key Network Components","body":"Internet Gateway (IGW) Attached to the VPC. Provides a path between the VPC and the internet — instances in public subnets with a public IP communicate through it. One IGW per VPC; horizontally scaled, redundant, and managed by AWS — not a bottleneck. NAT Gateway Sits in a public subnet. Lets private-subnet instances initiate outbound connections (downloading updates, calling external APIs) without being reachable from the internet. Cost$0.045/hour (~$32/month) per NAT Gateway, plus data processing charges. For production, put one in each AZ for fault tolerance. For dev/learning, one is enough — just know it's a single point of failure. Cost-saving tipConsider a NAT Instance (a small EC2 instance configured to do NAT) instead — ~$3/month for a t3.nano vs $32/month. Less reliable, but fine for learning. Bastion Host (Jump Box) An EC2 instance in a public subnet that you SSH into, then SSH from there into private instances — the only instance with a public IP and SSH access, a single auditable entry point into your private network. Modern alternative: AWS Systems Manager Session Manager — no bastion, no SSH keys to manage, all sessions logged in CloudTrail."},{"page":"index.html","pageLabel":"Week 1","id":"d4-6","title":"Security Groups vs NACLs","body":"Two layers of firewall, working at different levels. AspectSecurity GroupNACL LevelInstance (attached to ENI)Subnet (all traffic entering/leaving) StateStateful — allowed inbound response is auto-allowed outboundStateless — must explicitly allow both directions RulesAllow only (implicit deny for everything else)Allow and Deny, evaluated in rule-number order DefaultDeny all inbound, allow all outboundAllow all inbound and outbound EvaluationAll rules together (most permissive wins)In order (first match wins) Practical security group design ALB (public)Inbound: TCP 443 from 0.0.0.0/0 (HTTPS from anywhere) Inbound: TCP 80 from 0.0.0.0/0 (HTTP, redirects to HTTPS) Outbound: All traffic to app-sg (forward to app servers) App serversInbound: TCP 8080 from alb-sg (only from the load balancer) Outbound: TCP 5432 to db-sg (connect to database) Outbound: TCP 443 to 0.0.0.0/0 (call external APIs via NAT) DatabaseInbound: TCP 5432 from app-sg (only from app servers) Outbound: None needed (database doesn't initiate connections) Notice the chain: internet → ALB (443) → app (only from ALB) → database (only from app). Each layer only talks to its neighbor — the database cannot be reached from the internet, there's no path."},{"page":"index.html","pageLabel":"Week 1","id":"d4-7","title":"VPC Flow Logs","body":"Captures metadata about IP traffic flowing through your VPC — source/destination IP, port, protocol, action (ACCEPT/REJECT), bytes transferred. Doesn't capture packet contents — it's metadata, not a packet capture. Sent to CloudWatch Logs or S3. Essential for: Debugging connectivity issues (\"why can't my app reach the database?\") Security auditing (\"who tried to access this subnet?\") Compliance (\"prove that no unauthorized traffic reached the database subnet\")"},{"page":"index.html","pageLabel":"Week 1","id":"d4-8","title":"Building the VPC by Hand — Step by Step","body":"The practical exercise for Day 4. Do it in the console first to understand each component, then repeat via CLI to prove you can script it. Create the VPC with CIDR 10.0.0.0/16 Create 6 subnets (public × 2 AZs, private-app × 2 AZs, private-db × 2 AZs) Create and attach an Internet Gateway Create a NAT Gateway in one public subnet (allocate an Elastic IP first) Create route tables — public → 0.0.0.0/0 to IGW, associate with public subnets; private → 0.0.0.0/0 to NAT Gateway, associate with private subnets Create security groups (ALB-sg, app-sg, db-sg) with the rules described above Enable VPC Flow Logs to CloudWatch Verify: launch a test instance in the public subnet, confirm internet access. Launch one in the private subnet, confirm it can reach the internet via NAT but cannot be reached from outside."},{"page":"index.html","pageLabel":"Week 1","id":"d4-9","title":"Why Each Component Exists — and What Breaks Without It","body":"Now that you've built every piece, here's the full justification for each one — what it does, why it exists, and what fails if you remove it. How the pieces fit together Internet Internet Gateway VPC 10.0.0.0/16 — fahad-devops-vpc AZ-a — ap-south-1a AZ-b — ap-south-1b Public 10.0.1.0/24 ALB (alb-sg) NAT Gateway Public 10.0.2.0/24 ALB (alb-sg) Private app 10.0.10.0/24 EC2 (app-sg) Private app 10.0.11.0/24 EC2 (app-sg) Private DB 10.0.20.0/24 RDS (db-sg) Private DB 10.0.21.0/24 RDS (db-sg) Public subnet — internet-facing (ALB, NAT) Private app subnet — reachable only from the ALB Private DB subnet — reachable only from the app tier Only one NAT Gateway (in AZ-a) — a deliberate cost trade-off for this learning build. Production would place one NAT per AZ for redundancy. VPC (fahad-devops-vpc — 10.0.0.0/16) What it does Your private isolated network inside AWS. Nothing can enter or leave unless you explicitly allow it. Why it exists Without a VPC, all your resources sit on a shared flat network. The VPC is the wall around your house — it defines what's inside your network and what's outside. Without it You'd have to use the default VPC, which puts everything in public subnets with public IPs. Fine for testing; a security risk for production. Public subnets (10.0.1.0/24, 10.0.2.0/24) What they do Subnets where the route table points 0.0.0.0/0 to the Internet Gateway. Resources here can have public IPs and be reached from the internet. Why they exist The ALB needs to receive traffic from users on the internet. The NAT Gateway needs internet access to relay traffic for private instances. These are the only things that should be internet-facing. Without them No way for users to reach your application — your ALB would have no internet connectivity. Why two? One per AZ (ap-south-1a and ap-south-1b). If AZ-a goes down, the ALB in AZ-b keeps serving. This is high availability. Application Load Balancer (fahad-alb) What it does Sits in both public subnets, terminates TLS, and distributes incoming requests across healthy targets in the app subnets. Why it exists Users need one stable DNS name to hit, not a shifting list of individual EC2 IPs. The ALB also health-checks targets — if the App instance in AZ-a stops responding, the ALB simply stops sending it traffic, no manual intervention needed. Without it You'd expose EC2 instances directly to the internet (defeating the whole private-subnet design), or hand out individual instance IPs that break every time Auto Scaling replaces one. Why registered in both public subnets? An ALB is inherently multi-AZ — AWS runs its nodes in every AZ you attach it to. Registering both public subnets gives it the same redundancy as the rest of the architecture, for free. Private app subnets (10.0.10.0/24, 10.0.11.0/24) What they do Subnets with no direct internet route. Traffic goes outbound only through the NAT Gateway. Why they exist Your application servers don't need to be internet-facing. They receive traffic only from the ALB, and reach the internet only through NAT. Even if your app has a vulnerability, an attacker can't directly connect from the internet. Without them You'd put app servers in public subnets with public IPs. Any misconfigured security group would expose them directly to the internet. EC2 instances (Auto Scaling group, app-sg) What they do Run the actual application code, one per AZ at minimum, launched through an Auto Scaling Group rather than as standalone instances. Why Auto Scaling instead of a fixed instance? If an instance crashes or an AZ has issues, the ASG launches a replacement automatically in a healthy AZ — you don't get paged to manually relaunch a server at 2am. Why they can only be reached through the ALB app-sg only allows inbound traffic from alb-sg. Even if you know an instance's private IP, you can't connect to it directly — traffic has to come through the load balancer, which is the only thing the security group trusts. Private DB subnets (10.0.20.0/24, 10.0.21.0/24) What they do The most isolated subnets. No internet route at all (or outbound-only via NAT for updates). Why they exist Your database is the crown jewel — it holds all your customer data. It should only be reachable from the app servers, nothing else. Not from the internet, not from your laptop, not from any other service. Why separate from app subnets? So you can apply different security groups per tier. DB-sg only allows port 5432 from App-sg. Separate subnets also let you apply different NACL rules per tier. RDS (db-sg) What it does A managed relational database in the private DB subnets, holding the application's persistent data. Why it exists db-sg allows inbound traffic only from app-sg on port 5432. The database has no route to the internet in either direction — nothing outside the app tier can reach it, and it can't reach out. Without it You'd run your own database engine on EC2, taking on patching, backups, and failover yourself — the trade-off from the shared responsibility model in 3.2. Why two DB subnets, one per AZ? So RDS Multi-AZ has somewhere to place a synchronous standby in AZ-b. If the primary in AZ-a fails, AWS promotes the standby automatically — the active-passive failover pattern from 2.6, applied at the database layer. Internet Gateway (fahad-igw) What it does The door between your VPC and the public internet. Bidirectional — traffic flows both in and out. Why it exists Without it, nothing in your VPC can reach the internet, and no one on the internet can reach anything in your VPC. Your ALB wouldn't work, your users couldn't connect. Without it A completely isolated VPC. Useful for extremely sensitive air-gapped workloads, but not for a web application. Key detail One IGW per VPC. Fully managed by AWS — horizontally scaled, redundant, never a bottleneck. NAT Gateway (fahad-nat) What it does Sits in a public subnet, relays outbound traffic for private subnets. One-way — the internet can't initiate connections through it. Why it exists Your app servers in private subnets need to reach the internet: downloading OS updates, calling external APIs (Stripe, SendGrid), pulling Docker images. NAT lets them do this without being directly reachable. Without it Your private instances would be completely isolated — they couldn't install packages, call external services, or push metrics to external monitoring. Why in a public subnet? The NAT needs internet access to relay traffic. It gets this through the IGW via the public route table. Putting it in a private subnet would create a chicken-and-egg problem — NAT needs internet to provide internet. Cost warningNAT Gateway is ~$32/month — the most expensive component in your VPC. Delete it when not studying. Also delete the Elastic IP after, or it charges too. Route tables (fahad-public-rt, fahad-private-rt) What they do Rules that tell traffic where to go. Every subnet is associated with one route table: \"if the destination matches this CIDR, send it there.\" Why they exist Without route tables, traffic inside the VPC wouldn't know how to reach the internet or other subnets. Route tables are the GPS of your network. Route tableRuleMeaning fahad-public-rt10.0.0.0/16 → localTraffic within the VPC stays internal 0.0.0.0/0 → IGWEverything else goes to the internet fahad-private-rt10.0.0.0/16 → localTraffic within the VPC stays internal 0.0.0.0/0 → NATEverything else goes outbound through NAT Without themEvery subnet would use the VPC's main route table (which has only the local route). No internet access for anything. Security groups (fahad-alb-sg, fahad-app-sg, fahad-db-sg) What they do Stateful firewalls attached to individual resources. They define who can talk to whom on which port. The chain Internet → ALB-sg (port 443) → App-sg (port 8080, only from ALB-sg) → DB-sg (port 5432, only from App-sg) Without them Everything in the VPC could talk to everything else on any port. A compromised ALB could connect directly to the database. Why reference security groups (not IPs)? If you wrote \"allow from 10.0.1.0/24,\" you'd have to update it every time the ALB's IP changes. By referencing ALB-sg as the source, it automatically applies to anything that has ALB-sg attached — regardless of IP. VPC Flow Logs What they do Capture metadata about every network connection — source IP, destination IP, port, protocol, accept/reject, bytes. Sent to CloudWatch Logs. Why they exist When something doesn't work (\"my app can't reach the database\"), flow logs tell you whether the traffic was attempted and rejected (security group issue) or never attempted (routing issue). Without them Debugging network issues blind. \"Is traffic being blocked or not reaching?\" becomes a guessing game. What about NACLs? Deliberately skippedThis build kept every subnet's NACL at its AWS default (allow all in, allow all out) and did all the enforcement with security groups instead. Security groups already implement the exact alb → app → db chain from 4.6; stacking a second, stateless firewall on top adds real operational cost (remembering to open both directions for every rule) without adding protection this architecture doesn't already have. Custom NACLs earn their place as a second layer at the public subnet boundary in stricter, compliance-driven environments — worth revisiting later, not required for this build. How a user request flows through all these components 1. User types your-app.com 2. Route 53 resolves to ALB's IP 3. Request hits ALB in public subnet (ALB-sg allows port 443) 4. ALB forwards to EC2 in private app subnet (App-sg allows from ALB-sg) 5. EC2 queries RDS in private DB subnet (DB-sg allows from App-sg) 6. Response flows back: RDS → EC2 → ALB → user If EC2 needs to call Stripe API: 7. EC2 → NAT Gateway → IGW → internet (outbound only, no inbound) The testFor each component, ask: \"what breaks if I remove this?\" If you can answer that for every piece, you understand the VPC. If you can't, re-read that component's section."},{"page":"index.html","pageLabel":"Week 1","id":"log-1","title":"AWS CLI Profile & Identity Verification","body":"IAM CLI Configured a named profile per 3.6 AWS CLI with Named Profiles, then confirmed it with get-caller-identity — the \"whoami\" of AWS. Account ID, ARN, and User ID all came back as expected for the profile."},{"page":"index.html","pageLabel":"Week 1","id":"log-2","title":"Budget Alarm Setup","body":"Billing Set alerts at 50% and 80% of the monthly budget per 3.5 Budget Alarm Setup, before touching anything that costs money."},{"page":"index.html","pageLabel":"Week 1","id":"log-3","title":"First EC2 Instance via CLI","body":"ap-south-1 t3.micro Terminated Launched an instance, tagged using the convention from 3.4 Tagging Strategy — tried both the console wizard and the CLI, to compare the two workflows. Observations IMDSv2: Required — the instance came up with the secure metadata option already enforced (see 3.7, Instance Profiles and IMDSv2). No AllowV1 fallback to worry about. State: Terminated — this instance was launched and torn down as a CLI smoke test, not the one that will sit in the VPC being built in 4.8. VPC ID and Subnet ID show blank because AWS drops that networking detail from the console once an instance is terminated, not because it skipped a VPC — every EC2 instance launches into some VPC (the account's default VPC here, since none was specified with --subnet-id). Tags match the 3.4 convention exactly (Environment, Project, Owner, CostCenter, ManagedBy) — good for cost tracking and the \"who left this running?\" question, even on a throwaway instance. NextRe-launch into the hand-built VPC's public/private subnets per 4.8 once the subnets, route tables and security groups exist, so the instance actually lands somewhere purposeful instead of the default VPC."},{"page":"index.html","pageLabel":"Week 1","id":"log-4","title":"Creating the VPC","body":"10.0.0.0/16 fahad-devops-vpc Built by hand per 4.8 — \"VPC only\" (no default subnets/route tables auto-created), named fahad-devops-vpc, CIDR set to the plan from 4.2. Resource map showed 0 subnets and 1 (main) route table before the next steps. DNS resolution and DNS hostnames were both left enabled — required for instances to get usable DNS names and to resolve AWS service endpoints from inside the VPC."},{"page":"index.html","pageLabel":"Week 1","id":"log-5","title":"Creating the 6 Subnets","body":"2 AZs 3 tiers All six subnets from the 4.2 CIDR plan, created in one batch and confirmed Available. Observations CIDRs match the plan exactly: 10.0.1.0/24 / 10.0.2.0/24 (public), 10.0.10.0/24 / 10.0.11.0/24 (app), 10.0.20.0/24 / 10.0.21.0/24 (DB) — see 4.9 for why each tier exists. All six still show the VPC's default (main) route table at this point — the public/private route tables in 4.4 haven't been created and associated yet."},{"page":"index.html","pageLabel":"Week 1","id":"log-6","title":"Internet Gateway","body":"fahad-igw Created per 4.8. Not done yetThe IGW is created but not attached — its VPC column is empty and state reads Detached. The other IGW in this list (Attached) belongs to a different, unrelated VPC. Next step: Actions → Attach to VPC, selecting fahad-devops-vpc — without that, the public route table in 4.4 has nothing to point 0.0.0.0/0 at."},{"page":"index.html","pageLabel":"Week 1","id":"log-7","title":"NAT Gateway","body":"fahad-nat fahad-public-a Created in fahad-public-a only, matching the single-NAT cost trade-off from 4.9 and 4.5's cost note. NoteState shows Pending right after creation — NAT Gateways take a few minutes to become Available. It also has no primary public IP yet at this point, because the Elastic IP allocation step (4.8, step 4) hadn't finished attaching."},{"page":"index.html","pageLabel":"Week 1","id":"d1","title":"Principles & SDLC","body":""},{"page":"index.html","pageLabel":"Week 1","id":"d2","title":"Deployment & Multi-Region","body":""},{"page":"index.html","pageLabel":"Week 1","id":"d3","title":"AWS & IAM","body":""},{"page":"index.html","pageLabel":"Week 1","id":"d4","title":"VPC, Built by Hand","body":""},{"page":"index.html","pageLabel":"Week 1","id":"lab-log","title":"Lab Log","body":""},{"page":"index.html","pageLabel":"Week 1","id":"checklist","title":"A1 Checklist","body":"Use this checklist to make sure your document covers every required element. Checks are saved in this browser only. Branching model: trunk-based development described, short-lived branches, feature flags for incomplete work Rejected alternative: Gitflow described with specific reasons it was rejected (merge conflicts, slow feedback, incompatible with daily deploys) Release cadence: daily deployments, automated from main Deployment-strategy decision matrix: table mapping each service class (stateless API, stateful service, database migration, frontend) to a strategy with justification Environment and promotion diagram: dev → staging → prod with gates between each, what differs and what must be the same Multi-region topology: active-passive or active-active, with the choice justified RTO and RPO: specific numbers with business justification DORA baseline and targets: 4 metrics with current baseline and 3-month targets DORA measurement method: how each metric will be tracked Three IAM roles: application, operator, auditor — each with full policy JSON and least-privilege justification VPC architecture diagram: CIDR plan, subnet layout, route tables, security group chains All choices can be defended: for every decision, name the rejected alternative and explain the trade-off"},{"page":"week2.html","pageLabel":"Week 2","id":"d5-1","title":"Launching an EC2 Instance","body":"An EC2 instance is a virtual server. You choose four things when you launch one: AMI (Amazon Machine Image) The template — which OS, which pre-installed software. Amazon Linux 2023, Ubuntu 24.04, or your own custom image. Instance type The hardware — how much CPU and RAM. t3.micro (2 vCPU, 1 GB) is free tier. m5.xlarge (4 vCPU, 16 GB) is production-grade. Key pair SSH key for logging in. Create once, reuse. Download the .pem file and keep it safe — you can't download it again. Network settings Which VPC, which subnet, which security group. This determines who can reach the instance and what it can reach. Step 1: Find the latest AMI AMIs get deprecated regularly. Always search for the current one: # Latest Amazon Linux 2023 aws ec2 describe-images \\ --owners amazon \\ --filters \"Name=name,Values=al2023-ami-2023*-x86_64\" \\ \"Name=state,Values=available\" \\ --query \"Images | sort_by(@, &CreationDate) | [-1].{ID:ImageId,Name:Name}\" \\ --output table # Latest Ubuntu 24.04 aws ec2 describe-images \\ --owners 099720109477 \\ --filters \"Name=name,Values=ubuntu/images/hvm-ssd-gp3/ubuntu-noble-24.04-amd64-server-*\" \\ \"Name=state,Values=available\" \\ --query \"Images | sort_by(@, &CreationDate) | [-1].{ID:ImageId,Name:Name}\" \\ --output table Step 2: Check which instance types are free tier aws ec2 describe-instance-types \\ --filters \"Name=free-tier-eligible,Values=true\" \\ --query \"InstanceTypes[].InstanceType\" \\ --output table Gotchat2.micro is no longer free tier on newer accounts — it's been replaced by t3.micro. Always check with the command above before launching. Step 3: Find your VPC subnets and security groups # List subnets in your hand-built VPC aws ec2 describe-subnets \\ --filters \"Name=vpc-id,Values=vpc-0123456789abcdef0\" \\ --query \"Subnets[].{ID:SubnetId,Name:Tags[?Key=='Name']|[0].Value,CIDR:CidrBlock}\" \\ --output table # List security groups in your hand-built VPC aws ec2 describe-security-groups \\ --filters \"Name=vpc-id,Values=vpc-0123456789abcdef0\" \\ --query \"SecurityGroups[].{ID:GroupId,Name:GroupName}\" \\ --output table Common mistakeUsing the default VPC subnet (172.31.x.x) instead of your hand-built VPC subnet (10.0.x.x). Always verify the subnet ID belongs to vpc-0123456789abcdef0, not vpc-0fedcba9876543210 (the default). Step 4: Create security groups if missing # ALB security group aws ec2 create-security-group \\ --group-name fahad-alb-sg \\ --description \"ALB - allows HTTPS/HTTP from internet\" \\ --vpc-id vpc-0123456789abcdef0 \\ --query \"GroupId\" --output text # App security group aws ec2 create-security-group \\ --group-name fahad-app-sg \\ --description \"App servers - allows traffic from ALB only\" \\ --vpc-id vpc-0123456789abcdef0 \\ --query \"GroupId\" --output text # DB security group aws ec2 create-security-group \\ --group-name fahad-db-sg \\ --description \"Database - allows traffic from App only\" \\ --vpc-id vpc-0123456789abcdef0 \\ --query \"GroupId\" --output text Step 5: Add inbound rules (the chain) # ALB-sg: allow HTTPS and HTTP from internet aws ec2 authorize-security-group-ingress \\ --group-id <ALB_SG_ID> \\ --protocol tcp --port 443 --cidr 0.0.0.0/0 aws ec2 authorize-security-group-ingress \\ --group-id <ALB_SG_ID> \\ --protocol tcp --port 80 --cidr 0.0.0.0/0 # App-sg: allow 8080 from ALB-sg ONLY aws ec2 authorize-security-group-ingress \\ --group-id <APP_SG_ID> \\ --protocol tcp --port 8080 \\ --source-group <ALB_SG_ID> # DB-sg: allow 5432 from App-sg ONLY aws ec2 authorize-security-group-ingress \\ --group-id <DB_SG_ID> \\ --protocol tcp --port 5432 \\ --source-group <APP_SG_ID> Step 6: Launch the instance aws ec2 run-instances \\ --image-id <AMI_ID_FROM_STEP_1> \\ --instance-type t3.micro \\ --key-name devopsKeypair \\ --subnet-id <fahad-private-app-a SUBNET_ID> \\ --security-group-ids <fahad-app-sg ID> \\ --count 1 \\ --tag-specifications 'ResourceType=instance,Tags=[ {Key=Name,Value=fahad-web-dev}, {Key=Environment,Value=dev}, {Key=Owner,Value=fahad}, {Key=Project,Value=ecommerce-platform}, {Key=ManagedBy,Value=manual} ]' Step 7: Verify it landed in the right VPC # Check — IP should be 10.0.10.x, NOT 172.31.x.x aws ec2 describe-instances \\ --filters \"Name=tag:Name,Values=fahad-web-dev\" \\ \"Name=instance-state-name,Values=running\" \\ --query \"Reservations[].Instances[].{ID:InstanceId,IP:PrivateIpAddress,Subnet:SubnetId,VPC:VpcId,State:State.Name}\" \\ --output table Manage instances # List all running instances (clean table view) aws ec2 describe-instances \\ --filters \"Name=instance-state-name,Values=running\" \\ --query \"Reservations[].Instances[].{ID:InstanceId,State:State.Name,Type:InstanceType,IP:PrivateIpAddress,Name:Tags[?Key=='Name']|[0].Value}\" \\ --output table # Stop (keeps data, stops billing compute) aws ec2 stop-instances --instance-ids i-xxxxx # Start again aws ec2 start-instances --instance-ids i-xxxxx # Terminate (permanent delete) aws ec2 terminate-instances --instance-ids i-xxxxx # Terminate multiple at once aws ec2 terminate-instances --instance-ids i-aaa111 i-bbb222 i-ccc333 Free tier750 hours/month of t3.micro for 12 months. One instance 24/7 fits exactly. Two running simultaneously = the second one costs ~$7.50/month. Always check aws ec2 describe-instances to know what's running."},{"page":"week2.html","pageLabel":"Week 2","id":"d5-2","title":"User-Data Bootstrap Script","body":"A shell script that runs automatically on first boot, as root. Used to install packages, configure services, and prepare the instance for its role. #!/bin/bash yum update -y yum install -y httpd systemctl start httpd systemctl enable httpd echo \"<h1>Hello from $(hostname)</h1>\" > /var/www/html/index.html Pass it when launching: aws ec2 run-instances \\ --user-data file://bootstrap.sh \\ ... Failure modeUser-data runs once on first boot only. If it fails silently, the instance looks \"running\" but isn't configured. Check /var/log/cloud-init-output.log to debug. Best practiceKeep user-data short — install an agent, pull config from S3 or Parameter Store. Heavy installs belong in a Golden AMI (next section) for faster boot times."},{"page":"week2.html","pageLabel":"Week 2","id":"d5-3","title":"Golden AMI Pipeline","body":"Instead of installing everything via user-data on every boot, you bake a Golden AMI — a pre-configured image with all dependencies already installed. Instances boot in ~30 seconds instead of ~4 minutes. Step 1: Launch a base instance in a public subnet You need to SSH into this instance to install packages, so it must be in a public subnet with internet access and a security group that allows SSH. # Launch in fahad-public-a with ALB security group aws ec2 run-instances \\ --image-id ami-0aaaaaaaaaaaaaaaa \\ --instance-type t3.micro \\ --key-name devopsKeypair \\ --subnet-id subnet-0aaa1111aaaa11111 \\ --security-group-ids sg-0aaa00001111bbbb \\ --count 1 \\ --tag-specifications 'ResourceType=instance,Tags=[ {Key=Name,Value=fahad-golden-base}, {Key=Owner,Value=fahad} ]' \\ --profile fahad Don't use an existing project instanceNever bake a Golden AMI from an instance running a project (like nsl-ledgerly). It has project-specific code, credentials, logs, and SSH history that would leak into every new instance. Always start from a fresh base AMI. Step 2: Wait for the instance to be running, get its public IP # Wait ~30 seconds, then check (pending → running takes time) aws ec2 describe-instances \\ --instance-ids <INSTANCE_ID> \\ --query \"Reservations[0].Instances[0].{PublicIP:PublicIpAddress,State:State.Name}\" \\ --output table \\ --profile fahad Why wait 30 seconds?When you launch, the state is pending — AWS is finding a server, attaching the EBS volume, starting the VM, and assigning a public IP. Until the state becomes running, there's no public IP. That's why PublicDnsName is empty in the launch output. Step 3: Ensure SSH access (port 22) Check if port 22 is open in your security group: # Check existing rules aws ec2 describe-security-groups \\ --group-ids sg-0aaa00001111bbbb \\ --query \"SecurityGroups[0].IpPermissions[].{Port:FromPort}\" \\ --output table \\ --profile fahad # If port 22 is NOT listed, add it temporarily aws ec2 authorize-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad Why port 22?Port 22 = SSH, the protocol for remote login to Linux servers. Security groups block everything by default. Without a port 22 rule, your SSH connection is silently dropped — no error, just a timeout. The ALB security group only has 80 (HTTP) and 443 (HTTPS), which is correct for web traffic but doesn't include SSH. Troubleshooting: SSH hangs at \"Connecting\" If ssh -v shows debug1: Connecting to x.x.x.x port 22. and then hangs, check these three things in order: Problem 1: Route table not associated with the public subnet This is the most common issue. You created fahad-public-rt with the IGW route, but forgot to associate it with the public subnets. Without association, the subnet uses the VPC's main route table (which has no internet route). # Check: does the public subnet have a route table? aws ec2 describe-route-tables \\ --filters \"Name=association.subnet-id,Values=subnet-0aaa1111aaaa11111\" \\ --query \"RouteTables[].{ID:RouteTableId,Routes:Routes[].{Dest:DestinationCidrBlock,Target:GatewayId}}\" \\ --output table \\ --profile fahad # If empty → no route table associated. Fix: aws ec2 associate-route-table \\ --route-table-id rtb-0aaa99998888bbbb \\ --subnet-id subnet-0aaa1111aaaa11111 \\ --profile fahad # Do the same for fahad-public-b aws ec2 associate-route-table \\ --route-table-id rtb-0aaa99998888bbbb \\ --subnet-id subnet-0bbb2222bbbb22222 \\ --profile fahad Problem 2: Internet Gateway not attached to VPC # Check: is the IGW attached? aws ec2 describe-internet-gateways \\ --filters \"Name=attachment.vpc-id,Values=vpc-0123456789abcdef0\" \\ --query \"InternetGateways[].{ID:InternetGatewayId,State:Attachments[0].State}\" \\ --output table \\ --profile fahad # If State is \"detached\" or empty: aws ec2 attach-internet-gateway \\ --internet-gateway-id igw-0aaa11112222bbbb \\ --vpc-id vpc-0123456789abcdef0 \\ --profile fahad Problem 3: SSH key file not found # Error: \"Identity file /home/user/.ssh/mymac.pem not accessible: No such file\" # Find the key file on your machine find ~ -name \"*.pem\" -type f 2>/dev/null # If the key is lost (can't re-download from AWS), create a new one: aws ec2 create-key-pair \\ --key-name devopsKeypair \\ --query \"KeyMaterial\" \\ --output text \\ --profile fahad > ~/.ssh/devopsKeypair.pem chmod 400 ~/.ssh/devopsKeypair.pem # Then terminate the instance and relaunch with the new key # (you can't change the key of a running instance) Problem 4: Permission denied on .pem file # Error: \"Permissions 0644 for 'key.pem' are too open\" chmod 400 ~/.ssh/devopsKeypair.pem Step 4: SSH in and install packages ssh -i ~/.ssh/devopsKeypair.pem ec2-user@<PUBLIC_IP> # Inside the instance — run these: # Update OS sudo yum update -y # Install Python 3.12 and pip sudo yum install -y python3.12 python3.12-pip # Install Nginx sudo yum install -y nginx sudo systemctl enable nginx # Install app dependencies sudo pip3.12 install gunicorn django==5.1 # Install CloudWatch agent sudo yum install -y amazon-cloudwatch-agent # Harden SSH: disable root login sudo sed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config # Clean up before snapshot (remove temp files, logs, SSH history) sudo rm -rf /tmp/* /var/log/*.log history -c # Exit back to your local machine exit Step 5: Bake the Golden AMI aws ec2 create-image \\ --instance-id <INSTANCE_ID> \\ --name \"golden-ecommerce-v1.0-$(date +%Y%m%d)\" \\ --description \"AL2023 + Python 3.12 + Nginx + Django 5.1 + CW agent\" \\ --tag-specifications 'ResourceType=image,Tags=[ {Key=Name,Value=golden-ecommerce-v1.0}, {Key=Owner,Value=fahad}, {Key=Project,Value=ecommerce-platform}, {Key=BaseAMI,Value=ami-0aaaaaaaaaaaaaaaa} ]' \\ --profile fahad # Check if it's ready (takes 3-5 minutes: pending → available) aws ec2 describe-images \\ --owners self \\ --query \"Images[].{ID:ImageId,Name:Name,State:State}\" \\ --output table \\ --profile fahad BaseAMI tagTag the Golden AMI with the base AMI ID it was built from. When Amazon releases a security patch, you know which base to re-bake from. Step 6: Clean up # Terminate the base instance (no longer needed) aws ec2 terminate-instances \\ --instance-ids <INSTANCE_ID> \\ --profile fahad # Remove SSH access from ALB security group (security hygiene) aws ec2 revoke-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad Don't forgetAlways remove the port 22 rule after you're done. SSH from 0.0.0.0/0 means anyone on the internet can attempt to connect. In production, use AWS Systems Manager Session Manager instead — no SSH port needed at all. Step 7: Use the Golden AMI going forward # Our Golden AMI: ami-0bbbbbbbbbbbbbbbb # Use it instead of the base Amazon AMI in all future launches: aws ec2 run-instances \\ --image-id ami-0bbbbbbbbbbbbbbbb \\ --instance-type t3.micro \\ --key-name devopsKeypair \\ --subnet-id subnet-0ccc3333cccc33333 \\ --security-group-ids sg-0bbb00002222cccc \\ --profile fahad # Boot time: ~30 seconds (vs ~4 minutes with user-data bootstrap) Managing Golden AMIs # List all your custom AMIs aws ec2 describe-images \\ --owners self \\ --query \"Images[].{ID:ImageId,Name:Name,State:State,Date:CreationDate}\" \\ --output table \\ --profile fahad # Deregister an old AMI you no longer need aws ec2 deregister-image \\ --image-id ami-xxxxx \\ --profile fahad When to re-bake New OS security patch — Amazon releases an updated base AMI, you rebuild on top of it New system dependency — adding Redis client, upgrading Python version Configuration change — new monitoring agent config, updated SSH hardening NOT for app code changes — your app code is pulled fresh at boot (from S3 or a container registry), so you don't re-bake just for a code deploy Re-baking uses the same steps as the first bake — the difference is what you start from: First bake: Amazon Linux AMI → install everything → Golden v1.0 Re-bake: Golden v1.0 → apply changes → Golden v1.1 Security re-bake: NEW Amazon Linux AMI → install everything fresh → Golden v2.0 GotchaStep 6 of the first bake revoked port 22 from sg-0aaa00001111bbbb as security hygiene — which means it's closed by default every time you come back to re-bake. Both scenarios below re-open it before SSHing and revoke it again during clean-up. Skip that and SSH just hangs at \"Connecting\", same symptom as the troubleshooting section above, different cause. Scenario 1 — small update (add a package, change a config) Start from your existing Golden AMI — everything is already installed, you just add the change: # 1. Launch from your EXISTING Golden AMI (not the raw Amazon base) aws ec2 run-instances \\ --image-id ami-0bbbbbbbbbbbbbbbb \\ --instance-type t3.micro \\ --key-name devopsKeypair \\ --subnet-id subnet-0aaa1111aaaa11111 \\ --security-group-ids sg-0aaa00001111bbbb \\ --tag-specifications 'ResourceType=instance,Tags=[ {Key=Name,Value=fahad-golden-rebake} ]' \\ --profile fahad # 2. Re-open port 22 — Step 6 of the first bake revoked it, so it's # almost certainly closed again. Skipping this = SSH hangs at # \"Connecting\" with no error, same symptom as the troubleshooting # section above, just a different cause. aws ec2 authorize-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad # 3. SSH in — everything from v1.0 is already there ssh -i ~/.ssh/devopsKeypair.pem ec2-user@<PUBLIC_IP> # 4. Apply only the change sudo pip3.12 install redis # example: adding Redis client sudo systemctl enable redis # example: enabling a new service # 5. Clean up and exit sudo rm -rf /tmp/* /var/log/*.log history -c exit # 6. Bake as v1.1 aws ec2 create-image \\ --instance-id <INSTANCE_ID> \\ --name \"golden-ecommerce-v1.1-$(date +%Y%m%d)\" \\ --description \"v1.0 + Redis client\" \\ --tag-specifications 'ResourceType=image,Tags=[ {Key=Name,Value=golden-ecommerce-v1.1}, {Key=Owner,Value=fahad}, {Key=BaseAMI,Value=ami-0bbbbbbbbbbbbbbbb}, {Key=ChangeLog,Value=added-redis-client} ]' \\ --profile fahad # 7. Clean up — terminate the instance AND re-revoke port 22 aws ec2 terminate-instances --instance-ids <INSTANCE_ID> --profile fahad aws ec2 revoke-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad Scenario 2 — security re-bake (new Amazon base AMI) Amazon released a patched base AMI — you need to rebuild from scratch on the new base to get the OS-level security fixes: # 1. Find the latest Amazon Linux AMI aws ec2 describe-images \\ --owners amazon \\ --filters \"Name=name,Values=al2023-ami-2023*-x86_64\" \\ \"Name=state,Values=available\" \\ --query \"Images | sort_by(@, &CreationDate) | [-1].{ID:ImageId,Name:Name}\" \\ --output table --profile fahad # 2. Launch from the NEW base (not your old Golden AMI) aws ec2 run-instances \\ --image-id <NEW_AMAZON_AMI> \\ --instance-type t3.micro \\ --key-name devopsKeypair \\ --subnet-id subnet-0aaa1111aaaa11111 \\ --security-group-ids sg-0aaa00001111bbbb \\ --tag-specifications 'ResourceType=instance,Tags=[ {Key=Name,Value=fahad-golden-rebake-v2} ]' \\ --profile fahad # 3. Re-open port 22 — same reason as Scenario 1: Step 6 of the first # bake revoked it, so it's almost certainly closed right now. aws ec2 authorize-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad # 4. SSH in and install EVERYTHING from scratch (same as first bake) ssh -i ~/.ssh/devopsKeypair.pem ec2-user@<PUBLIC_IP> sudo yum update -y sudo yum install -y python3.12 python3.12-pip nginx amazon-cloudwatch-agent sudo pip3.12 install gunicorn django==5.1 sudo systemctl enable nginx sudo sed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config # ... plus any packages added since v1.0 (like redis from v1.1) sudo pip3.12 install redis sudo rm -rf /tmp/* /var/log/*.log history -c exit # 5. Bake as v2.0 (major version = new base) aws ec2 create-image \\ --instance-id <INSTANCE_ID> \\ --name \"golden-ecommerce-v2.0-$(date +%Y%m%d)\" \\ --description \"NEW base AL2023 + Python 3.12 + Nginx + Django 5.1 + Redis + CW agent\" \\ --tag-specifications 'ResourceType=image,Tags=[ {Key=Name,Value=golden-ecommerce-v2.0}, {Key=Owner,Value=fahad}, {Key=BaseAMI,Value=<NEW_AMAZON_AMI>}, {Key=ChangeLog,Value=security-rebake-new-base} ]' \\ --profile fahad # 6. Clean up — terminate the instance AND re-revoke port 22 aws ec2 terminate-instances --instance-ids <INSTANCE_ID> --profile fahad aws ec2 revoke-security-group-ingress \\ --group-id sg-0aaa00001111bbbb \\ --protocol tcp --port 22 --cidr 0.0.0.0/0 \\ --profile fahad Which scenario, when? TriggerApproachVersion bump Add a package (redis, memcached client)Scenario 1: build on existing Golden AMIv1.0 → v1.1 Change a config (Nginx settings, SSH hardening)Scenario 1: build on existing Golden AMIv1.1 → v1.2 Upgrade Python (3.12 → 3.13)Scenario 2: fresh from new basev1.x → v2.0 Amazon releases OS security patchScenario 2: fresh from new basev1.x → v2.0 Major dependency upgrade (Django 5.1 → 6.0)Scenario 2: safer to rebuild cleanv1.x → v2.0 After re-baking: update the launch template The Golden AMI is useless if your ASG (see 5.5) is still pointing to the old one — this uses the launch template mechanics from 5.4: # Create new launch template version with the new AMI aws ec2 create-launch-template-version \\ --launch-template-name ecommerce-web \\ --version-description \"v2.0 - security rebake\" \\ --source-version 1 \\ --launch-template-data '{\"ImageId\": \"ami-NEW_GOLDEN_AMI\"}' \\ --profile fahad # Set it as default aws ec2 modify-launch-template \\ --launch-template-name ecommerce-web \\ --default-version 2 \\ --profile fahad # ASG picks up the new version on next scale-out # To force all instances to update NOW: aws autoscaling start-instance-refresh \\ --auto-scaling-group-name fahad-web-asg \\ --profile fahad The instance-refresh command does a rolling replacement — terminates old instances one by one and launches new ones from the updated template. Zero downtime if you have multiple instances across AZs. Keep a version history # List all your Golden AMIs in order aws ec2 describe-images \\ --owners self \\ --query \"Images | sort_by(@, &CreationDate) | [].{ID:ImageId,Name:Name,Date:CreationDate,Base:Tags[?Key=='BaseAMI']|[0].Value}\" \\ --output table \\ --profile fahad This gives you a timeline of every Golden AMI you've built, when, and which base it came from. Never delete the currently active AMI — keep at least the previous version as rollback. Lab: v1.0 → v1.1 re-bake (adding Docker) This is what we actually did — a Scenario 1 re-bake to add Docker and Docker Compose to the Golden AMI. Install Docker and Docker Compose inside SSH session: # Install Docker sudo yum install -y docker sudo systemctl enable docker sudo systemctl start docker # Add ec2-user to docker group (no sudo needed for docker commands) sudo usermod -aG docker ec2-user # Install Docker Compose v2 (as a Docker CLI plugin) sudo mkdir -p /usr/local/lib/docker/cli-plugins sudo curl -SL \"https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64\" \\ -o /usr/local/lib/docker/cli-plugins/docker-compose sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose # Verify both docker --version docker compose version # If docker --version gives permission error, the group change hasn't taken effect: newgrp docker docker --version Gotcha: Redis client ≠ Redis server pip install redis installs the Python client library (lets your app talk to a Redis server). systemctl enable redis tries to start the Redis server process — which you didn't install. These are two different things with the same name. For production, use ElastiCache (AWS managed Redis) — your Golden AMI only needs the Python client, not the server. If you want a local Redis server for testing: # Only if you need Redis SERVER on the instance (not typical for production) sudo yum install -y redis6 sudo systemctl enable redis6 sudo systemctl start redis6 redis-cli ping # returns PONG Bake result: # After installing Docker, clean up and exit SSH: sudo rm -rf /tmp/* /var/log/*.log history -c exit # Bake v1.1 from local machine: aws ec2 create-image \\ --instance-id i-0eee5555ffff6666 \\ --name \"golden-ecommerce-v1.1-$(date +%Y%m%d)\" \\ --description \"v1.0 + Redis client + Docker + Docker Compose\" \\ --tag-specifications 'ResourceType=image,Tags=[ {Key=Name,Value=golden-ecommerce-v1.1}, {Key=Owner,Value=fahad}, {Key=BaseAMI,Value=ami-0bbbbbbbbbbbbbbbb}, {Key=ChangeLog,Value=added-redis-docker-compose} ]' \\ --profile fahad Current AMI inventory: VersionAMI IDContentsStatus v1.0ami-0bbbbbbbbbbbbbbbbPython 3.12, Nginx, Django 5.1, CW agentRollback (keep) v1.1ami-0cccccccccccccccv1.0 + Redis client + Docker + Docker ComposeCurrent (active) Where these AMIs actually live A Golden AMI is stored as an EBS snapshot in S3 — but you never see that S3 bucket directly. AWS manages it entirely behind the scenes; the AMI itself is just a pointer to it: Golden AMI (ami-0ccccccccccccccc) └── points to → EBS Snapshot (snap-xxxxx) └── stored in → S3 (AWS-managed, invisible to you) See both of our AMIs' snapshots at once: aws ec2 describe-images \\ --image-ids ami-0bbbbbbbbbbbbbbbb ami-0ccccccccccccccc \\ --query \"Images[].{AMI:ImageId,Name:Name,Snapshots:BlockDeviceMappings[].Ebs.SnapshotId}\" \\ --output table \\ --profile fahad WhatWhereCan you see it?Cost AMI metadata (name, tags, config)EC2 AMI registry (regional)Yes — describe-imagesFree EBS snapshot (the actual disk data)S3 (AWS-managed)Yes — describe-snapshots~$0.05/GB/month The S3 bucket itselfAWS's internal infrastructureNo — you can't browse itIncluded in snapshot price Regional, not AZ-specificAn AMI exists only in the region you built it in — ap-south-1 here, not tied to a specific AZ within it. Want to use it in eu-west-1? You have to copy it there explicitly: # Copy an AMI to another region (for multi-region deployment) aws ec2 copy-image \\ --source-image-id ami-0ccccccccccccccc \\ --source-region ap-south-1 \\ --region eu-west-1 \\ --name \"golden-ecommerce-v1.1-copy\" \\ --profile fahad The AMI registration itself is free — it's just a pointer. The snapshot behind it is what actually costs money, which is why deleting an AMI takes two steps (see below): deregistering alone leaves the snapshot billing forever. At this point you should see two snapshots — one for v1.0, one for v1.1 — roughly 8GB each, about $0.80/month total. Minimal cost, but worth knowing where it comes from. Troubleshooting: forgot the instance ID If you launched an instance but didn't save the ID, find it by searching running instances: # Find all running instances (not just yours) aws ec2 describe-instances \\ --filters \"Name=instance-state-name,Values=running\" \\ --query \"Reservations[].Instances[].{ID:InstanceId,Name:Tags[?Key=='Name']|[0].Value,IP:PrivateIpAddress,VPC:VpcId}\" \\ --output table \\ --profile fahad # If you know the IP from your SSH prompt (e.g. ec2-user@ip-10-0-1-164): # Look for IP 10.0.1.164 in the table — that row has your instance ID TipIf you filtered by tag:Owner,Values=fahad and got nothing, the instance might not have an Owner tag. Drop the tag filter and search all running instances. The SSH prompt always shows the private IP — match it to the table. When and how to delete old Golden AMIs SituationAction v1.1 just created (now)Keep v1.0 as rollback v1.1 tested and stable for a few daysSafe to delete v1.0 v2.0 created (security re-bake)Keep v1.1, delete v1.0 General ruleAlways keep current + one previous version Deleting an AMI — two steps (not one!): Deregistering the AMI alone isn't enough — the EBS snapshot behind it keeps charging ~$0.05/GB/month. # Step 1: Find the snapshot backing the AMI aws ec2 describe-images \\ --image-ids ami-0bbbbbbbbbbbbbbbb \\ --query \"Images[0].BlockDeviceMappings[].Ebs.SnapshotId\" \\ --output text \\ --profile fahad # Step 2: Deregister the AMI (removes it from your AMI list) aws ec2 deregister-image \\ --image-id ami-0bbbbbbbbbbbbbbbb \\ --profile fahad # Step 3: Delete the orphan snapshot (stops the storage charge) aws ec2 delete-snapshot \\ --snapshot-id <SNAPSHOT_ID_FROM_STEP_1> \\ --profile fahad Don't skip step 3If you only deregister without deleting the snapshot, the snapshot sits in your account silently charging. Check for orphan snapshots periodically: # Find all snapshots you own aws ec2 describe-snapshots \\ --owner-ids self \\ --query \"Snapshots[].{ID:SnapshotId,Size:VolumeSize,Date:StartTime,Desc:Description}\" \\ --output table \\ --profile fahad Immutable infraGolden AMIs are the foundation of immutable infrastructure: you never SSH into a production server to patch it. Build a new AMI with the fix, replace the old instances via the ASG. This prevents configuration drift — every instance is identical, built from the same image."},{"page":"week2.html","pageLabel":"Week 2","id":"d5-4","title":"Launch Templates","body":"A launch template is a versioned configuration blueprint: AMI, instance type, security groups, user-data, IAM role — everything needed to launch an instance, saved as a reusable template. # Create launch template using our Golden AMI v1.1 (with Docker) aws ec2 create-launch-template \\ --launch-template-name ecommerce-web \\ --version-description \"v1 - golden AMI v1.1 with Python+Nginx+Django+Docker\" \\ --launch-template-data '{ \"ImageId\": \"ami-0ccccccccccccccc\", \"InstanceType\": \"t3.micro\", \"KeyName\": \"devopsKeypair\", \"SecurityGroupIds\": [\"sg-0bbb00002222cccc\"], \"TagSpecifications\": [{ \"ResourceType\": \"instance\", \"Tags\": [ {\"Key\": \"Name\", \"Value\": \"fahad-web\"}, {\"Key\": \"Environment\", \"Value\": \"dev\"}, {\"Key\": \"Owner\", \"Value\": \"fahad\"}, {\"Key\": \"Project\", \"Value\": \"ecommerce-platform\"}, {\"Key\": \"ManagedBy\", \"Value\": \"manual\"} ] }] }' \\ --profile fahad Templates are versioned. When you update (new Golden AMI, different instance type), create a new version — the old version stays as rollback: # Create a new version (e.g. updated Golden AMI) aws ec2 create-launch-template-version \\ --launch-template-name ecommerce-web \\ --version-description \"v2 - updated golden AMI\" \\ --source-version 1 \\ --launch-template-data '{ \"ImageId\": \"ami-NEW_GOLDEN_AMI_ID\" }' \\ --profile fahad # List versions aws ec2 describe-launch-template-versions \\ --launch-template-name ecommerce-web \\ --query \"LaunchTemplateVersions[].{Version:VersionNumber,Desc:VersionDescription,AMI:LaunchTemplateData.ImageId}\" \\ --output table \\ --profile fahad # Set default version aws ec2 modify-launch-template \\ --launch-template-name ecommerce-web \\ --default-version 2 \\ --profile fahad Auto Scaling Groups reference a template version — $Latest (always newest), $Default (the one you set), or a specific number."},{"page":"week2.html","pageLabel":"Week 2","id":"d5-5","title":"Auto Scaling Groups (ASG)","body":"An ASG maintains a fleet of instances — it ensures you always have the right number running, replaces unhealthy ones, and spreads them across AZs. Three capacity settings SettingWhat it meansExample MinimumNever go below this, even during scale-in1 (always at least 1 running) DesiredHow many to run right now — ASG launches/terminates to match1 (free tier safe with nsl-ledgerly running) MaximumNever go above this, even under heavy load3 (budget ceiling) How it works with AZs If you set desired=4 across 2 AZs, ASG places 2 in each AZ. If AZ-a goes down, ASG launches 2 replacements in AZ-b (up to the max). This is automatic fault tolerance — you don't write any code for it. Health checks ASG checks if instances are healthy. Two types: EC2 health check: is the instance running? (default — catches crashes and hardware failures) ELB health check: is the ALB's health check passing? (catches app-level failures — process running but returning 500s) Always use ELB health checks when you have a load balancer. An instance can be \"running\" (EC2 healthy) but serving errors (ELB unhealthy). Without ELB health checks, ASG won't replace it. Step 1: Create the launch template (prerequisite) The ASG needs a launch template to know what to launch. We created this in 5.4: # Verify your launch template exists aws ec2 describe-launch-templates \\ --launch-template-names ecommerce-web \\ --query \"LaunchTemplates[0].{ID:LaunchTemplateId,Name:LaunchTemplateName,Version:LatestVersionNumber}\" \\ --output table \\ --profile fahad # Our template: lt-0aaa77778888bbbb (ecommerce-web, references Golden AMI v1.1) Step 2: Create the ASG # Create with desired=1 to stay within free tier aws autoscaling create-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --launch-template LaunchTemplateName=ecommerce-web,Version='$Latest' \\ --min-size 1 \\ --max-size 3 \\ --desired-capacity 1 \\ --vpc-zone-identifier \"subnet-0ccc3333cccc33333,subnet-0ddd4444dddd44444\" \\ --tags Key=Name,Value=fahad-web-asg,PropagateAtLaunch=true \\ Key=Environment,Value=dev,PropagateAtLaunch=true \\ Key=Owner,Value=fahad,PropagateAtLaunch=true \\ --profile fahad vpc-zone-identifierThis specifies which subnets the ASG launches instances into. Using both private app subnets (fahad-private-app-a + fahad-private-app-b) across two AZs gives automatic fault tolerance. Free tier warningUsing desired-capacity 1 (not 2) because nsl-ledgerly is already using the free tier t3.micro allowance. Two t3.micro running simultaneously = the second one costs ~$0.01/hr (~$7.50/month). Step 3: Verify the ASG launched an instance # Check ASG status (wait ~30 seconds after creation) aws autoscaling describe-auto-scaling-groups \\ --auto-scaling-group-names fahad-web-asg \\ --query \"AutoScalingGroups[0].{Min:MinSize,Max:MaxSize,Desired:DesiredCapacity,Instances:Instances[].{ID:InstanceId,AZ:AvailabilityZone,Health:HealthStatus,State:LifecycleState}}\" \\ --output table \\ --profile fahad Troubleshooting: instances array is empty If the ASG shows \"Instances\": [], either it's still launching (wait 30 seconds) or something failed. Check the scaling activity log: # Shows WHY a launch failed (wrong subnet, SG not in VPC, quota hit, etc.) aws autoscaling describe-scaling-activities \\ --auto-scaling-group-name fahad-web-asg \\ --query \"Activities[0].{Status:StatusCode,Desc:Description,Cause:Cause}\" \\ --profile fahad # If this returns null, the ASG hasn't even attempted yet — just wait longer # Full ASG dump (use when --query hides the problem) aws autoscaling describe-auto-scaling-groups \\ --auto-scaling-group-names fahad-web-asg \\ --profile fahad Lab: testing scale-out and scale-in This is the powerful part — manually scaling to see the ASG distribute instances across AZs: # Scale up to 2 instances aws autoscaling set-desired-capacity \\ --auto-scaling-group-name fahad-web-asg \\ --desired-capacity 2 \\ --profile fahad # Wait 30 seconds, then check — should see 2 instances in different AZs aws autoscaling describe-auto-scaling-groups \\ --auto-scaling-group-names fahad-web-asg \\ --query \"AutoScalingGroups[0].Instances[].{ID:InstanceId,AZ:AvailabilityZone,Health:HealthStatus}\" \\ --output table \\ --profile fahad What we observed: After scale-out to desired=2: +--------------+----------+-----------------------+ | AZ | Health | ID | +--------------+----------+-----------------------+ | ap-south-1b | Healthy | i-0ccc3333dddd4444 | | ap-south-1a | Healthy | i-0aaa1111bbbb2222 | +--------------+----------+-----------------------+ → ASG automatically placed one in each AZ for fault tolerance Why it landed one per AZ — the actual mechanism It comes down to this one line from the create-auto-scaling-group command: --vpc-zone-identifier \"subnet-0ccc3333cccc33333,subnet-0ddd4444dddd44444\" That gave the ASG two subnets in two different AZs: subnet-0ccc3333cccc33333 = fahad-private-app-a → ap-south-1a subnet-0ddd4444dddd44444 = fahad-private-app-b → ap-south-1b The ASG automatically distributes instances across whichever subnets it's given, balancing AZs as it goes: desired=2, 2 subnets → 1 in ap-south-1a, 1 in ap-south-1b desired=4, 2 subnets → 2 in ap-south-1a, 2 in ap-south-1b One subnet = no fault toleranceIf --vpc-zone-identifier had listed only subnet-0ccc3333cccc33333, every instance would land in ap-south-1a alone — if that AZ goes down, everything goes down. There's no separate \"spread across AZs\" setting to remember: the ASG only ever launches into the subnets you hand it, so AZ coverage is really a side effect of which subnets you listed. The whole trickGive the ASG subnets in multiple AZs, and it handles the distribution automatically. No extra config, no code — just listing two subnet IDs. # Scale back down to 1 (save money!) aws autoscaling set-desired-capacity \\ --auto-scaling-group-name fahad-web-asg \\ --desired-capacity 1 \\ --profile fahad # Wait 30 seconds, check which one was terminated aws autoscaling describe-auto-scaling-groups \\ --auto-scaling-group-names fahad-web-asg \\ --query \"AutoScalingGroups[0].Instances[].{ID:InstanceId,AZ:AvailabilityZone}\" \\ --output table \\ --profile fahad What we observed: After scale-in to desired=1: +--------------+-----------------------+ | AZ | ID | +--------------+-----------------------+ | ap-south-1a | i-0aaa1111bbbb2222 | +--------------+-----------------------+ → ASG terminated the ap-south-1b instance automatically → Default termination policy: pick the AZ with more instances, terminate the oldest ASG termination policies When the ASG needs to remove an instance during scale-in, it follows a policy: PolicyHow it picksUse when DefaultBalance AZs first → oldest launch template → closest to next billing hourMost cases (what we used) OldestInstanceTerminate the longest-running instanceRolling AMI updates — replace old images first NewestInstanceTerminate the most recently launchedTesting — keep the stable old instances OldestLaunchTemplateTerminate instances using the oldest template versionAfter launch template update — phase out old config Manage ASG lifecycle # Check all ASG details aws autoscaling describe-auto-scaling-groups \\ --auto-scaling-group-names fahad-web-asg \\ --query \"AutoScalingGroups[0].{Name:AutoScalingGroupName,Min:MinSize,Max:MaxSize,Desired:DesiredCapacity,HealthCheck:HealthCheckType,Instances:Instances[].{ID:InstanceId,AZ:AvailabilityZone,Health:HealthStatus}}\" \\ --profile fahad # Update settings (e.g. change max from 3 to 6) aws autoscaling update-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --max-size 6 \\ --profile fahad # Force replace all instances with new launch template version (rolling update) aws autoscaling start-instance-refresh \\ --auto-scaling-group-name fahad-web-asg \\ --profile fahad # Protect a specific instance from scale-in (e.g. running a long job) aws autoscaling set-instance-protection \\ --auto-scaling-group-name fahad-web-asg \\ --instance-ids i-xxxxx \\ --protected-from-scale-in \\ --profile fahad # Delete ASG entirely (terminates all its instances) aws autoscaling delete-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --force-delete \\ --profile fahad End-of-session cleanup Budget ruleSince nsl-ledgerly uses your free tier t3.micro, any ASG instance costs real money. Delete the ASG at the end of each study session. The launch template and Golden AMI stay (free) — recreating the ASG next session is one command. # Delete ASG (terminates its instances automatically) aws autoscaling delete-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --force-delete \\ --profile fahad # Verify no extra instances left running aws ec2 describe-instances \\ --filters \"Name=instance-state-name,Values=running\" \\ --query \"Reservations[].Instances[].{ID:InstanceId,Name:Tags[?Key=='Name']|[0].Value,Type:InstanceType}\" \\ --output table \\ --profile fahad # Should show only nsl-ledgerly Quick recreation next session # One command to bring the ASG back (template + AMI already exist) aws autoscaling create-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --launch-template LaunchTemplateName=ecommerce-web,Version='$Latest' \\ --min-size 1 \\ --max-size 3 \\ --desired-capacity 1 \\ --vpc-zone-identifier \"subnet-0ccc3333cccc33333,subnet-0ddd4444dddd44444\" \\ --tags Key=Name,Value=fahad-web-asg,PropagateAtLaunch=true \\ Key=Owner,Value=fahad,PropagateAtLaunch=true \\ --profile fahad"},{"page":"week2.html","pageLabel":"Week 2","id":"d5-6","title":"Scaling Policies","body":"Target tracking (simplest, most common) Set a target metric, ASG adjusts automatically. You don't define thresholds or step sizes — AWS handles the math: # Keep average CPU at 60% aws autoscaling put-scaling-policy \\ --auto-scaling-group-name fahad-web-asg \\ --policy-name cpu-target-60 \\ --policy-type TargetTrackingScaling \\ --target-tracking-configuration '{ \"PredefinedMetricSpecification\": { \"PredefinedMetricType\": \"ASGAverageCPUUtilization\" }, \"TargetValue\": 60.0 }' \\ --profile fahad What AWS auto-creates for you When you set a target tracking policy, AWS automatically creates two CloudWatch alarms: AlarmWhat it doesAuto-created name AlarmHighCPU > 60% → scale OUT (add instances)TargetTracking-fahad-web-asg-AlarmHigh-xxxxx AlarmLowCPU drops well below 60% → scale IN (remove instances)TargetTracking-fahad-web-asg-AlarmLow-xxxxx You set one target (60% CPU) and AWS created the entire feedback loop — monitoring, decision logic, and actions — automatically. This is why target tracking is the recommended starting point. # See the auto-created alarms aws cloudwatch describe-alarms \\ --alarm-name-prefix \"TargetTracking-fahad-web-asg\" \\ --query \"MetricAlarms[].{Name:AlarmName,State:StateValue,Threshold:Threshold}\" \\ --output table \\ --profile fahad Lab output — what we got Policy created: PolicyARN: arn:aws:autoscaling:ap-south-1:111122223333:scalingPolicy:... Auto-created alarms: AlarmHigh: TargetTracking-fahad-web-asg-AlarmHigh-7cd8afbb-... AlarmLow: TargetTracking-fahad-web-asg-AlarmLow-173efae5-... → One put-scaling-policy command = policy + 2 CloudWatch alarms + auto-scaling logic → No manual alarm configuration needed Predefined metric types for target tracking PredefinedMetricTypeWhat it tracksUse when ASGAverageCPUUtilizationAverage CPU across all instancesGeneral purpose — most common choice ASGAverageNetworkInAverage bytes received per instanceNetwork-bound workloads (streaming, file uploads) ASGAverageNetworkOutAverage bytes sent per instanceNetwork-bound workloads (API responses, downloads) ALBRequestCountPerTargetRequests per target from the ALBWeb apps — scale by traffic volume, not CPU # Example: scale based on requests per target (needs ALB target group ARN) aws autoscaling put-scaling-policy \\ --auto-scaling-group-name fahad-web-asg \\ --policy-name requests-per-target-1000 \\ --policy-type TargetTrackingScaling \\ --target-tracking-configuration '{ \"PredefinedMetricSpecification\": { \"PredefinedMetricType\": \"ALBRequestCountPerTarget\", \"ResourceLabel\": \"app/fahad-web-alb/xxxxx/targetgroup/fahad-web-tg/xxxxx\" }, \"TargetValue\": 1000.0 }' \\ --profile fahad Step scaling (more granular control) Define explicit thresholds and actions — you control exactly how many instances to add at each level: # First create a CloudWatch alarm that triggers the policy aws cloudwatch put-metric-alarm \\ --alarm-name fahad-cpu-high-70 \\ --metric-name CPUUtilization \\ --namespace AWS/EC2 \\ --statistic Average \\ --period 300 \\ --threshold 70 \\ --comparison-operator GreaterThanThreshold \\ --evaluation-periods 2 \\ --dimensions Name=AutoScalingGroupName,Value=fahad-web-asg \\ --profile fahad # Then create the step scaling policy aws autoscaling put-scaling-policy \\ --auto-scaling-group-name fahad-web-asg \\ --policy-name cpu-step-scale-out \\ --policy-type StepScaling \\ --adjustment-type ChangeInCapacity \\ --step-adjustments '[ {\"MetricIntervalLowerBound\": 0, \"MetricIntervalUpperBound\": 20, \"ScalingAdjustment\": 1}, {\"MetricIntervalLowerBound\": 20, \"ScalingAdjustment\": 3} ]' \\ --profile fahad This means: if CPU > 70% → add 1 instance. If CPU > 90% (70+20) → add 3 instances. More control than target tracking, but more to configure. CPU levelActionHow the step adjustment reads 70% – 90%Add 1 instanceLower bound 0, upper bound 20 (above the alarm threshold of 70) > 90%Add 3 instancesLower bound 20, no upper bound (20+ above threshold) < 30%Remove 1 instanceSeparate scale-in policy (not shown) Scheduled scaling Scale based on time, not metrics. Good for predictable traffic patterns: # Scale up to 3 instances at 8am Dhaka time (2am UTC) on weekdays aws autoscaling put-scheduled-update-group-action \\ --auto-scaling-group-name fahad-web-asg \\ --scheduled-action-name morning-scale-up \\ --recurrence \"0 2 * * MON-FRI\" \\ --desired-capacity 3 \\ --profile fahad # Scale back to 1 instance at 10pm Dhaka time (4pm UTC) on weekdays aws autoscaling put-scheduled-update-group-action \\ --auto-scaling-group-name fahad-web-asg \\ --scheduled-action-name evening-scale-down \\ --recurrence \"0 16 * * MON-FRI\" \\ --desired-capacity 1 \\ --profile fahad # List scheduled actions aws autoscaling describe-scheduled-actions \\ --auto-scaling-group-name fahad-web-asg \\ --query \"ScheduledUpdateGroupActions[].{Name:ScheduledActionName,Recurrence:Recurrence,Desired:DesiredCapacity}\" \\ --output table \\ --profile fahad # Delete a scheduled action aws autoscaling delete-scheduled-action \\ --auto-scaling-group-name fahad-web-asg \\ --scheduled-action-name morning-scale-up \\ --profile fahad Cron formatThe recurrence uses cron syntax: minute hour day-of-month month day-of-week. All times are UTC. Dhaka is UTC+6, so 8am Dhaka = 2am UTC. Scale-in protection Prevents specific instances from being terminated during scale-in. Use for instances running long jobs that shouldn't be interrupted: # Protect a specific instance aws autoscaling set-instance-protection \\ --auto-scaling-group-name fahad-web-asg \\ --instance-ids i-xxxxx \\ --protected-from-scale-in \\ --profile fahad # Remove protection aws autoscaling set-instance-protection \\ --auto-scaling-group-name fahad-web-asg \\ --instance-ids i-xxxxx \\ --no-protected-from-scale-in \\ --profile fahad Which policy type to choose? Policy typeComplexityBest forYou manage Target trackingSimplestMost workloads — set target, AWS handles the restJust the target value Step scalingMediumWorkloads with sudden spikes — need different responses at different levelsAlarms + step adjustments ScheduledSimplestPredictable traffic patterns — business hours, batch windowsCron schedules + desired counts CombinedMost complexProduction — scheduled as baseline + target tracking for unexpected spikesAll of the above Start hereFor the ramp plan, target tracking on CPU is enough. Add step scaling or scheduled scaling only if Manager asks \"what would you do for a flash sale?\" — that's when you'd combine scheduled (pre-scale before the sale) + target tracking (handle unexpected spikes during). Manage scaling policies # List all policies on an ASG aws autoscaling describe-policies \\ --auto-scaling-group-name fahad-web-asg \\ --query \"ScalingPolicies[].{Name:PolicyName,Type:PolicyType,Target:TargetTrackingConfiguration.TargetValue}\" \\ --output table \\ --profile fahad # Delete a scaling policy aws autoscaling delete-policy \\ --auto-scaling-group-name fahad-web-asg \\ --policy-name cpu-target-60 \\ --profile fahad # Note: deleting a target tracking policy also deletes its auto-created CloudWatch alarms"},{"page":"week2.html","pageLabel":"Week 2","id":"d6-1","title":"Application Load Balancer (ALB) Deep Dive","body":"The ALB operates at Layer 7 (HTTP/HTTPS). Think of it as a smart receptionist — every request arrives at the front desk, the receptionist checks what's needed (path, host, headers) and sends the guest to the right team (target group). It also checks which team members are healthy — if someone failed their health check, no traffic goes their way. Where the ALB sits in the network Internet │ │ HTTPS :443 — TLS terminates here (cert from ACM, see 6.3) ▼ Internet Gateway (fahad-igw) │ ▼ ┌─────────────────────────────────────────────────┐ │ ALB — public subnets, both AZs │ │ fahad-public-a (10.0.1.0/24) │ │ fahad-public-b (10.0.2.0/24) │ └─────────────────────┬─────────────────────────────┘ │ HTTP :80 — plain, never leaves the VPC ▼ ┌─────────────────────────────────────────────────┐ │ Target group → EC2 (Auto Scaling Group) │ │ fahad-private-app-a (10.0.10.0/24) │ │ fahad-private-app-b (10.0.11.0/24) │ └─────────────────────┬─────────────────────────────┘ │ :5432 ▼ ┌─────────────────────────────────────────────────┐ │ RDS — private DB subnets │ │ fahad-private-db-a (10.0.20.0/24) │ │ fahad-private-db-b (10.0.21.0/24) │ └─────────────────────────────────────────────────┘ Same three-tier VPC from Week 1 (4.9) — the ALB is the only new resource sitting in the public tier. Everything below it, including the instances it forwards to, stays in private subnets with no route to the internet. Key components Listener → Rule → Target Group → Instances Listener: \"I'm listening on port 443 for HTTPS traffic\" Rule: \"If the path starts with /api, forward to target group A\" Target group: \"Here are 2 healthy EC2 instances that can handle this\" Listener Listens on a port (80 for HTTP, 443 for HTTPS). Each listener has rules that determine where to send traffic. Rule Conditions (path, host, headers) + an action (forward to target group, redirect, fixed response). Target group A group of instances (or IPs, or Lambda functions) that receive traffic. Each has its own health check. You cannot change a target group's traffic port after creation — if you need a different port, create a new target group. Path-based routing ALB :443 ├── /api/* → target-group-api (EC2 instances running Django) ├── /static/* → target-group-static (S3 via redirect) ├── /health → fixed response: 200 \"OK\" └── default → target-group-web (EC2 instances running Nginx) One ALB replaces what would otherwise be multiple Nginx reverse proxy configurations. You define the routing in the AWS console or CLI, not in config files. Lab: build the ALB step by step Step 1: Create the ALB — must be in public subnets (both AZs) with the ALB security group. AWS requires at least 2 AZs for redundancy. aws elbv2 create-load-balancer \\ --name fahad-web-alb \\ --subnets subnet-0aaa1111aaaa11111 subnet-0bbb2222bbbb22222 \\ --security-groups sg-0aaa1111aaaa11111 \\ --scheme internet-facing \\ --type application \\ --query \"LoadBalancers[0].{ARN:LoadBalancerArn,DNS:DNSName,State:State.Code}\" \\ --output table \\ --profile fahad Save the ALB ARN and DNS name from the output. The DNS name is what users hit in their browser. Step 2: Create a target group — tells the ALB where to forward traffic. aws elbv2 create-target-group \\ --name fahad-web-tg \\ --protocol HTTP \\ --port 80 \\ --vpc-id vpc-0123456789abcdef0 \\ --target-type instance \\ --health-check-path / \\ --health-check-port 80 \\ --health-check-interval-seconds 30 \\ --healthy-threshold-count 2 \\ --unhealthy-threshold-count 3 \\ --query \"TargetGroups[0].TargetGroupArn\" \\ --output text \\ --profile fahad Port must matchThe target group port must match what your app actually listens on. If Nginx listens on port 80, use port 80. If Django listens on 8080, use 8080. The health check port should also match — a mismatch is the #1 cause of \"unhealthy\" targets. Step 3: Create a listener — connects the ALB to the target group. aws elbv2 create-listener \\ --load-balancer-arn <ALB_ARN> \\ --protocol HTTP \\ --port 80 \\ --default-actions Type=forward,TargetGroupArn=<TARGET_GROUP_ARN> \\ --profile fahad Gotcha: swapped ARNsThe ALB ARN and target group ARN look similar. Putting the wrong one in --load-balancer-arn gives: \"is not a valid load balancer ARN\". Tell them apart by the middle of the string: .../loadbalancer/app/... = ALB, .../targetgroup/... = target group. Step 4: Attach the ASG to the target group aws autoscaling attach-load-balancer-target-groups \\ --auto-scaling-group-name fahad-web-asg \\ --target-group-arns <TARGET_GROUP_ARN> \\ --profile fahad Existing instances don't auto-registerAttaching a target group to an ASG only affects future launches. Instances already running must be registered manually: # Manually register an existing instance aws elbv2 register-targets \\ --target-group-arn <TARGET_GROUP_ARN> \\ --targets Id=i-0aaa1111aaaa11111 \\ --profile fahad # Or force the ASG to replace all instances (rolling update) aws autoscaling start-instance-refresh \\ --auto-scaling-group-name fahad-web-asg \\ --profile fahad Step 5: Verify target health aws elbv2 describe-target-health \\ --target-group-arn <TARGET_GROUP_ARN> \\ --query \"TargetHealthDescriptions[].{ID:Target.Id,Health:TargetHealth.State,Reason:TargetHealth.Reason}\" \\ --output table \\ --profile fahad StateWhat it meansAction initialJust registered, first health checks runningWait 30–60 seconds healthyHealth checks passing, ALB sends traffic hereYou're good — try the browser unhealthyHealth checks failing, ALB stops sending trafficCheck the Reason field — see troubleshooting below drainingInstance being removed, finishing in-flight requestsNormal during scale-in or deregistration Step 6: Test in browser http://fahad-web-alb-XXXXXXXXXX.ap-south-1.elb.amazonaws.com Expected results: ✓ Nginx welcome page → full path working (internet → ALB → EC2 → Nginx) ✗ 502 Bad Gateway → target unhealthy or traffic port mismatch ✗ 503 Service Unavail → no healthy targets registered in the target group Weighted target groups (canary deployments) One rule, two target groups, adjustable weights: aws elbv2 modify-rule \\ --rule-arn <RULE_ARN> \\ --actions '[{ \"Type\": \"forward\", \"ForwardConfig\": { \"TargetGroups\": [ {\"TargetGroupArn\": \"<TG_V1_ARN>\", \"Weight\": 90}, {\"TargetGroupArn\": \"<TG_V2_ARN>\", \"Weight\": 10} ] } }]' \\ --profile fahad 90% of traffic to v1, 10% to v2. Change weights to shift traffic during canary deploys — no server restart needed. Manage ALB # List all ALBs aws elbv2 describe-load-balancers \\ --query \"LoadBalancers[].{Name:LoadBalancerName,DNS:DNSName,State:State.Code}\" \\ --output table \\ --profile fahad # List all target groups aws elbv2 describe-target-groups \\ --query \"TargetGroups[].{Name:TargetGroupName,Port:Port,HealthPort:HealthCheckPort,HealthPath:HealthCheckPath}\" \\ --output table \\ --profile fahad # List listeners on an ALB aws elbv2 describe-listeners \\ --load-balancer-arn <ALB_ARN> \\ --query \"Listeners[].{Port:Port,Protocol:Protocol}\" \\ --output table \\ --profile fahad End-of-session cleanup ALB costs ~$16/monthDelete the ALB and target groups at the end of each study session. Recreating takes 3 commands. # Delete ALB (stops the charge) aws elbv2 delete-load-balancer \\ --load-balancer-arn <ALB_ARN> \\ --profile fahad # Delete target groups (free, but cleanup) aws elbv2 delete-target-group \\ --target-group-arn <TG_ARN> \\ --profile fahad # Delete ASG (stops extra EC2 charge) aws autoscaling delete-auto-scaling-group \\ --auto-scaling-group-name fahad-web-asg \\ --force-delete \\ --profile fahad"},{"page":"week2.html","pageLabel":"Week 2","id":"d6-2","title":"Health Checks — Debugging Guide","body":"The ALB periodically sends an HTTP request to each target. If the response is healthy, traffic flows. If unhealthy, the target is removed from rotation. SettingWhat it doesRecommended PathURL the ALB hits/health — a lightweight endpoint that checks DB connectivity PortWhich port to check onSame as traffic port (avoid mismatches) IntervalHow often to check30 seconds TimeoutHow long to wait for a response5 seconds Healthy thresholdConsecutive successes to mark healthy2 Unhealthy thresholdConsecutive failures to mark unhealthy3 Success codesWhich HTTP status codes count as healthy200 (or 200-299) # Modify health check settings on an existing target group aws elbv2 modify-target-group \\ --target-group-arn <TARGET_GROUP_ARN> \\ --health-check-path /health \\ --health-check-port 80 \\ --health-check-interval-seconds 30 \\ --healthy-threshold-count 2 \\ --unhealthy-threshold-count 3 \\ --profile fahad Troubleshooting by error reason Reason: Target.FailedHealthChecks The ALB reached the instance but got an unexpected HTTP response (not 200). Health check path (/health) doesn't exist — app returns 404 App redirects the path (e.g. / → /login returns 302, not 200) Nothing listening on the health check port — app crashed or wrong port # Fix: change health check to a path that returns 200 aws elbv2 modify-target-group \\ --target-group-arn <TARGET_GROUP_ARN> \\ --health-check-path / \\ --profile fahad Reason: Target.Timeout The ALB sent the health check but got no response at all. Packets were silently dropped. Almost always a security group issue. What we experienced: ALB health check → port 80 → App-sg → BLOCKED (only port 8080 was allowed) Result: Target.Timeout The security group had: ✓ port 8080 from ALB-sg (traffic port) ✗ port 80 from ALB-sg (health check port — MISSING) The health check packets arrived at the instance but the security group silently dropped them → ALB never got a response → timeout # Check what the security group allows aws ec2 describe-security-groups \\ --group-ids <APP_SG_ID> \\ --query \"SecurityGroups[0].IpPermissions[].{Port:FromPort,Source:UserIdGroupPairs[0].GroupId}\" \\ --output table \\ --profile fahad # If the health check port isn't listed, add it aws ec2 authorize-security-group-ingress \\ --group-id <APP_SG_ID> \\ --protocol tcp --port 80 \\ --source-group <ALB_SG_ID> \\ --profile fahad RuleThe health check port must be explicitly allowed in the target's security group with the ALB security group as the source. Same VPC doesn't matter — security groups still apply. Reason: Elb.RegistrationInProgress The target was just registered. Wait 30–60 seconds for the first health checks to complete. Reason: Elb.InternalError ALB internal error. Rare and usually transient. Wait a few minutes and check again. Troubleshooting: 502 Bad Gateway (healthy target, wrong port) Health check passes but browser shows 502. This means the health check port and traffic port are different: What we experienced: Target group created on port 8080 (nothing listening there) Health check changed to port 80 (Nginx responds → target shows \"healthy\") Browser → ALB forwards to port 8080 → connection refused → 502 The fix: can't change a target group's traffic port after creation. Must create a new target group on port 80. # Create new target group on the correct port aws elbv2 create-target-group \\ --name fahad-web-tg-80 \\ --protocol HTTP \\ --port 80 \\ --vpc-id vpc-0123456789abcdef0 \\ --target-type instance \\ --health-check-path / \\ --health-check-port 80 \\ --query \"TargetGroups[0].TargetGroupArn\" \\ --output text \\ --profile fahad # Update the listener to use the new target group aws elbv2 modify-listener \\ --listener-arn <LISTENER_ARN> \\ --default-actions Type=forward,TargetGroupArn=<NEW_TG_ARN> \\ --profile fahad # Swap ASG attachment aws autoscaling detach-load-balancer-target-groups \\ --auto-scaling-group-name fahad-web-asg \\ --target-group-arns <OLD_TG_ARN> \\ --profile fahad aws autoscaling attach-load-balancer-target-groups \\ --auto-scaling-group-name fahad-web-asg \\ --target-group-arns <NEW_TG_ARN> \\ --profile fahad # Register existing instances (attach only affects future launches) aws elbv2 register-targets \\ --target-group-arn <NEW_TG_ARN> \\ --targets Id=i-0aaa1111aaaa11111 \\ --profile fahad # Clean up old target group aws elbv2 delete-target-group \\ --target-group-arn <OLD_TG_ARN> \\ --profile fahad Troubleshooting: 503 Service Unavailable The ALB has no healthy targets at all. # Check if any targets are registered aws elbv2 describe-target-health \\ --target-group-arn <TARGET_GROUP_ARN> \\ --profile fahad # Empty = no targets registered → attach ASG + register instances (see 6.1 step 4) # All unhealthy = follow the debugging steps above Summary: the debugging decision tree Browser shows error ├── 502 Bad Gateway │ ├── Target unhealthy? → check Reason field (FailedHealthChecks / Timeout) │ └── Target healthy but 502? → traffic port ≠ health check port → new TG ├── 503 Service Unavailable │ ├── No targets? → register-targets or attach ASG │ └── All unhealthy? → fix the health check (see above) └── Timeout (page never loads) └── ALB in wrong subnets (private instead of public) or SG missing 80/443 Django tipDon't set the health check path to / on a Django app that redirects / to /login (302). The ALB sees a 302, not a 200, and marks the target unhealthy. Always create a dedicated /health endpoint that returns 200 directly."},{"page":"week2.html","pageLabel":"Week 2","id":"d6-3","title":"TLS Termination with ACM","body":"ACM (AWS Certificate Manager) provides free, auto-renewing TLS/SSL certificates. The ALB terminates TLS — meaning it handles encryption/decryption, and forwards plain HTTP to your backend instances. User → HTTPS (encrypted) → ALB → HTTP (plain) → EC2 instances ↑ TLS terminates here Certificate from ACM Benefits: your EC2 instances don't need to handle TLS certificates, reducing complexity and CPU overhead. The certificate auto-renews — no more expired cert emergencies. Key concepts Certificate A public/private key pair issued by ACM for a domain name (or a wildcard like *.example.com). ACM holds the private key — you never download it, which is exactly why the ALB can use it without you handling key files. Domain validation DNS validation (recommended): ACM gives you a CNAME record to add to your DNS zone, proving you control the domain. Email validation: ACM emails the domain's registered contacts — slower, and breaks auto-renewal if no one clicks the link next time. Always use DNS validation. Region requirement An ACM certificate can only be attached to resources in the same region it was issued in. A cert for an ALB in ap-south-1 must be requested in ap-south-1 — this is the single most common ACM mistake. (CloudFront is the one exception: it always needs its certs in us-east-1, regardless of where your other resources live.) Certificate ARN What you actually reference when creating the HTTPS listener — not the domain name, not the certificate ID by itself. SSL security policy A named bundle of allowed TLS versions and cipher suites, attached to the HTTPS listener (not the certificate). Older policies still allow TLS 1.0/1.1; pick a modern one (ELBSecurityPolicy-TLS13-1-2-2021-06) unless you have a legacy client that genuinely needs an older protocol. The lifecycle 1. Request certificate (ACM) — status: PENDING_VALIDATION 2. Add CNAME validation record (Route 53) — proves you own the domain 3. ACM validates the record — status: ISSUED (few minutes) 4. Attach cert ARN to ALB HTTPS listener — port 443 now serves HTTPS 5. Redirect HTTP → HTTPS on port 80 — no unencrypted path left 6. ACM auto-renews ~60 days before expiry — as long as the CNAME still exists Lab: request a certificate and wire it to the ALB Step 1: Request the certificate — DNS validation, same region as the ALB aws acm request-certificate \\ --domain-name app.example.com \\ --validation-method DNS \\ --tags Key=Name,Value=fahad-app-cert Key=Owner,Value=fahad \\ --query \"CertificateArn\" \\ --output text \\ --profile fahad Wrong region = invisible certificateACM certificates don't show up across regions in the console or CLI by default. If describe-certificates comes back empty, check --region matches where you actually requested it — this alone accounts for most \"my certificate disappeared\" moments. Step 2: Get the DNS validation record ACM wants aws acm describe-certificate \\ --certificate-arn <CERT_ARN> \\ --query \"Certificate.DomainValidationOptions[0].ResourceRecord\" \\ --output table \\ --profile fahad # Returns a Name (CNAME record name), Type (CNAME), and Value (the target) Step 3: Add that record to Route 53 aws route53 change-resource-record-sets \\ --hosted-zone-id <HOSTED_ZONE_ID> \\ --change-batch '{ \"Changes\": [{ \"Action\": \"UPSERT\", \"ResourceRecordSet\": { \"Name\": \"<VALIDATION_RECORD_NAME>\", \"Type\": \"CNAME\", \"TTL\": 300, \"ResourceRecords\": [{\"Value\": \"<VALIDATION_RECORD_VALUE>\"}] } }] }' \\ --profile fahad Don't delete this record laterACM re-checks this CNAME every time it auto-renews. Deleting it after issuance \"to tidy up\" is what silently breaks renewal 13 months later — leave it in place for the life of the certificate. Step 4: Wait for validation, then confirm ISSUED aws acm wait certificate-validated --certificate-arn <CERT_ARN> --profile fahad aws acm describe-certificate \\ --certificate-arn <CERT_ARN> \\ --query \"Certificate.Status\" \\ --output text \\ --profile fahad # PENDING_VALIDATION → ISSUED, usually within a few minutes once DNS propagates Step 5: Create the HTTPS listener on the ALB aws elbv2 create-listener \\ --load-balancer-arn <ALB_ARN> \\ --protocol HTTPS \\ --port 443 \\ --certificates CertificateArn=<CERT_ARN> \\ --ssl-policy ELBSecurityPolicy-TLS13-1-2-2021-06 \\ --default-actions Type=forward,TargetGroupArn=<TARGET_GROUP_ARN> \\ --profile fahad Step 6: Redirect the existing HTTP listener to HTTPS # Modify the port-80 listener's default action from \"forward\" to \"redirect\" aws elbv2 modify-listener \\ --listener-arn <HTTP_LISTENER_ARN> \\ --default-actions '[{ \"Type\": \"redirect\", \"RedirectConfig\": { \"Protocol\": \"HTTPS\", \"Port\": \"443\", \"StatusCode\": \"HTTP_301\" } }]' \\ --profile fahad Best practiceEvery HTTP request now gets a 301 straight to HTTPS instead of being forwarded to a target group. Nobody can reach your app unencrypted, even by typing http:// by hand. Step 7: Point DNS at the ALB, then test # Alias record — same ALB, now serving HTTPS aws route53 change-resource-record-sets \\ --hosted-zone-id <HOSTED_ZONE_ID> \\ --change-batch '{ \"Changes\": [{ \"Action\": \"UPSERT\", \"ResourceRecordSet\": { \"Name\": \"app.example.com\", \"Type\": \"A\", \"AliasTarget\": { \"HostedZoneId\": \"<ALB_HOSTED_ZONE_ID>\", \"DNSName\": \"<ALB_DNS_NAME>\", \"EvaluateTargetHealth\": true } } }] }' \\ --profile fahad # Inspect the handshake and certificate chain directly curl -vI https://app.example.com 2>&1 | grep -A5 \"Server certificate\" SymptomLikely cause Certificate stuck at PENDING_VALIDATIONCNAME record missing, in the wrong hosted zone, or not yet propagated — re-check Step 3 describe-certificates returns nothingWrong --region — certificate was requested somewhere else Browser shows NET::ERR_CERT_COMMON_NAME_INVALIDRequested domain doesn't match the URL — a cert for example.com doesn't cover app.example.com unless requested as a SAN or wildcard Renewal fails after ~13 monthsThe validation CNAME was deleted from Route 53 after issuance Manage certificates # List all certificates in this region aws acm list-certificates \\ --query \"CertificateSummaryList[].{Domain:DomainName,ARN:CertificateArn,Status:Status}\" \\ --output table \\ --profile fahad # Full detail on one certificate (validation status, expiry, in-use resources) aws acm describe-certificate \\ --certificate-arn <CERT_ARN> \\ --query \"Certificate.{Domain:DomainName,Status:Status,NotAfter:NotAfter,InUseBy:InUseBy}\" \\ --output table \\ --profile fahad Cleanup noteUnlike the ALB or NAT Gateway, an ACM certificate costs nothing to leave alone — there's no per-hour charge. You only need to delete one if it's actually InUseBy nothing and you're tidying up; leaving it (and its validation CNAME) in place between study sessions is fine and saves you re-validating next time."},{"page":"week2.html","pageLabel":"Week 2","id":"d6-4","title":"Network Load Balancer (NLB)","body":"The NLB operates at Layer 4 (TCP/UDP). It doesn't inspect HTTP headers or paths — it just forwards TCP connections extremely fast. ALB (path-based, HTTP) vs NLB (TCP passthrough) — both landing on the same target group FeatureALBNLB Layer7 (HTTP/HTTPS)4 (TCP/UDP) RoutingPath, host, header, query stringPort-based only PerformanceGood (thousands of req/sec)Extreme (millions of req/sec) Static IPNo (DNS name only)Yes (Elastic IP per AZ) TLS terminationYesYes (TLS listener) Use whenHTTP APIs, web appsNon-HTTP protocols, extreme perf, static IPs needed For your ramp plan: use ALB for everything HTTP. NLB is for when you need TCP pass-through (database proxies, gRPC, gaming servers) or static IPs for firewall allowlisting. Two behaviors that surprise people coming from ALB Client IP is preserved by default An ALB always shows your instance its own IP as the source (you read the real client IP from the X-Forwarded-For header). An NLB, by default, preserves the original client IP at the TCP layer — your instance sees the actual visitor IP directly, which matters if your app does IP-based logic (rate limiting, geo-blocking) without an HTTP layer to read headers from. Cross-zone load balancing is off by default An ALB always balances evenly across every AZ's targets. An NLB defaults to keeping traffic within the AZ it arrived in — if AZ-a has 1 healthy target and AZ-b has 5, an NLB won't rebalance across zones unless you explicitly enable cross-zone load balancing (which then also starts charging inter-AZ data transfer). Lab: create an NLB with a TCP listener # 1. Allocate a static Elastic IP per AZ (this is what gives the NLB a fixed IP) aws ec2 allocate-address --domain vpc --profile fahad # 2. Create the NLB, one EIP per public subnet aws elbv2 create-load-balancer \\ --name fahad-tcp-nlb \\ --type network \\ --scheme internet-facing \\ --subnet-mappings SubnetId=subnet-0aaa1111aaaa11111,AllocationId=<EIP_ALLOC_A> \\ SubnetId=subnet-0bbb2222bbbb22222,AllocationId=<EIP_ALLOC_B> \\ --query \"LoadBalancers[0].{ARN:LoadBalancerArn,DNS:DNSName}\" \\ --output table \\ --profile fahad # 3. Target group — note the protocol is TCP, not HTTP aws elbv2 create-target-group \\ --name fahad-tcp-tg \\ --protocol TCP \\ --port 5432 \\ --vpc-id vpc-0123456789abcdef0 \\ --target-type instance \\ --health-check-protocol TCP \\ --query \"TargetGroups[0].TargetGroupArn\" \\ --output text \\ --profile fahad # 4. Listener — forwards raw TCP, no path/header awareness at all aws elbv2 create-listener \\ --load-balancer-arn <NLB_ARN> \\ --protocol TCP \\ --port 5432 \\ --default-actions Type=forward,TargetGroupArn=<TARGET_GROUP_ARN> \\ --profile fahad TCP health checks are shallowerA TCP health check only confirms the port accepts a connection — it can't tell you the app behind it is actually healthy (a hung process still accepts TCP connections). For anything where that distinction matters, add an HTTP health check on a different port even though the listener itself stays TCP. Manage & cleanup # List NLBs aws elbv2 describe-load-balancers --query \"LoadBalancers[?Type=='network']\" --profile fahad # Delete — same commands as ALB cleanup aws elbv2 delete-load-balancer --load-balancer-arn <NLB_ARN> --profile fahad aws ec2 release-address --allocation-id <EIP_ALLOC_A> --profile fahad aws ec2 release-address --allocation-id <EIP_ALLOC_B> --profile fahad Elastic IPs cost money when idleAn unattached Elastic IP is billed hourly. Release both EIPs right after deleting the NLB — it's easy to delete the load balancer and forget the IPs are still sitting there charging."},{"page":"week2.html","pageLabel":"Week 2","id":"d6-5","title":"Route 53 — DNS Records & Routing","body":"Record types TypePoints toExample AIPv4 addressapi.example.com → 203.0.113.10 AAAAIPv6 addressapi.example.com → 2600:1f18:... CNAMEAnother domain namewww.example.com → example.com AliasAWS resource (ALB, S3, CloudFront)api.example.com → my-alb-123.elb.amazonaws.com Use AliasAlias records are free (no query charge), support zone apex (example.com, not just subdomains), and automatically resolve to the current IP of the AWS resource. Always use Alias over CNAME for AWS resources. Routing policies PolicyHow it routesUse for SimpleOne record, one answerSingle-region, single endpoint WeightedSplit traffic by percentageCanary at DNS level, gradual migration LatencyRoute to lowest-latency regionActive-active multi-region FailoverPrimary + secondary, auto-switch on health check failureActive-passive DR GeolocationRoute by user's country/continentData residency compliance, localized content Public vs private hosted zones Public hosted zone Resolvable from the internet. This is what you use for a real domain — example.com, delegated to Route 53's name servers at your registrar. Private hosted zone Only resolvable from inside VPCs you attach it to. Useful for internal service discovery (db.internal) without exposing internal names publicly. Lab: create a hosted zone and point it at the ALB # 1. Create the public hosted zone aws route53 create-hosted-zone \\ --name example.com \\ --caller-reference \"fahad-$(date +%s)\" \\ --query \"HostedZone.Id\" \\ --output text \\ --profile fahad # 2. Get the 4 name servers Route 53 assigned — these go at your domain registrar aws route53 get-hosted-zone \\ --id <HOSTED_ZONE_ID> \\ --query \"DelegationSet.NameServers\" \\ --output table \\ --profile fahad # 3. Alias record pointing the apex domain straight at the ALB (no CNAME needed) aws route53 change-resource-record-sets \\ --hosted-zone-id <HOSTED_ZONE_ID> \\ --change-batch '{ \"Changes\": [{ \"Action\": \"UPSERT\", \"ResourceRecordSet\": { \"Name\": \"example.com\", \"Type\": \"A\", \"AliasTarget\": { \"HostedZoneId\": \"<ALB_HOSTED_ZONE_ID>\", \"DNSName\": \"<ALB_DNS_NAME>\", \"EvaluateTargetHealth\": true } } }] }' \\ --profile fahad # 4. Verify resolution dig example.com +short nslookup example.com NS delegation is the step people skipCreating the hosted zone alone does nothing for a live domain — until you copy Route 53's 4 name servers into your registrar's NS records, the internet has no idea Route 53 is authoritative for your domain. This is the single most common \"why doesn't my domain resolve\" cause, and it can take up to 48 hours to propagate (usually much faster). TTL — the trade-off you're actually making Time-to-live controls how long resolvers cache a record before re-checking. Low TTL (60s) means changes propagate fast but you pay more DNS query volume and clients feel any Route 53 blip. High TTL (3600s+) is cheaper and steadier but a bad record takes longer to fix everywhere. Drop the TTL to 60 seconds before a planned cutover (like switching to a new ALB), then raise it back afterward. Manage & cleanup # List all hosted zones aws route53 list-hosted-zones --query \"HostedZones[].{Name:Name,Id:Id}\" --output table --profile fahad # List records in a zone aws route53 list-resource-record-sets --hosted-zone-id <HOSTED_ZONE_ID> --output table --profile fahad # Delete the hosted zone (all records must be removed first, except the default NS/SOA) aws route53 delete-hosted-zone --id <HOSTED_ZONE_ID> --profile fahad CostA hosted zone is $0.50/month regardless of traffic — the one thing in this whole lab that isn't free to leave idle, though it's small enough that most people just keep the zone and delete the records pointing at expensive resources (ALB, NLB) instead."},{"page":"week2.html","pageLabel":"Week 2","id":"d7-1","title":"Multi-AZ & Read Replicas","body":"Two different features that solve different problems: FeatureMulti-AZRead Replica PurposeHigh availability (failover)Read scaling (performance) ReplicationSynchronous — standby always matches primaryAsynchronous — may lag seconds behind Can you read from it?No — standby is dormant until failoverYes — send read queries to it FailoverAutomatic, ~60 seconds, same endpointManual — must promote to standalone Cross-region?No (same region, different AZ)Yes — can be in another region (for DR) CostDouble (two instances running)Additive (each replica is an extra instance) For the ramp planEnable Multi-AZ for production (automatic failover). Add read replicas only when you have read-heavy queries that overwhelm the primary. For learning, single-AZ is fine — Multi-AZ doubles the RDS cost. CLI: Create an RDS instance # 1. Create a DB subnet group (tells RDS which subnets to use) aws rds create-db-subnet-group \\ --db-subnet-group-name fahad-db-subnets \\ --db-subnet-group-description \"Private DB subnets for ecommerce\" \\ --subnet-ids subnet-0eee5555eeee55555 subnet-0fff6666ffff66666 # 2. Create the RDS instance (single-AZ for learning, saves cost) aws rds create-db-instance \\ --db-instance-identifier fahad-ecommerce-db \\ --db-instance-class db.t3.micro \\ --engine postgres \\ --engine-version 16 \\ --master-username ecomadmin \\ --master-user-password '<USE_A_STRONG_PASSWORD>' \\ --allocated-storage 20 \\ --db-subnet-group-name fahad-db-subnets \\ --vpc-security-group-ids <DB_SG_ID> \\ --backup-retention-period 7 \\ --no-publicly-accessible \\ --storage-encrypted \\ --tags Key=Name,Value=fahad-ecommerce-db \\ Key=Environment,Value=dev \\ Key=Owner,Value=fahad # 3. Check status (takes 5-10 minutes to become \"available\") aws rds describe-db-instances \\ --db-instance-identifier fahad-ecommerce-db \\ --query \"DBInstances[0].{Status:DBInstanceStatus,Endpoint:Endpoint.Address,Port:Endpoint.Port}\" \\ --output table # 4. Get the connection endpoint (once available) aws rds describe-db-instances \\ --db-instance-identifier fahad-ecommerce-db \\ --query \"DBInstances[0].Endpoint.Address\" \\ --output text # 5. Create a read replica (for read scaling later) aws rds create-db-instance-read-replica \\ --db-instance-identifier fahad-ecommerce-db-read \\ --source-db-instance-identifier fahad-ecommerce-db # 6. Force a Multi-AZ failover (for testing DR) aws rds reboot-db-instance \\ --db-instance-identifier fahad-ecommerce-db \\ --force-failover # Delete RDS (skip final snapshot for dev) aws rds delete-db-instance \\ --db-instance-identifier fahad-ecommerce-db \\ --skip-final-snapshot Costdb.t3.micro is free tier (750 hrs/month). Multi-AZ doubles it. Don't forget to delete the RDS instance when done studying — unlike EC2, there's no \"stop\" for single-AZ RDS (Multi-AZ can be stopped for up to 7 days). Splitting reads to the replica in Django A read replica does nothing by itself — your app has to be told to send read queries there. Django's multi-database routing handles this: # settings.py DATABASES = { 'default': { # primary — all writes 'ENGINE': 'django.db.backends.postgresql', 'HOST': 'fahad-ecommerce-db.xxxxx.ap-south-1.rds.amazonaws.com', }, 'replica': { # read replica — reads only 'ENGINE': 'django.db.backends.postgresql', 'HOST': 'fahad-ecommerce-db-read.xxxxx.ap-south-1.rds.amazonaws.com', }, } # A router that sends reads to the replica, writes to the primary class ReplicaRouter: def db_for_read(self, model, **hints): return 'replica' def db_for_write(self, model, **hints): return 'default' DATABASE_ROUTERS = ['myapp.routers.ReplicaRouter'] Replication lag is realA read immediately after a write can hit the replica before the write has replicated — a user submits a form, gets redirected, and doesn't see their own data yet. Read-your-own-writes flows (checkout confirmation, profile update) should read from default, not replica. Promoting a read replica If the primary needs replacing permanently (not a failover — a deliberate migration), a replica can be promoted to a standalone, writable instance. This is one-way — you can't turn it back into a replica afterward. aws rds promote-read-replica \\ --db-instance-identifier fahad-ecommerce-db-read \\ --backup-retention-period 7 \\ --profile fahad Watching replica lag aws cloudwatch get-metric-statistics \\ --namespace AWS/RDS \\ --metric-name ReplicaLag \\ --dimensions Name=DBInstanceIdentifier,Value=fahad-ecommerce-db-read \\ --start-time $(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%S) \\ --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \\ --period 300 \\ --statistics Average Maximum \\ --profile fahad Measured in seconds. A steadily growing number means the replica can't keep up with write volume — usually a sign to upsize the replica's instance class, not just tolerate the lag."},{"page":"week2.html","pageLabel":"Week 2","id":"d7-2","title":"Backups & Point-in-Time Recovery","body":"RDS automated backups give you two things: Daily snapshots: full backup taken during your backup window Transaction logs: captured every 5 minutes, stored in S3 Together, these let you restore to any second within your retention window (up to 35 days). # Restore to a specific point in time — creates a NEW instance aws rds restore-db-instance-to-point-in-time \\ --source-db-instance-identifier my-prod-db \\ --target-db-instance-identifier my-prod-db-restored \\ --restore-time \"2026-09-03T10:30:00Z\" CriticalPoint-in-time recovery creates a new RDS instance — it does NOT overwrite the existing one. You must update your app's connection string to point to the new instance. Plan for this in your runbook. Manual snapshots vs automated backups Automated backups Run inside your configured backup window, deleted automatically when the instance is deleted (unless you keep a final snapshot). Feed the point-in-time recovery window. Manual snapshots You trigger these yourself, they persist forever until you delete them — even after the source instance is gone. This is what you take before a risky migration, and what you copy to another region for disaster recovery. # Set the automated backup window and retention aws rds modify-db-instance \\ --db-instance-identifier fahad-ecommerce-db \\ --backup-retention-period 7 \\ --preferred-backup-window \"17:00-17:30\" \\ --apply-immediately \\ --profile fahad # Take a manual snapshot before a risky change aws rds create-db-snapshot \\ --db-instance-identifier fahad-ecommerce-db \\ --db-snapshot-identifier fahad-ecommerce-db-pre-migration-$(date +%Y%m%d) \\ --profile fahad # Copy a snapshot to another region (DR) aws rds copy-db-snapshot \\ --source-db-snapshot-identifier arn:aws:rds:ap-south-1:111122223333:snapshot:fahad-ecommerce-db-pre-migration-20260903 \\ --target-db-snapshot-identifier fahad-ecommerce-db-pre-migration-20260903-eu \\ --source-region ap-south-1 \\ --region eu-west-1 \\ --profile fahad # Restore an instance FROM a snapshot (different from point-in-time restore) aws rds restore-db-instance-from-db-snapshot \\ --db-instance-identifier fahad-ecommerce-db-restored \\ --db-snapshot-identifier fahad-ecommerce-db-pre-migration-20260903 \\ --profile fahad Backup window vs maintenance windowThe backup window and the maintenance window (for engine patches) must not overlap — RDS won't let a patch and a backup fight for the same I/O at once. Pick windows in your lowest-traffic hours, offset by a few hours from each other."},{"page":"week2.html","pageLabel":"Week 2","id":"d7-3","title":"Parameter Groups","body":"Database engine configuration — PostgreSQL settings like max_connections, shared_buffers, work_mem. Applied to an instance or cluster. The default parameter group is read-only. Create a custom one to tune settings: aws rds create-db-parameter-group \\ --db-parameter-group-name ecommerce-pg16 \\ --db-parameter-group-family postgres16 \\ --description \"Tuned for ecommerce workload\" Reboot requiredSome parameter changes (like shared_buffers) require a reboot to take effect. Others (like work_mem) apply immediately. The console shows \"pending-reboot\" for the ones that need it. Attach it, tune a value, apply it # Attach the custom parameter group to the instance aws rds modify-db-instance \\ --db-instance-identifier fahad-ecommerce-db \\ --db-parameter-group-name ecommerce-pg16 \\ --apply-immediately \\ --profile fahad # Change a dynamic parameter — takes effect without a reboot aws rds modify-db-parameter-group \\ --db-parameter-group-name ecommerce-pg16 \\ --parameters \"ParameterName=work_mem,ParameterValue=16384,ApplyMethod=immediate\" \\ --profile fahad # Change a static parameter — queued until the next reboot aws rds modify-db-parameter-group \\ --db-parameter-group-name ecommerce-pg16 \\ --parameters \"ParameterName=shared_buffers,ParameterValue={DBInstanceClassMemory/32768},ApplyMethod=pending-reboot\" \\ --profile fahad # Check what's pending vs applied aws rds describe-db-instances \\ --db-instance-identifier fahad-ecommerce-db \\ --query \"DBInstances[0].DBParameterGroups[0].ParameterApplyStatus\" \\ --output text \\ --profile fahad Parameter groups aren't per-instanceThe same parameter group can be attached to multiple instances. Changing it changes the config for every instance using it — don't tune \"just for one database\" without first checking describe-db-instances for who else is attached to that group."},{"page":"week2.html","pageLabel":"Week 2","id":"d7-4","title":"Safe SQL Operations","body":"Transaction-safe destructive queries Never run UPDATE or DELETE without a transaction wrapper. This pattern saves you from accidental data destruction: BEGIN; -- See what will be affected FIRST SELECT count(*) FROM orders WHERE status = 'cancelled' AND created_at < '2026-01-01'; -- If the count looks right, proceed DELETE FROM orders WHERE status = 'cancelled' AND created_at < '2026-01-01'; -- Verify the result SELECT count(*) FROM orders WHERE status = 'cancelled' AND created_at < '2026-01-01'; -- Should be 0 -- Only then commit — or ROLLBACK if something looks wrong COMMIT; Reverse-out scripts Before running any migration or data change, write the script that undoes it. Store it alongside the change: migrations/ ├── 042_add_full_name_column.sql ← the forward migration ├── 042_add_full_name_column_REVERSE.sql ← the undo script └── 042_add_full_name_column_README.md ← what it does and when it was run Test the reverse script before you run the forward migration. If you can't undo it cleanly, that changes your deployment plan (you need a longer bake time before committing). Connecting to a private RDS instance Your database has no public IP — psql from your laptop can't reach it directly. Tunnel through an instance inside the VPC using Session Manager, no bastion or open SSH port needed: aws ssm start-session \\ --target <INSTANCE_ID_IN_VPC> \\ --document-name AWS-StartPortForwardingSessionToRemoteHost \\ --parameters '{\"host\":[\"fahad-ecommerce-db.xxxxx.ap-south-1.rds.amazonaws.com\"],\"portNumber\":[\"5432\"],\"localPortNumber\":[\"5433\"]}' \\ --profile fahad # In another terminal, connect through the tunnel psql -h localhost -p 5433 -U ecomadmin -d postgres Batching destructive changes on large tables A single UPDATE or DELETE touching millions of rows holds locks for the whole duration and can bloat the transaction log. Batch it instead: -- Instead of one giant UPDATE, loop in batches DO $$ DECLARE rows_affected INT; BEGIN LOOP UPDATE orders SET archived = true WHERE id IN ( SELECT id FROM orders WHERE archived = false AND created_at < '2024-01-01' LIMIT 1000 ); GET DIAGNOSTICS rows_affected = ROW_COUNT; EXIT WHEN rows_affected = 0; COMMIT; END LOOP; END $$; Maintenance window awarenessSchedule large batch operations outside RDS's maintenance window and outside your backup window (7.2) — a patch, a backup, and a million-row batch job all competing for I/O at once is how a routine migration turns into an incident."},{"page":"week2.html","pageLabel":"Week 2","id":"d7-5","title":"EXPLAIN Plans","body":"When a query is slow, EXPLAIN ANALYZE shows you exactly what PostgreSQL is doing: EXPLAIN ANALYZE SELECT o.id, o.total, u.email FROM orders o JOIN users u ON u.id = o.user_id WHERE o.status = 'pending' AND o.created_at > '2026-08-01'; What to look for in the output: Seq Scan on a large table: missing index. Add one on the filtered columns. Nested Loop with high row count: bad join order or missing index on the join column. Sort with high cost: consider adding an index that matches the ORDER BY. Actual rows ≫ estimated rows: stale statistics. Run ANALYZE tablename; Your Django contextDjango's ORM generates SQL you don't always see. Use django-debug-toolbar or queryset.query to inspect the SQL, then run EXPLAIN on the slow ones directly in psql. Reading the plan tree Plans nest — the innermost (most indented) lines run first, feeding rows up to the operations above them. Two costs matter per node: Nested Loop (cost=0.43..8.45 rows=1 width=40) (actual time=0.03..0.05 rows=1 loops=1) -> Index Scan using orders_status_idx on orders o (cost=0.29..4.30 ...) -> Index Scan using users_pkey on users u (cost=0.14..4.15 ...) cost=START..TOTAL — PostgreSQL's own estimate, in arbitrary units, before running actual time=START..TOTAL rows=N loops=M — what really happened when EXPLAIN ANALYZE ran it (multiply time by loops for the real total) The gap between the estimated rows= and the actual rows= is the single most useful number on the page — a huge gap means the planner is choosing a bad strategy because its statistics are wrong, not because the query itself is bad. BUFFERS — is it even hitting disk? EXPLAIN (ANALYZE, BUFFERS) SELECT o.id, o.total, u.email FROM orders o JOIN users u ON u.id = o.user_id WHERE o.status = 'pending'; shared hit = read from cache (fast). shared read = read from disk (slow). A query that's fast in your test but slow in production is often the same query hitting a cold cache instead of a warm one — BUFFERS is how you tell the difference from the plan alone, without guessing. Adding the index this query is missing -- Matches the WHERE clause from the query above CREATE INDEX CONCURRENTLY idx_orders_status_created ON orders (status, created_at) WHERE status = 'pending'; -- Confirm the planner picked it up EXPLAIN ANALYZE SELECT o.id, o.total, u.email FROM orders o JOIN users u ON u.id = o.user_id WHERE o.status = 'pending' AND o.created_at > '2026-08-01'; -- Seq Scan should now read Index Scan (or Bitmap Index Scan) Always CONCURRENTLY in productionA plain CREATE INDEX takes a lock that blocks writes to the table for the whole build — on a large table that can be minutes of a frozen app. CREATE INDEX CONCURRENTLY takes longer and can't run inside a transaction block, but never blocks writes. Finding what to EXPLAIN in the first place -- Requires the pg_stat_statements extension (enable via parameter group: shared_preload_libraries) SELECT query, calls, mean_exec_time, total_exec_time FROM pg_stat_statements ORDER BY total_exec_time DESC LIMIT 10; Sort by total_exec_time to find what's costing the most aggregate time (a cheap query called 100,000 times can outweigh a slow query called twice) — that's usually a better place to start than guessing from user complaints."},{"page":"week2.html","pageLabel":"Week 2","id":"d8-1","title":"S3 Deep Dive","body":"Lifecycle policies Automatically transition objects between storage classes to optimize cost: Day 0: Standard ($0.023/GB) ← hot data, frequent access Day 30: Standard-IA ($0.0125/GB) ← infrequent access, lower cost Day 90: Glacier Instant ($0.004/GB) ← archive, millisecond retrieval Day 365: Delete ← remove old data Set this on the bucket once, and S3 moves objects automatically. No scripts needed. Versioning Keeps all versions of every object. If you accidentally overwrite or delete a file, the previous version still exists. Deleting a versioned object adds a \"delete marker\" — the object is hidden but not gone. You can restore it. RequirementCross-region replication requires versioning enabled on both source and destination buckets. Only new objects replicate — existing objects must be copied manually with aws s3 sync. Cross-region replication Asynchronous replication to a bucket in another region. Used for disaster recovery (RPO) and compliance (data in specific regions). Combined with lifecycle policies, the replicated bucket can use cheaper storage classes. CLI: S3 bucket operations # Create a bucket (name must be globally unique) aws s3 mb s3://fahad-ecommerce-assets-dev # Enable versioning aws s3api put-bucket-versioning \\ --bucket fahad-ecommerce-assets-dev \\ --versioning-configuration Status=Enabled # Upload a file aws s3 cp ./logo.png s3://fahad-ecommerce-assets-dev/images/logo.png # Upload an entire folder aws s3 sync ./static/ s3://fahad-ecommerce-assets-dev/static/ # List objects aws s3 ls s3://fahad-ecommerce-assets-dev/ --recursive # Download a file aws s3 cp s3://fahad-ecommerce-assets-dev/images/logo.png ./downloaded-logo.png # Set lifecycle policy (Standard → IA after 30 days → Glacier after 90) aws s3api put-bucket-lifecycle-configuration \\ --bucket fahad-ecommerce-assets-dev \\ --lifecycle-configuration '{ \"Rules\": [{ \"ID\": \"archive-old-objects\", \"Status\": \"Enabled\", \"Filter\": {\"Prefix\": \"\"}, \"Transitions\": [ {\"Days\": 30, \"StorageClass\": \"STANDARD_IA\"}, {\"Days\": 90, \"StorageClass\": \"GLACIER\"} ], \"Expiration\": {\"Days\": 365} }] }' # Check bucket size and object count aws s3 ls s3://fahad-ecommerce-assets-dev/ --recursive --summarize \\ | tail -2 # Block all public access (security best practice) aws s3api put-public-access-block \\ --bucket fahad-ecommerce-assets-dev \\ --public-access-block-configuration \\ BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true # Delete all objects and the bucket aws s3 rb s3://fahad-ecommerce-assets-dev --force"},{"page":"week2.html","pageLabel":"Week 2","id":"d8-2","title":"Secrets Manager & Parameter Store","body":"FeatureSecrets ManagerParameter Store (SSM) PurposePasswords, API keys, DB credentialsConfiguration values, feature flags, non-secret config Auto rotationYes — Lambda rotates credentials on a scheduleNo built-in rotation Cost$0.40/secret/month + API callsFree tier (standard params), $0.05/advanced/month HierarchyFlat namesHierarchical paths: /app/prod/db-host Use whenDB passwords, third-party API keysApp config, feature flags, non-sensitive settings # Store a database password aws secretsmanager create-secret \\ --name ecommerce/prod/db-password \\ --secret-string '{\"username\":\"app\",\"password\":\"s3cureP@ss\"}' # Retrieve it (your app does this at startup) aws secretsmanager get-secret-value \\ --secret-id ecommerce/prod/db-password \\ --query SecretString --output text # Update (rotate) a secret aws secretsmanager update-secret \\ --secret-id ecommerce/prod/db-password \\ --secret-string '{\"username\":\"app\",\"password\":\"n3wP@ssw0rd\"}' # List all secrets aws secretsmanager list-secrets \\ --query \"SecretList[].{Name:Name,LastChanged:LastChangedDate}\" \\ --output table # Delete a secret (has a 7-day recovery window by default) aws secretsmanager delete-secret \\ --secret-id ecommerce/prod/db-password # --- Parameter Store (SSM) --- # Store a config value (not secret — use StringType) aws ssm put-parameter \\ --name \"/ecommerce/dev/app-port\" \\ --value \"8080\" \\ --type String # Store a secret value (encrypted) aws ssm put-parameter \\ --name \"/ecommerce/dev/api-key\" \\ --value \"sk-abc123\" \\ --type SecureString # Retrieve a parameter aws ssm get-parameter \\ --name \"/ecommerce/dev/app-port\" \\ --query \"Parameter.Value\" --output text # Retrieve with decryption (for SecureString) aws ssm get-parameter \\ --name \"/ecommerce/dev/api-key\" \\ --with-decryption \\ --query \"Parameter.Value\" --output text # List all parameters under a path aws ssm get-parameters-by-path \\ --path \"/ecommerce/dev/\" \\ --query \"Parameters[].{Name:Name,Value:Value}\" \\ --output table NeverNever hardcode secrets in your application code, Dockerfiles, environment files committed to Git, or Terraform state. Always read from Secrets Manager or Parameter Store at runtime."},{"page":"week2.html","pageLabel":"Week 2","id":"d8-3","title":"CloudWatch — Metrics, Alarms & Logs","body":"Metrics Every AWS service publishes metrics to CloudWatch automatically: EC2 CPU utilization, RDS connections, ALB request count, S3 bucket size. You can also publish custom metrics from your app. Alarms Trigger actions when a metric crosses a threshold: # Alert when CPU exceeds 80% for 5 minutes aws cloudwatch put-metric-alarm \\ --alarm-name high-cpu-web \\ --metric-name CPUUtilization \\ --namespace AWS/EC2 \\ --statistic Average \\ --period 300 \\ --threshold 80 \\ --comparison-operator GreaterThanThreshold \\ --evaluation-periods 2 \\ --alarm-actions arn:aws:sns:ap-south-1:111122223333:alerts Alarms can trigger: SNS notifications (email/SMS), Auto Scaling actions, Lambda functions, or Systems Manager actions. CLI: CloudWatch metrics and alarms # Create an SNS topic for alerts (you need this for alarm actions) aws sns create-topic --name fahad-alerts \\ --query \"TopicArn\" --output text # Subscribe your email to the topic aws sns subscribe \\ --topic-arn arn:aws:sns:ap-south-1:111122223333:fahad-alerts \\ --protocol email \\ --notification-endpoint fahadmdkamal@gmail.com # Check your email and click \"Confirm subscription\" # Create alarm: high CPU on an instance aws cloudwatch put-metric-alarm \\ --alarm-name fahad-high-cpu-web \\ --metric-name CPUUtilization \\ --namespace AWS/EC2 \\ --statistic Average \\ --period 300 \\ --threshold 80 \\ --comparison-operator GreaterThanThreshold \\ --evaluation-periods 2 \\ --alarm-actions arn:aws:sns:ap-south-1:111122223333:fahad-alerts \\ --dimensions Name=InstanceId,Value=i-xxxxx # Create alarm: high 5xx errors on ALB aws cloudwatch put-metric-alarm \\ --alarm-name fahad-alb-5xx-errors \\ --metric-name HTTPCode_Target_5XX_Count \\ --namespace AWS/ApplicationELB \\ --statistic Sum \\ --period 60 \\ --threshold 10 \\ --comparison-operator GreaterThanThreshold \\ --evaluation-periods 2 \\ --alarm-actions arn:aws:sns:ap-south-1:111122223333:fahad-alerts # List all alarms aws cloudwatch describe-alarms \\ --query \"MetricAlarms[].{Name:AlarmName,State:StateValue,Metric:MetricName}\" \\ --output table # Get CPU metrics for an instance (last 1 hour) aws cloudwatch get-metric-statistics \\ --namespace AWS/EC2 \\ --metric-name CPUUtilization \\ --dimensions Name=InstanceId,Value=i-xxxxx \\ --start-time $(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%S) \\ --end-time $(date -u +%Y-%m-%dT%H:%M:%S) \\ --period 300 \\ --statistics Average \\ --output table Dashboards Build a visual dashboard per environment. Include: request rate, error rate (5xx), latency p50/p95/p99, CPU utilization, active DB connections, and disk usage. This is the first thing you look at during an incident. CLI: Logs CloudWatch Logs centralizes logs from all your services. Structure: Log group: /ecs/ecommerce-prod ← one per service per environment Log stream: instance-i-0aee8c/app ← one per source (instance, container) Log events: individual log lines ← timestamped, searchable # Create a log group aws logs create-log-group \\ --log-group-name /ec2/fahad-web-dev # List log groups aws logs describe-log-groups \\ --query \"logGroups[].{Name:logGroupName,Size:storedBytes}\" \\ --output table # Tail logs in real-time (requires aws-cli v2) aws logs tail /ec2/fahad-web-dev --follow # Search logs for errors (last 1 hour) aws logs filter-log-events \\ --log-group-name /ec2/fahad-web-dev \\ --filter-pattern \"ERROR\" \\ --start-time $(date -u -d '1 hour ago' +%s)000 \\ --query \"events[].{Time:timestamp,Message:message}\" \\ --output table # Delete a log group (cleanup) aws logs delete-log-group \\ --log-group-name /ec2/fahad-web-dev Log Insights lets you query across all log streams with SQL-like syntax (in the Console → CloudWatch → Logs → Insights): fields @timestamp, @message | filter @message like /ERROR/ | sort @timestamp desc | limit 50"},{"page":"week2.html","pageLabel":"Week 2","id":"d8-4","title":"X-Ray Distributed Tracing","body":"X-Ray traces a request as it flows through your entire system — from the ALB, through your app, to the database, to any external APIs. It shows you exactly where time is spent. Trace: GET /api/orders ├── ALB: 2ms ├── Django app: 145ms │ ├── Auth middleware: 3ms │ ├── DB query (orders): 89ms ← bottleneck! │ ├── DB query (users): 12ms │ └── Serialize response: 41ms └── Total: 147ms Without X-Ray, debugging \"the API is slow\" means guessing. With X-Ray, you see the 89ms database query is the bottleneck — then use EXPLAIN to fix it. Your Django contextThe aws-xray-sdk Python package patches Django, requests, boto3, and psycopg2 automatically. Add it to your middleware and every request is traced — no per-function instrumentation needed."},{"page":"week2.html","pageLabel":"Week 2","id":"d8-5","title":"AWS Budgets — Cost Alerts & Tracking","body":"AWS Budgets lets you set spending limits and get email alerts when you approach or exceed them. Essential for the ramp plan — one forgotten NAT Gateway or ALB can eat your credits overnight. Method 1: Inline (single command, no files) aws budgets create-budget \\ --account-id 111122223333 \\ --budget '{ \"BudgetName\": \"ramp-plan-fahad\", \"BudgetLimit\": {\"Amount\": \"80\", \"Unit\": \"USD\"}, \"TimeUnit\": \"QUARTERLY\", \"BudgetType\": \"COST\", \"TimePeriod\": { \"Start\": \"2026-09-01T00:00:00Z\", \"End\": \"2026-12-31T23:59:59Z\" } }' \\ --notifications-with-subscribers '[ { \"Notification\": { \"NotificationType\": \"ACTUAL\", \"ComparisonOperator\": \"GREATER_THAN\", \"Threshold\": 50, \"ThresholdType\": \"PERCENTAGE\" }, \"Subscribers\": [{ \"SubscriptionType\": \"EMAIL\", \"Address\": \"fahadmdkamal@gmail.com\" }] }, { \"Notification\": { \"NotificationType\": \"ACTUAL\", \"ComparisonOperator\": \"GREATER_THAN\", \"Threshold\": 80, \"ThresholdType\": \"PERCENTAGE\" }, \"Subscribers\": [{ \"SubscriptionType\": \"EMAIL\", \"Address\": \"fahadmdkamal@gmail.com\" }] } ]' \\ --profile fahad Method 2: File-based (separate JSON files — cleaner for version control) Create two files, then reference them: budget.json: { \"BudgetName\": \"ramp-plan-fahad\", \"BudgetLimit\": { \"Amount\": \"80\", \"Unit\": \"USD\" }, \"TimeUnit\": \"QUARTERLY\", \"BudgetType\": \"COST\", \"CostTypes\": { \"IncludeTax\": true, \"IncludeSubscription\": true, \"UseBlended\": false, \"IncludeRefund\": false, \"IncludeCredit\": false, \"IncludeUpfront\": true, \"IncludeRecurring\": true, \"IncludeOtherSubscription\": true, \"IncludeSupport\": true, \"IncludeDiscount\": true, \"UseAmortized\": false }, \"TimePeriod\": { \"Start\": \"2026-09-01T00:00:00Z\", \"End\": \"2026-12-31T23:59:59Z\" } } Date formatDates must be ISO 8601: 2026-09-01T00:00:00Z — with T between date and time, and Z for UTC. Using underscores (2026-09-01_00:00) or omitting Z will fail. notifications.json: [ { \"Notification\": { \"NotificationType\": \"ACTUAL\", \"ComparisonOperator\": \"GREATER_THAN\", \"Threshold\": 50, \"ThresholdType\": \"PERCENTAGE\" }, \"Subscribers\": [{ \"SubscriptionType\": \"EMAIL\", \"Address\": \"fahadmdkamal@gmail.com\" }] }, { \"Notification\": { \"NotificationType\": \"ACTUAL\", \"ComparisonOperator\": \"GREATER_THAN\", \"Threshold\": 80, \"ThresholdType\": \"PERCENTAGE\" }, \"Subscribers\": [{ \"SubscriptionType\": \"EMAIL\", \"Address\": \"fahadmdkamal@gmail.com\" }] } ] GotchaThe notifications file must be a bare JSON array [...], not an object {\"NotificationWithSubscribers\": [...]}. The CLI flag already names the parameter — the file provides just the value. The command: # cd into the directory containing the JSON files first cd ~/Desktop/devops-syllabus aws budgets create-budget \\ --account-id 111122223333 \\ --budget file://budget.json \\ --notifications-with-subscribers file://notifications.json \\ --profile fahad file:// pathsfile://budget.json is relative to your current directory. Use file:///full/path/budget.json (triple slash) for absolute paths, or cd into the directory first. Method 3: AWS Console Go to AWS Console → Billing → Budgets → Create budget. Use the guided wizard — it generates the same thing as the CLI but with a visual form. Good for one-off budgets; CLI is better for reproducibility. All possible field values — reference Budget object fields: FieldPossible valuesDescription BudgetNameAny string (unique per account)Name for the budget. Used in describe/delete commands. BudgetLimit.AmountAny number as a string — e.g. \"80\", \"100.50\"Must be a string, not a number. \"80\" not 80. BudgetLimit.UnitUSD | GB | HOURSUSD for cost budgets. GB or HOURS for usage budgets. BudgetTypeCOST | USAGE | RI_UTILIZATION | RI_COVERAGE | SAVINGS_PLANS_UTILIZATION | SAVINGS_PLANS_COVERAGECOST tracks dollars spent. USAGE tracks resource consumption (hours, GB). The RI/SP types track reserved instance and savings plan efficiency. TimeUnitDAILY | MONTHLY | QUARTERLY | ANNUALLYHow the budget resets. MONTHLY = $80/month. QUARTERLY = $80/quarter. DAILY = $80/day (unusual). TimePeriod: FieldFormatExample StartISO 8601 timestamp2026-09-01T00:00:00Z EndISO 8601 timestamp2026-12-31T23:59:59Z TipIf you omit TimePeriod, the budget starts immediately and has no end date — it recurs forever at the TimeUnit interval. Fine for ongoing monitoring. CostTypes (all booleans — what to include in the cost calculation): FieldDefaultWhen to change IncludeTaxtrueSet false to track pre-tax spend only IncludeSubscriptiontrueIncludes marketplace subscriptions IncludeCredittrueSet false to see raw spend before credits are applied — recommended for learning so you see what you'd actually owe IncludeRefundtrueInclude refunds in the calculation IncludeSupporttrueInclude AWS Support plan charges IncludeDiscounttrueInclude volume and bundled discounts UseBlendedfalseSet true for blended rate (averaged across linked accounts in an Organization) UseAmortizedfalseSet true to spread upfront RI/SP payments across their term IncludeUpfronttrueInclude one-time upfront fees IncludeRecurringtrueInclude recurring monthly fees IncludeOtherSubscriptiontrueInclude other subscription costs CostFilters (optional — narrow budget to specific services): // Track ALL services (recommended — don't add this field) // No CostFilters = everything is tracked // Track only EC2 \"CostFilters\": { \"Service\": [\"Amazon Elastic Compute Cloud - Compute\"] } // Track only RDS and EC2 \"CostFilters\": { \"Service\": [ \"Amazon Elastic Compute Cloud - Compute\", \"Amazon Relational Database Service\" ] } // Track only a specific tag \"CostFilters\": { \"TagKeyValue\": [\"user:Project$ecommerce-platform\"] } RecommendationDon't filter by service for a learning budget — you'll miss NAT Gateway ($32/month), ALB ($16/month), and other costs that aren't EC2. Track everything. Notification fields: FieldPossible valuesDescription NotificationTypeACTUAL | FORECASTEDACTUAL = alert when you've already spent X. FORECASTED = alert when AWS predicts you'll exceed X by end of period. ComparisonOperatorGREATER_THAN | LESS_THAN | EQUAL_TOUsually GREATER_THAN. LESS_THAN is useful for RI utilization (\"alert if utilization drops below 80%\"). Threshold0–100 (when PERCENTAGE) or absolute numberThe value to compare against. 50 = 50% or $50 depending on ThresholdType. ThresholdTypePERCENTAGE | ABSOLUTE_VALUEPERCENTAGE = % of BudgetLimit. ABSOLUTE_VALUE = exact dollar amount. Subscriber fields: FieldPossible valuesDescription SubscriptionTypeEMAIL | SNSEMAIL sends directly to the address. SNS publishes to an SNS topic (can trigger Lambda, Slack, PagerDuty). AddressEmail address or SNS topic ARNWhere the alert goes. Manage existing budgets # List all budgets aws budgets describe-budgets \\ --account-id 111122223333 \\ --query \"Budgets[].{Name:BudgetName,Limit:BudgetLimit.Amount,Spent:CalculatedSpend.ActualSpend.Amount,Type:BudgetType,TimeUnit:TimeUnit}\" \\ --output table \\ --profile fahad # Update a budget (change the limit to $100) aws budgets update-budget \\ --account-id 111122223333 \\ --new-budget '{ \"BudgetName\": \"ramp-plan-fahad\", \"BudgetLimit\": {\"Amount\": \"100\", \"Unit\": \"USD\"}, \"TimeUnit\": \"QUARTERLY\", \"BudgetType\": \"COST\", \"TimePeriod\": { \"Start\": \"2026-09-01T00:00:00Z\", \"End\": \"2026-12-31T23:59:59Z\" } }' \\ --profile fahad # Delete a budget aws budgets delete-budget \\ --account-id 111122223333 \\ --budget-name \"ramp-plan-fahad\" \\ --profile fahad # Check current spend vs budget aws ce get-cost-and-usage \\ --time-period Start=2026-09-01,End=2026-09-30 \\ --granularity DAILY \\ --metrics \"UnblendedCost\" \\ --profile fahad # Spend breakdown by service (find what's costing the most) aws ce get-cost-and-usage \\ --time-period Start=2026-09-01,End=2026-09-30 \\ --granularity MONTHLY \\ --metrics \"UnblendedCost\" \\ --group-by Type=DIMENSION,Key=SERVICE \\ --profile fahad Common budget patterns PatternSettingsUse case Zero-spend trip-wireLimit: $1, TimeUnit: MONTHLY, Threshold: 80%Catch any unexpected spend immediately. Alert at $0.80. Learning budgetLimit: $80, TimeUnit: QUARTERLY, Thresholds: 50% + 80%Your current setup. Covers the full ramp plan period. Monthly guard railLimit: $50, TimeUnit: MONTHLY, Threshold: 90%Per-month cap for ongoing projects. Forecast alertNotificationType: FORECASTED, Threshold: 100%Alert before you exceed — AWS predicts the trajectory and warns early. Per-service budgetCostFilter on SERVICE, Limit: $30Track one expensive service (e.g. RDS only). Per-project budgetCostFilter on TagKeyValue, Limit: $100Track spend by project tag — requires tagged resources."},{"page":"week2.html","pageLabel":"Week 2","id":"d5","title":"EC2, AMIs & Auto Scaling","body":""},{"page":"week2.html","pageLabel":"Week 2","id":"d6","title":"Load Balancing & DNS","body":""},{"page":"week2.html","pageLabel":"Week 2","id":"d7","title":"RDS, SQL & Data Management","body":""},{"page":"week2.html","pageLabel":"Week 2","id":"d8","title":"Storage, Secrets & Observability","body":""},{"page":"week2.html","pageLabel":"Week 2","id":"d9","title":"Build Day + Checkpoint 1","body":"This is the integration day. Everything you built in Days 5–8 comes together into a single working stack: Route 53 (DNS) └── ALB (HTTPS, TLS from ACM) ├── Target group → ASG → EC2 instances (app) │ ├── reads secrets from Secrets Manager │ ├── reads/writes S3 (static assets) │ └── connects to RDS (PostgreSQL) │ └── RDS Multi-AZ (PostgreSQL) ├── automated daily backups └── CloudWatch alarms on connections, CPU, storage CloudWatch: dashboard + alarms + log groups for every component Checkpoint 1 deliverables Full stack running: a request hits the DNS name, reaches the ALB, is forwarded to an EC2 instance, which queries RDS and returns a response. Architecture diagram: draw every component, subnet, AZ, security group, and data flow path. Cost estimate: document expected monthly cost. Compare against the $60 budget. Identify the top cost drivers. Manager's review: walk through the architecture, demonstrate each component, explain every design choice and its trade-off. Common failuresSecurity group not allowing ALB → EC2 traffic. RDS in a public subnet. No health check endpoint. Secrets hardcoded in user-data. No tags. No monitoring. Manager will check all of these."},{"page":"week2.html","pageLabel":"Week 2","id":"checklist","title":"A2 Checklist","body":"Checks are saved in this browser only. EC2 instance launches from a launch template with all 6 tags (Name, Environment, Owner, Project, CostCenter, ManagedBy) Auto Scaling Group with min/desired/max set, spread across 2 AZs, ELB health checks enabled Scaling policy configured (target tracking on CPU or request count) ALB with HTTPS listener, ACM certificate, path-based routing rules, health check on /health Route 53 Alias record pointing to the ALB RDS PostgreSQL in private subnet, security group allows only from app-sg on port 5432 RDS backups enabled with retention period, point-in-time recovery tested S3 bucket with versioning enabled and lifecycle policy (Standard → IA → Glacier → Delete) Secrets Manager storing DB credentials; app reads them at runtime, not hardcoded CloudWatch dashboard with request rate, error rate, latency, CPU, DB connections CloudWatch alarms for high CPU, high error rate, low disk space — connected to SNS for email alerts Architecture diagram showing all components, subnets, AZs, security groups, and data flow"},{"page":"week3.html","pageLabel":"Week 3","id":"d10","title":"Production Container Images","body":"Building an image that's actually fit to run in production, not just to work on a laptop. A Dockerfile that's small and cacheable Multi-stage builds Non-root users Health endpoints Configuration through the environment, not baked into the image Tagging by commit ECR and image scanning A local multi-container stack to develop against"},{"page":"week3.html","pageLabel":"Week 3","id":"d11","title":"ECS Fargate","body":"The serverless container runtime the customer's platform already runs on. Clusters, task definitions and revisions, services Task role vs execution role CPU and memory sizing awsvpc networking and ENI limits ALB integration and service discovery Service auto scaling Rolling deployments and the deployment circuit breaker"},{"page":"week3.html","pageLabel":"Week 3","id":"d12","title":"ECS With Real Deployment Strategies","body":"Three services routed by path, sharing a database — Week 1's deployment-strategy decision matrix applied for real. Blue/green deployment with CodeDeploy A canary release A schema change deployed backward-compatibly Deploy under load and prove zero dropped requests"},{"page":"week3.html","pageLabel":"Week 3","id":"d13","title":"EKS Fundamentals","body":"The Kubernetes side of the ECS-vs-EKS conversation the customer keeps having. Control plane vs data plane Managed node groups vs Fargate profiles kubectl and contexts Pods, deployments, services, ingress ConfigMaps and secrets Requests and limits IRSA for pod-level IAM The horizontal pod autoscaler and the AWS Load Balancer Controller"},{"page":"week3.html","pageLabel":"Week 3","id":"d14","title":"ECS vs EKS, Decided","body":"Deploy the same service to EKS behind an ALB ingress and perform a rolling update, then a troubleshooting clinic across both runtimes. Image pull failure Wrong subnet Missing permission Failing probe Out-of-memory kill Write the ECS-vs-EKS comparison Designated giveThis Friday is the only flexible day in the month. If a public holiday or absence costs a day anywhere in September, it's taken from here — EKS drops to a single day and the comparison becomes a written exercise instead. AWS, Terraform and Jenkins weeks are never compressed."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-1","title":"What Terraform Is, and Why","body":"Terraform is an Infrastructure-as-Code (IaC) tool: you describe the infrastructure you want in text files, and Terraform figures out which API calls turn what currently exists into that. This is a declarative model — you say what should exist (\"one t3.micro instance, this security group attached\"), not how to get there. Compare that to the CLI work from Weeks 1–3, which was imperative — each aws ec2 run-instances command was a step, and nothing recorded that the step had happened except the resource itself. Console / CLI (Weeks 1–3)Terraform What you writeA sequence of clicks or commands to run onceA description of the end state, applied repeatedly Record of intentNone — only whatever exists right nowThe .tf files, committed to git Re-running itRe-clicking or re-running risks creating duplicatesRe-applying an unchanged config does nothing (idempotent) Review before changeNot possible — the change already happenedterraform plan shows the change before it happens IdempotentRunning the same Terraform config twice in a row with nothing changed produces \"no changes\" the second time — it doesn't create a second copy of anything. This is the property that makes it safe to re-run in CI on every commit, unlike re-running a one-off aws command by hand. Terraform itself is cloud-agnostic — the same tool manages AWS, GitHub, Datadog, or a Kubernetes cluster. What connects it to a specific system is a provider (covered next): a plugin that translates Terraform's generic \"create this resource\" into that system's actual API calls."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-2","title":"Install & Provider Configuration","body":"# Install (Linux, via HashiCorp's apt repo) wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | \\ sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg > /dev/null echo \"deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] \\ https://apt.releases.hashicorp.com $(lsb_release -cs) main\" | \\ sudo tee /etc/apt/sources.list.d/hashicorp.list sudo apt update && sudo apt install terraform terraform version Every Terraform project starts by declaring which providers it needs, and pinning their versions — the same principle as pinning a package version in requirements.txt, for the same reason: an unpinned provider can ship a breaking change and your next plan behaves differently with no line changed in your own code. # versions.tf terraform { required_version = \">= 1.7.0\" required_providers { aws = { source = \"hashicorp/aws\" version = \"~> 5.0\" # any 5.x, never 6.x } } } provider \"aws\" { region = \"ap-south-1\" profile = \"fahad\" } Unpinned = unreproducibleSkip required_providers and Terraform silently installs whatever the latest provider version is the first time anyone runs init. Two engineers running init a week apart can end up on two different AWS provider versions, applying the exact same .tf files with different results. A shorter command, if you want one terraform gets typed dozens of times an hour — a shell alias shortens it to two letters. This is a shell feature, not a Terraform one, so it goes in your shell's own config file, not anywhere in this project. # zsh (~/.zshrc) or bash (~/.bashrc) -- same syntax either way echo \"alias tf='terraform'\" >> ~/.zshrc source ~/.zshrc # reload the current shell so the new alias takes effect tf plan tf apply tf state list Where it lives mattersAn alias defined only by typing it at the prompt disappears the moment that terminal closes. Appending it to ~/.zshrc/~/.bashrc makes it permanent — that file re-runs every time a new interactive shell starts, which is also why a fresh terminal always picks up an edit without needing source at all. Widening the constraint later needs -upgrade Bump version = \"~> 5.0\" to \"~> 6.0\" after init has already run once, and a plain terraform init fails instead of picking up the new version: │ Error: Failed to query available provider packages │ │ Could not retrieve the list of available versions for provider hashicorp/aws: │ locked provider registry.terraform.io/hashicorp/aws 5.100.0 does not match │ configured version constraint ~> 6.0; must use terraform init -upgrade to │ allow selection of new versions terraform init -upgrade Why plain init can't fix this on its own.terraform.lock.hcl records the exact provider version everyone on the project is currently using — that's the whole point of a lock file (same reason a package-lock.json exists). A bare init only installs whatever the lock file already says; it deliberately never widens a lock to satisfy a new constraint on its own, since silently upgrading everyone's provider version on a routine init is exactly the unreproducible behavior the lock file exists to prevent. -upgrade is the explicit, opt-in step that re-resolves the constraint and rewrites the lock file — commit that rewritten lock file so the rest of the team gets the same resolved version. A stray env var beats the profile every timeA real plan once failed with InvalidClientTokenId: The security token included in the request is invalid — even though aws sts get-caller-identity --profile fahad worked fine from a different terminal, same machine, same credentials file. Cause: an AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY left exported earlier in that specific shell session, from something typed by hand. Both the AWS CLI and the Terraform AWS provider check environment variables before the profile in ~/.aws/credentials — a stale exported key silently overrides profile = \"fahad\" in every command run in that terminal, with no warning that it's happening. env | grep AWS is the first thing to check when a credential error doesn't match what ~/.aws/credentials actually contains."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-3","title":"HCL Basics: Resources & Data Sources","body":"Terraform's config language is HCL. Everything is a named block: a type, a local name you choose, and a body of arguments. If you already know a programming language HCL isn't object-oriented, but the shapes rhyme closely enough that mapping onto OOP terms you already know is a legitimate shortcut — as long as you also clock where the mapping breaks down. HCLOOP equivalent A provider (hashicorp/aws)An imported library/SDK — brings a vocabulary of pre-built \"classes\" into scope, the way import boto3 brings AWS's API surface into a Python script A resource/data type (aws_instance, aws_ami)A class — a fixed blueprint of what arguments and attributes this kind of thing has. You never define one yourself; the provider plugin ships it, versioned (15.2) resource \"aws_instance\" \"app\" { ami = ... }Instantiating an object: app = aws_instance(ami=...). The local name (\"app\") is the variable you're assigning that instance to A data blockCalling a read-only getter/query function — not constructing something new, just fetching an existing object's attributes into a reference you can use aws_instance.app.public_ipDot-notation property access on an object instance — exactly like my_object.public_ip A variable blockA typed function parameter, complete with a default value and validation — closer to a TypeScript-style typed parameter than an untyped one A locals blockA local constant computed once — like const in JS or final in Java: read-only, can't be overridden from outside An output blockA return statement — hands a value back to whoever called this config: a human at the CLI for a root module, or the parent config for a child module A module callCalling a function with named arguments (its variables) that hands back named return values (its outputs) — see 16.4 for designing that interface Where the analogy breaksYou can't subclass aws_instance, add a method to it, or define your own resource types — the \"classes\" here are closed, fixed by whatever the provider version ships (15.2). And critically, HCL isn't executed top-to-bottom like a script: block order in the file is irrelevant. Terraform reads the references between blocks (see the dependency-order note further down this section) and builds a dependency graph, then executes in whatever order that graph requires — closer to how a spreadsheet evaluates formulas by their cell references than to a program running line by line. Typed, but not general-purposevariable blocks are genuinely strictly typed (string, number, bool, list(...), map(...), object({...})) with no implicit coercion between incompatible types, and support the same kind of input validation you'd write at a typed function's boundary (16.4). What HCL doesn't have is general-purpose control flow — no user-defined classes, no arbitrary loops, no if/else statements outside of a handful of expression-level substitutes (a ternary, for expressions, dynamic blocks). It's a declarative configuration language with a real type system bolted on, not a programming language you'd write an algorithm in. resource \"aws_security_group\" \"web\" { name = \"web-sg\" vpc_id = var.vpc_id ingress { from_port = 80 to_port = 80 protocol = \"tcp\" cidr_blocks = [\"0.0.0.0/0\"] } } resource means \"create and manage this\" — Terraform will create it, and destroy it if you remove the block. data means \"look this up, don't manage it\" — for something that already exists and you just need to reference (an existing AMI, an existing VPC someone else created). What you can set vs what Terraform tells you afterward An aws_instance plan shows 40+ fields, but only a handful are ones you control — the rest are marked (known after apply) because AWS assigns them at creation time and no config value could ever have set them. + resource \"aws_instance\" \"app_server\" { + ami = \"ami-0333333333333333\" # you set this + instance_type = \"t3.micro\" # you set this + arn = (known after apply) # AWS assigns this + id = (known after apply) # AWS assigns this + key_name = (known after apply) # settable, just unset + public_ip = (known after apply) # AWS assigns this + subnet_id = (known after apply) # settable, just unset + vpc_security_group_ids = (known after apply) # settable, just unset } (known after apply) covers two different situations that look identical in a plan: an argument you simply didn't set (like key_name or subnet_id above — add it and it stops being unknown), and a true output that no config value could ever populate (like arn or id — those only exist once AWS creates the real object). CategoryExamples on aws_instance Set, and showing a real valueami, instance_type, tags Optional, unset — add these to customisekey_name, subnet_id, vpc_security_group_ids, associate_public_ip_address, availability_zone, user_data, monitoring, root_block_device, metadata_options, iam_instance_profile Pure output — never settable, at allarn, id, instance_state, private_dns, public_dns, primary_network_interface_id, password_data The registry docs tell you which is whichEvery resource page on the Terraform Registry splits into an \"Argument Reference\" section (settable — everything in the second row above) and an \"Attributes Reference\" section (read-only — the third row). If something's listed only under Attributes, no amount of config will ever set it directly; it's always read back via a reference like aws_instance.app_server.public_ip, the same dot-notation used for data.aws_ami.ubuntu.id below. data \"aws_ami\" \"ubuntu\" { most_recent = true owners = [\"099720109477\"] # Canonical filter { name = \"name\" values = [\"ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*\"] } } resource \"aws_instance\" \"app\" { ami = data.aws_ami.ubuntu.id # reference: type.name.attribute instance_type = \"t3.micro\" vpc_security_group_ids = [aws_security_group.web.id] } The data block, reserved keywords vs names you chose data Reserved keyword — \"look this up, don't create or manage it.\" The opposite of resource. \"aws_ami\" The data source's type. Fixed, defined by the AWS provider — this is the provider's built-in \"look up an AMI\" query, not something you name yourself. \"ubuntu\" A local name you invented. Purely a label so the rest of your config can reference this lookup (data.aws_ami.ubuntu.id above). Rename it to \"my_ami\" and nothing breaks except that one reference. most_recent A fixed argument from the aws_ami schema, not custom. When the filters below match more than one AMI, take the newest instead of erroring on ambiguity. filter { name = \"name\" ... } filter is a repeatable nested block — stack several and all must match (AND logic). Inside it, name is a fixed key that says which AMI field you're filtering on; confusingly, its value here is also the literal string \"name\", meaning \"filter on the AMI's own Name field\" (as opposed to architecture, owner-alias, etc.) — the key and the value are unrelated, they just happen to use the same word. values Fixed argument, always a list even for one item. Supports * wildcards — the trailing * here exists because the real AMI name ends in a build timestamp that changes with every release. owners Fixed top-level argument, restricts results to AMIs published by this specific AWS account. 099720109477 is Canonical's real, publicly documented account ID for official Ubuntu AMIs — not a secret, the same ID AWS's own docs use. Why owners isn't optionalWithout it, the name-pattern filter alone could match an AMI from any account that happens to use a similar name — including a malicious lookalike published to look official. Pinning owners is what makes this query trustworthy, not just convenient. This runs as a live queryThe whole data block executes against the real AWS API at plan/apply time — it returns whatever AMI currently matches, so aws_instance.app always gets the latest Ubuntu AMI instead of a hardcoded ID that goes stale next month. This is where dependency order comes fromaws_instance.app references aws_security_group.web.id and data.aws_ami.ubuntu.id — Terraform reads these references to build its dependency graph automatically. It knows to create the security group before the instance without you writing anything that says \"do this first.\" This is exactly why explicit depends_on (see 16.3) is a last resort, not a first instinct — most ordering should come from references like these. Heredoc strings — for a multi-line value like user_data Writing a multi-line script inline as a normal quoted string means escaping every newline. A heredoc avoids that — everything between the opening marker and its matching closing line is taken literally, newlines included: resource \"aws_instance\" \"app\" { # ... user_data = <<-EOF #!/bin/bash sudo apt-get update sudo apt-get install -y apache2 EOF } No space between <<- and the marker<<- EOF (with a space) fails to parse at all — Invalid expression: Expected the start of an expression, but found an invalid expression token — because <<- and the marker name have to be written as one unbroken token, <<-EOF. With the space, Terraform doesn't recognize it as a heredoc opener, and everything that follows (including the actual script content) gets misparsed as HCL rather than treated as string content. Why the hyphen<<-EOF (with the hyphen) strips the leading whitespace common to every line, which is what lets the closing EOF sit indented to match the surrounding code instead of needing to start at column 0, the way a plain <<EOF would require."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-4","title":"The Workflow: init, plan, apply, destroy","body":"# 1. Download providers/modules this config needs (run once per checkout, or after adding one) terraform init # 2. Show what WOULD change, without changing anything terraform plan # 3. Actually make the change (re-shows the plan, asks for confirmation) terraform apply # 4. Tear everything this config manages back down terraform destroy init Downloads the provider plugins declared in versions.tf and any modules referenced by source, and sets up the backend that stores state (Day 17). Safe to re-run any time — it doesn't touch real infrastructure. plan Compares your .tf files against Terraform's record of what it last created (the state file) and against the real infrastructure, and prints exactly what it would add, change, or destroy. Nothing is touched. This is the step you never skip. apply Runs the same comparison as plan, shows it to you, and — after you type yes — makes the real API calls. destroy The inverse of apply: deletes everything this config currently manages. Same confirmation prompt, same real API calls, in reverse. Never apply without reading the planThe habit that matters more than any command: read what plan says before typing yes at apply. A one-line variable change can produce a plan that destroys and recreates a database if it touches a value that forces replacement — the diff is the only place that would be visible before it happens."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-5","title":"Reading a Plan Diff Line by Line","body":"Terraform will perform the following actions: # aws_instance.app will be updated in-place ~ resource \"aws_instance\" \"app\" { id = \"i-0aaa1111aaaa11111\" ~ instance_type = \"t3.micro\" -> \"t3.small\" # (28 unchanged attributes hidden) } # aws_launch_template.app must be replaced -/+ resource \"aws_launch_template\" \"app\" { ~ id = \"lt-0bbb2222bbbb22222\" -> (known after apply) ~ image_id = \"ami-0111111111111111\" -> \"ami-0222222222222222\" # forces replacement name_prefix = \"app-\" } Plan: 1 to add, 1 to change, 1 to destroy. ~ update in-place The resource stays, but an attribute changes on the live object — no downtime for something like an EC2 instance's instance_type (it does still require a stop/start under the hood for that particular attribute, which Terraform will also tell you about). -/+ destroy and re-create (replacement) The changed attribute can't be updated on the existing object at all — AWS's API has no \"update\" call for it — so Terraform must delete the old one and create a new one. The line # forces replacement tells you exactly which attribute triggered this. + / - alone A resource being newly created, or a resource being removed because its block was deleted from the config. \"Plan: X to add, Y to change, Z to destroy\" The summary line at the bottom. Read the detail above it first — \"1 to change\" reads harmless right up until you notice it's the production database's launch template being replaced, not updated. The line to actually watch for# forces replacement is the single most consequential phrase in any plan output. For anything stateful (a database, an EBS volume with data on it), a forced replacement means the resource is destroyed and a brand-new empty one created in its place — Terraform will not warn you beyond this comment, and it will not stop to ask if you're sure that's what you meant."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-6","title":"Variables, Locals & Outputs","body":"Three ways values flow through a config: variable for input the caller supplies, locals for a value computed once and reused, output for a value handed back out. variable \"environment\" { type = string description = \"dev or stg\" validation { condition = contains([\"dev\", \"stg\"], var.environment) error_message = \"environment must be \\\"dev\\\" or \\\"stg\\\".\" } } locals { name_prefix = \"app-${var.environment}\" # computed once, referenced everywhere below } resource \"aws_instance\" \"app\" { ami = data.aws_ami.ubuntu.id instance_type = \"t3.micro\" tags = { Name = local.name_prefix } } output \"instance_public_ip\" { value = aws_instance.app.public_ip description = \"Public IP, for the smoke test after apply.\" } variable vs localsA variable is set from outside the config — a .tfvars file, a -var flag, an environment variable. A locals value is computed inside the config from other values and can never be overridden from outside. If you find yourself wanting to override a locals value, it should have been a variable from the start. Validation catches typos before AWS doesWithout the validation block above, passing environment = \"staging\" (instead of \"stg\") would sail through plan and only fail deep into apply once some downstream resource tries to use it, with a much less obvious error. Same principle as module-level validation (see 16.4), applied to root-level variables too. Outputting a whole count/for_each collection, not just one instance The output above reads one instance's public_ip directly. With count or for_each, there's no single instance to read from — aws_instance.app is a collection, one object per index or key. A for expression inside the output pulls one attribute out of every entry at once: resource \"aws_instance\" \"app\" { count = 3 # ... } output \"instance_ids\" { value = { for idx, inst in aws_instance.app : idx => inst.id } description = \"Every instance's ID, keyed by its count index.\" } # result: { 0 = \"i-0aaa...\", 1 = \"i-0bbb...\", 2 = \"i-0ccc...\" } This is a map comprehension, not a loop{ for k, v in collection : k => v.attr } reads almost exactly like Python's {k: v.attr for k, v in collection.items()} — both take an iterable of key/value pairs and return a brand-new map with the same keys but transformed values. Unlike a Python for statement, there's no loop body here at all — the whole expression is the result, which is true of every for in HCL, inside an output, a locals, or a for_each argument alike."},{"page":"week4.html","pageLabel":"Week 4","id":"d15-7","title":"Supplying Variable Values — .tfvars Files and Loading Order","body":"A variable block (15.6) declares that an input exists. It doesn't supply a value — that's a separate question, and a .tfvars file is the most common answer. It's just a list of assignments, one per variable, no variable keyword involved: # stage.tfvars instance_type = \"t3.micro\" region = \"ap-south-1\" ami_id = \"ami-0333333333333333\" username_prefix = \"fmk-tf\" app_env = \"staged\" A second file, production.tfvars, declares the exact same variable names with different values — that's the entire mechanism behind \"the same code, different environment.\" Nothing in main.tf changes; only which file gets loaded does. Two ways a .tfvars file actually gets loaded terraform.tfvars (or anything named *.auto.tfvars) Loaded automatically, every time — no flag needed. Convenient for a single-environment project, but it means there's nothing in the command line that says which values got used; you'd have to go look at the file. Any other filename — stage.tfvars, production.tfvars Never loaded automatically. Requires an explicit -var-file flag, every time: terraform plan -var-file=\"stage.tfvars\" terraform plan -var-file=\"production.tfvars\" This is the better default for anything with more than one environment — the environment being targeted is now visible in the command itself, not hidden inside a filename someone has to already know to look for. Precedence, when more than one source sets the same variableLowest to highest: an TF_VAR_name environment variable, then terraform.tfvars, then any *.auto.tfvars files (alphabetical), then every -var-file in the order given on the command line, then a bare -var flag — highest priority, wins over everything. In practice this rarely needs memorizing in full; the one rule worth keeping is that whatever's passed explicitly on the command line always beats whatever's sitting in a file. A variable with no type accepts anything # valid HCL -- no type, no default, no description variable \"app_env\" {} No type means no validation at allThis isn't a shortcut, it's an opt-out. Terraform infers the type from whatever's actually passed at runtime and checks nothing — a .tfvars file typo that supplies a list where a string was expected won't be caught here; it'll propagate until something downstream breaks, with an error far away from the actual mistake. It also means the variable is required with no visible signal of what shape a caller should supply — compare that to variable \"instance_count\" with type = number and default = 2 (15.6/16.4), which documents both the shape and a safe fallback in three lines. Always give a variable a type, even one as loose as any — that's still a deliberate choice, not an accident of leaving the argument out."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-1","title":"count vs for_each","body":"Both repeat a resource block. count indexes by number (0, 1, 2…). for_each indexes by a stable key from a map or set of strings. That difference is small on day one and expensive a month later. # count -- indexed by position resource \"aws_subnet\" \"private\" { count = length(var.private_cidrs) vpc_id = var.vpc_id cidr_block = var.private_cidrs[count.index] tags = { Name = \"private-${count.index}\" } } # state addresses: aws_subnet.private[0], aws_subnet.private[1], aws_subnet.private[2] # for_each -- indexed by key resource \"aws_subnet\" \"private\" { for_each = var.private_subnets # map(object({ cidr = string, az = string })) vpc_id = var.vpc_id cidr_block = each.value.cidr availability_zone = each.value.az tags = { Name = \"private-${each.key}\" } } # state addresses: aws_subnet.private[\"a\"], aws_subnet.private[\"b\"], aws_subnet.private[\"c\"] The gotchaRemove the middle element from a count-driven list and every subnet after it shifts down one index. Terraform doesn't see \"one subnet removed\" — it sees [2] now holding what used to be at [3], so it destroys and recreates every resource from that index onward, not just the one you actually deleted. On a subnet with an attached NAT gateway or an RDS instance, that's an outage triggered by an unrelated one-line diff. The fixfor_each keys are strings, not positions, so removing one entry (aws_subnet.private[\"b\"]) only touches that address — everything else in state is untouched. If you're stuck with a list, convert it to a map keyed by something stable first: { for s in var.list : s.name => s }. count.index only exists where count is setTrying to reference count.index inside a locals block fails outright: Error: Reference to \"count\" in non-counted context — The \"count\" object can only be used in \"module\", \"resource\", and \"data\" blocks, and only when the \"count\" argument is set. A locals block has no loop of its own to index into. Building a list of per-item values for later use needs a for expression with its own loop variable instead — [for i in range(3) : \"name-${i}\"] — then index into that list with count.index from inside the resource that actually has count. merge() — overriding one key of a map without losing the rest A common need with count/for_each: reuse a shared base set of tags on every resource, but give each instance its own Name. Assigning tags directly replaces the whole map — merge() combines two maps instead, with the second map's keys winning on any overlap: variable \"project_environment\" { type = map(string) default = { Name = \"app\" Owner = \"fahad\" Event = \"learning-devops-tf\" } } resource \"aws_instance\" \"app\" { count = 3 # ... tags = merge( var.project_environment, { Name = \"${var.project_environment[\"Name\"]}-${count.index + 1}\" } ) } # result per instance: { Name = \"app-1\", Owner = \"fahad\", Event = \"learning-devops-tf\" } # { Name = \"app-2\", Owner = \"fahad\", Event = \"learning-devops-tf\" } ... Without merge(), the rest of the map silently disappearstags = { Name = \"app-${count.index + 1}\" } on its own is valid HCL — it just replaces tags entirely, so Owner and Event quietly vanish from every instance with no warning. merge(mapA, mapB) returns a new map containing every key from both — mapB's value wins wherever a key exists in both — which is what lets one shared variable stay the single source of truth for the tags every resource has in common. countfor_each Index typeNumberString key (map or set) Removing a middle itemReshuffles and recreates everything after itDestroys only that one resource Reference syntaxcount.indexeach.key / each.value Good fitN identical, disposable copies (e.g. N NAT gateways, one per AZ index)Anything you'll add to, remove from, or look up by name later Default to for_each. Reach for count only for a genuinely fixed, order-independent replication count, or the simple count = var.enabled ? 1 : 0 toggle for an optional resource."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-2","title":"Dynamic Blocks","body":"A dynamic block generates a repeatable nested block from a list or map — for arguments that appear once per resource, plain interpolation is enough; dynamic is for things like a security group's ingress block, where you need a variable number of them. variable \"ingress_rules\" { type = list(object({ port = number cidr_blocks = list(string) description = string })) } resource \"aws_security_group\" \"app\" { name = \"app-sg\" vpc_id = var.vpc_id dynamic \"ingress\" { for_each = var.ingress_rules content { from_port = ingress.value.port to_port = ingress.value.port protocol = \"tcp\" cidr_blocks = ingress.value.cidr_blocks description = ingress.value.description } } } Only for nested blocksdynamic exists because ingress { ... } is a nested block, not an argument — you can't just pass it a list the way you'd pass a list to cidr_blocks. If the thing you're repeating is a plain argument, you don't need dynamic at all; a for expression inside the argument is enough. Readable but easy to overuseA security group with one dynamic \"ingress\" block driven by a well-named variable is clear. Three or four nested dynamic blocks inside one resource, each driven by a different variable, is usually a sign the resource wants to be a module instead — dynamic blocks trade explicitness for flexibility, and that trade stops paying off past one level of nesting. A dynamic block only means anything inside a resourceWriting dynamic \"ingress\" { ... } at the top level of a file — not nested inside any resource block — fails with Unexpected block: Blocks of type \"dynamic\" are not expected here. A dynamic block generates a nested block for whichever resource it lives inside; on its own, with no resource around it, Terraform doesn't recognize dynamic as a valid block type at all. It has to sit exactly where a literal ingress { } block would otherwise go, inside resource \"aws_security_group\" \"main\" { ... }. ingress.value isn't self-reference — it's a name collisionThe label on dynamic \"ingress\" does two jobs at once: it says which nested block type to generate, and it becomes the default name of the loop variable inside content { } — which is why ingress.value can look like the block referencing itself. It isn't; it's the same role as each.value on a resource-level for_each, just defaulting to a name that happens to match the block label. An explicit iterator argument removes the ambiguity — add iterator = rule alongside for_each in the block above, then use rule.value.port in place of every ingress.value.port inside content { }. Same result, but rule.value reads unambiguously as \"the current item,\" never as the block calling itself."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-3","title":"lifecycle and Explicit Dependencies","body":"Terraform infers most dependencies automatically from references between resources. lifecycle and depends_on exist for the cases it can't infer, or gets wrong for your situation. lifecycle meta-argument resource \"aws_launch_template\" \"app\" { name_prefix = \"app-\" image_id = var.ami_id instance_type = var.instance_type lifecycle { create_before_destroy = true } } resource \"aws_db_instance\" \"primary\" { # ... lifecycle { prevent_destroy = true ignore_changes = [password] # rotated out-of-band, don't fight it } } create_before_destroy Builds the replacement before tearing down the original. Without it, a launch template change destroys the old template first — if the new one fails to create, the ASG is left pointing at nothing. Essential for anything an ASG or ALB target group references. prevent_destroy Makes terraform destroy (and any plan that would replace the resource) fail loudly instead of deleting it. Use it on anything with data you can't regenerate from code — the primary database, not the ASG. ignore_changes Tells Terraform to stop diffing specific attributes, even if the real resource drifts from what's in config. Use it for values something else legitimately manages (autoscaling-adjusted desired_capacity, a password rotated by Secrets Manager) — never as a blanket way to silence a diff you don't understand. prevent_destroy blocks real teardown tooIt doesn't distinguish \"someone fat-fingered destroy\" from \"we're intentionally decommissioning this.\" A genuine teardown means removing the lifecycle block (or setting prevent_destroy = false) first, applying that change, then destroying — an explicit two-step, by design. Explicit depends_on Terraform sees a dependency wherever one resource's argument references another resource's attribute. depends_on is for ordering that exists only in AWS's behavior, not in any attribute reference — e.g. an IAM role's policy attachment finishing before the compute resource that assumes that role is created. resource \"aws_ecs_service\" \"app\" { # nothing here references aws_iam_role_policy_attachment.ecs_exec directly, # but the task will fail to start if the permissions aren't attached yet depends_on = [aws_iam_role_policy_attachment.ecs_exec] # ... } Reach for it lastEvery explicit depends_on is a sign Terraform couldn't infer the relationship from your config. Before adding one, check whether restructuring the reference (e.g. reading the role's ARN from an output instead of hardcoding it) would let Terraform infer the same ordering automatically — that stays correct even if the resources are later refactored."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-4","title":"Module Interface Design","body":"A module's variables.tf and outputs.tf are its API. Everything inside main.tf is an implementation detail the caller shouldn't need to know about. # variables.tf variable \"name\" { type = string description = \"Prefix applied to every resource this module creates.\" } variable \"instance_type\" { type = string default = \"t3.micro\" description = \"EC2 instance type for the ASG's launch template.\" validation { condition = can(regex(\"^t3\\\\.\", var.instance_type)) error_message = \"Only t3.* instance types are approved for this environment.\" } } variable \"vpc_id\" { type = string description = \"VPC to launch into. No default -- callers must be explicit.\" } # outputs.tf output \"asg_name\" { value = aws_autoscaling_group.this.name description = \"Name of the created Auto Scaling Group, for attaching alarms.\" } output \"security_group_id\" { value = aws_security_group.this.id description = \"SG ID, so callers can add extra ingress rules without editing this module.\" } Required vs optionalA variable with no default is required — the caller must supply it, and Terraform refuses to plan without it. That's the right choice for anything with no safe guess (vpc_id, name). Give a default only to genuinely optional knobs (instance_type, tag overrides) — defaulting something like vpc_id \"for convenience\" is how a module quietly gets applied into the wrong VPC. Validate at the boundaryA validation block turns \"the apply failed 4 minutes in with an opaque AWS error\" into \"the plan refused to run with a one-line message,\" for the exact same underlying mistake. Put constraints you already know at input time (allowed instance families, CIDR shape, name length limits) into validation blocks rather than discovering them from a provider error. Pin the module's own required versions too, so a caller can't apply it against an incompatible Terraform or provider release without being told explicitly: # versions.tf terraform { required_version = \">= 1.7.0\" required_providers { aws = { source = \"hashicorp/aws\" version = \"~> 5.0\" } } } Shaping data at the boundary between two modules A module's output doesn't have to hand back exactly what it holds internally — narrowing it to just what the next module needs is itself part of interface design. day02-modules/main.tf does exactly this when wiring network's output into compute's input: module \"compute\" { source = \"./modules/compute\" subnet_ids = { for az, subnet in module.network.public_subnets : az => subnet.id } # ... } module.network.public_subnets is a map of whole subnet objects (id, cidr_block, arn, tags — everything). compute's subnet_ids variable only wants plain ID strings. The for-expression bridges that gap: module.network.public_subnets the for-expression's result { { \"1a\" = { id = \"subnet-0aaa...\", \"1a\" = \"subnet-0aaa...\" cidr_block = \"11.0.1.0/24\", --> \"1b\" = \"subnet-0bbb...\" arn = \"...\", tags = {...} } } \"1b\" = { id = \"subnet-0bbb...\", ... } } Same keys in, same keys outfor az, subnet in ... : az => subnet.id keeps every key exactly as it was (\"1a\", \"1b\") and replaces only the value — the whole subnet object collapses down to just its .id. This is the same map-to-map for-expression as 15.6's collection output, just used at a module boundary instead of in an output block — narrowing a rich internal object down to the one field a caller actually needs, rather than making every downstream module reach into fields it has no business touching."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-5","title":"Versioning & Sourcing Modules","body":"Where a module's code lives, and how a caller pins to a specific version of it, has direct consequences for whether a plan run next month reproduces the one you ran today. SourceSyntaxReproducible? Local pathsource = \"../modules/network\"Yes, but only within one repo — fine for this ramp plan's single-repo layout Git, floating branchsource = \"git::https://.../network.git?ref=main\"No — main keeps moving, the same config can resolve to different code tomorrow Git, pinned tagsource = \"git::https://.../network.git?ref=v1.4.0\"Yes — a tag is (by convention) immutable Terraform Registrysource = \"app.terraform.io/org/network/aws\", version = \"~> 1.4\"Yes, with the added benefit of registry-enforced semver module \"network\" { source = \"git::https://github.com/example-org/tf-modules.git//network?ref=v1.4.0\" name = \"app\" vpc_cidr = \"10.0.0.0/16\" } module \"ecs_service\" { source = \"app.terraform.io/example-org/ecs-service/aws\" version = \"~> 2.1\" name = \"api\" cluster_arn = module.ecs_cluster.arn } A floating ref isn't a version?ref=main means \"whatever the module author most recently pushed.\" Two engineers running terraform plan an hour apart, on the exact same calling code, can get different plans if main moved in between — and there's no diff in your own repo to explain why. Always pin to a tag or a full commit SHA, never a branch name. Semver on the module itselfA published module should bump its major version on any breaking interface change (a variable removed, a required input added, an output renamed) — exactly like a library. Callers then use ~> constraints to accept patch/minor updates automatically while breaking changes require a deliberate version bump in the caller's own code."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-6","title":"Composition vs Over-Abstraction, and Repo Layout","body":"A module is worth the indirection when it's used more than once, or when its interface has genuinely stabilized. Before either is true, a module is just a resource block wearing a costume — one more file to open to see what actually gets created. Rule of threeWrite the resource inline the first time. Copy it the second time. Only extract a module the third time a near-identical block shows up — by then the actual variable inputs (what really differs between the three copies) are obvious, instead of guessed at up front. Over-abstraction looks like cautionA module with 40 input variables \"for flexibility,\" most of which every caller sets to the same default, isn't reusable — it's one specific configuration with extra steps. It hides the actual resource shape from anyone reading the calling code, and every new requirement means threading one more variable through the whole interface. If every caller passes the same value for a variable, that value belongs inside the module, not in its interface. A repository layout that survives a year of change . ├── modules/ │ ├── network/ # VPC, subnets, route tables, NAT │ ├── security-groups/ │ ├── compute-asg/ # launch template + ASG + target group attachment │ └── ecs-service/ # task def + service, one block per microservice ├── environments/ │ ├── dev/ │ │ ├── main.tf # calls the modules above with dev's variable values │ │ ├── backend.tf # dev's remote state config │ │ └── terraform.tfvars │ └── stg/ │ ├── main.tf # same module calls, staging's variable values │ ├── backend.tf │ └── terraform.tfvars └── .github/workflows/ # or Jenkinsfile -- fmt, validate, plan-on-PR The two environment directories call the same modules with different variable values and a different backend key — that's what makes \"dev and stg differ only by variable values\" (this week's deliverable) actually true, rather than aspirational. Nothing about a module's internals should need to know which environment is calling it."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-7","title":"Provisioners — file, connection, and Why They're a Last Resort","body":"Everything so far declares desired state — Terraform figures out the API calls. A provisioner is the one place that breaks that model: it runs an imperative action (upload a file, run a script) against a resource right after it's created, over SSH or WinRM. It needs a connection block to say how to actually log in — which needs a real key pair to exist first. Generating a key pair to a custom path ssh-keygen prompts for a save location; pointing it somewhere other than the default ~/.ssh/id_rsa keeps a project-specific key out of your global SSH config: ssh-keygen -t rsa -b 2048 # Enter file in which to save the key (~/.ssh/id_rsa): ./deployer_key # Enter passphrase (empty for no passphrase): [leave blank for a provisioner -- see note below] This writes two files: deployer_key (the private key — never committed, never shared) and deployer_key.pub (the public key — safe to share, this is what AWS actually stores). Terraform reads the public half into aws_key_pair, and the private half into the connection block below to actually authenticate: resource \"aws_key_pair\" \"deployer\" { key_name = \"deployer-key\" public_key = file(\"./deployer_key.pub\") } A hand-typed key is one truncation away from a real errorA public key pasted or retyped as an inline string is easy to accidentally clip — a real apply once failed with InvalidKey.Format: Key is not in valid OpenSSH public key format because the leading AAAAB3NzaC1yc2E... chunk (a fixed, required part of every RSA key's encoding) got cut off during a copy-paste. Reading the key with file(\"./deployer_key.pub\"), as above, removes the chance of that entirely — the exact bytes ssh-keygen wrote are what gets sent, nothing retyped in between. key_name = \"a-string\" isn't a reference — it's a coincidencekey_name = \"deployer-key\" on the instance and key_name = \"deployer-key\" on the aws_key_pair happening to match is invisible to Terraform's dependency graph (15.3) — a bare string creates no edge between the two resources. A real apply creating both at once launched the EC2 instance and the key pair in parallel, and the instance's RunInstances call reached AWS before the key pair existed, failing with InvalidKeyPair.NotFound even though the key pair resource itself succeeded moments later in the same apply. The fix is the reference already used above — key_name = aws_key_pair.deployer.key_name — which both names the actual key and forces the correct creation order, the same mechanism 15.3 covers for any two resources that need to happen in sequence. An empty passphrase is the practical choice for a provisionerAn SSH key with a passphrase demands it interactively on every connection — but the file provisioner authenticates non-interactively, with no way to type a passphrase in when Terraform asks. A passphrase-protected key here just fails auth silently, the same \"looks like a hang, not an error\" symptom as the wrong-username case below. For a key that automation (not a human at a terminal) is going to use, leave the passphrase blank. resource \"aws_instance\" \"app\" { ami = var.ami_id instance_type = \"t3.micro\" key_name = aws_key_pair.deployer.key_name vpc_security_group_ids = [aws_security_group.main.id] provisioner \"file\" { source = \"./provisioned_file.txt\" destination = \"/home/ec2-user/provisioned_file.txt\" } connection { type = \"ssh\" host = self.public_ip user = \"ec2-user\" private_key = file(\"./deployer_key\") timeout = \"4m\" } } Wrong SSH user looks like a hang, not an errorA real apply using user = \"ubuntu\" against an Amazon Linux 2023 AMI sat at Still creating... for over 4 minutes with no error message at all — because Amazon Linux's login user is ec2-user, not ubuntu (the same AMI-dependent-username gotcha as week2.html's SSH troubleshooting, showing up again here). Every failed SSH attempt is silently retried until the connection block's own timeout is exhausted, so a wrong username doesn't fail fast — it fails slow, looking exactly like a stuck instance. Match the user to the AMI family: ec2-user for Amazon Linux, ubuntu for Ubuntu, admin for Debian. The destination path needs to be writable by that userFixing the username above can surface a second, different failure: scp: /home/provisioned_file.txt: Permission denied. /home/ itself is owned by root — no login user can write directly into it, only into their own subdirectory one level down (/home/ec2-user/). /tmp is the simpler default destination when the exact home directory isn't worth hardcoding, since it's world-writable regardless of which user connects. local-exec — runs on your machine, not the resource Easy to misread at first: local-exec runs its command on whatever machine is running terraform apply — never on the resource itself, and it needs no connection block at all, because it never connects to anything. resource \"aws_instance\" \"app\" { # ... provisioner \"local-exec\" { command = \"touch hello.txt\" } } \"touch hello.txt\" doesn't touch the instanceA real run of the block above created hello.txt sitting in the same directory terraform apply was run from — not on the EC2 instance, not anywhere in AWS. local-exec is for side effects on the machine running Terraform itself: writing an output value to a local file, triggering a local notification, kicking off a script that talks to some other system entirely. To actually run a command on the resource, the provisioner needed is remote-exec, next. remote-exec — runs commands on the resource, over the same connection Same connection block as file (SSH, same host/user/key) — but instead of uploading a file, it runs shell commands directly on the remote resource: resource \"aws_instance\" \"app\" { # ... provisioner \"remote-exec\" { inline = [ \"sudo yum install -y httpd\", \"sudo systemctl enable --now httpd\" ] } connection { type = \"ssh\" host = self.public_ip user = \"ec2-user\" private_key = file(\"./deployer_key\") } } inline vs script vs scriptsinline (above) is a list of commands run in order, each over its own SSH exec call — fine for two or three lines. script uploads and runs one local script file; scripts does the same for a list of them. For anything longer than a handful of commands, a script file is easier to read, test locally, and version — the same reasoning that already favors a userdata.sh file (5.2 of week2.html) over an inline heredoc. Prefer user_data over a provisioner whenever the choice existsuser_data (15.2, 18.8.1 section 9) runs as part of the instance's own boot process — no SSH session, no network round-trip from wherever apply happens to run, no connection timeout to tune. A provisioner needs Terraform itself to hold an SSH connection open during apply, which means it can fail for reasons that have nothing to do with the resource being wrong — a security group change, a flaky network, a key mismatch. HashiCorp's own guidance is to treat provisioners as a last resort, for the narrow cases user_data genuinely can't cover (pulling a file that doesn't exist until another resource in the same apply finishes, for instance) — not as the default way to configure a new instance. SSHing in yourself afterward — the same key, a manual connection Everything above wires a key pair into an instance so Terraform's own provisioner can log in. Logging in yourself afterward, from a terminal, reuses the exact same key pair — no separate setup: chmod 400 ./deployer_key # SSH refuses a private key that's readable by anyone else ssh -i \"./deployer_key\" ec2-user@<instance-public-dns-or-ip> The ingress CIDR means \"from,\" never \"to\"A real security group ended up with its port-22 rule allowing the EC2 instance's own public IP — which looked plausible, since that's literally the address baked into the instance's own hostname (ec2-203-0-113-10...). But an ingress cidr_blocks always means \"traffic is allowed to arrive from this address\" — never \"this is the address being connected to.\" The instance's own IP is irrelevant to its own ingress rules; what belongs there is the client's IP — the machine running ssh, found with curl ifconfig.me, not the server being connected to. Getting this backwards produces the same silent, response-less hang as any other SG mismatch — nothing distinguishes \"wrong direction\" from \"wrong IP entirely\" at the network level. A hardcoded personal IP goes staleLocking SSH to \"just my IP\" via a /32 CIDR is good practice over 0.0.0.0/0, but most home and mobile ISPs assign dynamic addresses — the correct CIDR today can silently stop working after a router reboot or a network change, with the exact same symptom as every other SG mismatch: ssh just hangs, no error to point at the real cause. Re-running curl ifconfig.me and comparing it against the security group's current rule is the fastest way to rule this in or out before suspecting the key, the instance, or anything else."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-8","title":"Templating: templatefile() and .tftpl Files","body":"18.9 built a Lambda IAM policy with jsonencode({...}) — real HCL values, mechanically serialized into JSON. templatefile() is the other approach: write the JSON (or a script, or any text) as its own file, with ${...} placeholders, and have Terraform render it with real values substituted in. # main.tf resource \"aws_iam_user_policy\" \"instance_manager\" { name = \"InstanceManagePolicy\" user = aws_iam_user.created_user.name policy = templatefile(\"${path.module}/user-policy.tftpl\", { ec2_policies = [ \"ec2:RunInstances\", \"ec2:StopInstances\", \"ec2:TerminateInstances\", \"ec2:RequestSpotInstances\", ] }) } # user-policy.tftpl { \"Version\": \"2012-10-17\", \"Statement\": [ { \"Effect\": \"Allow\", \"Action\": ${jsonencode(ec2_policies)}, \"Resource\": \"*\" } ] } templatefile(path, vars) takes two arguments: the file to render, and a map of variables the template can reference. Inside the .tftpl file, ${ec2_policies} would insert a raw HCL representation of the list — wrapping it in jsonencode(...), as above, is what turns it into valid JSON array syntax instead. Why reach for a separate file at all Nothing here is impossible with jsonencode() alone — the difference is where the content lives. A large JSON policy, a multi-line bootstrap script, or an nginx config reads far more naturally as its own properly-formatted file (real syntax highlighting, no HCL string-escaping) than as a nested HCL data structure. templatefile() also supports its own mini control-flow syntax inside the template file itself — a for directive and an if directive, the template-text equivalents of a for expression (15.6) and a conditional. %{ for } — repeating a line once per item # allowed_ips.tftpl %{ for ip in allowed_ips ~} allow ${ip}; %{ endfor ~} deny all; templatefile(\"${path.module}/allowed_ips.tftpl\", { allowed_ips = [\"203.0.113.10\", \"203.0.113.20\"] }) # rendered output allow 203.0.113.10; allow 203.0.113.20; deny all; The ~ is a whitespace-strip marker, not part of the loopWithout ~, every %{ for ... } and %{ endfor } line still emits its own trailing newline into the output — a template written across multiple lines would render with extra blank lines where the directive lines themselves used to be. %{ for ip in allowed_ips ~} strips the newline immediately after the directive; %{ endfor ~} does the same before it. The result is clean output with no artifacts from the directive syntax itself — always worth adding on multi-line for/if directives unless you've checked the output without it and it already looks right. A key-value version works the same way as a two-variable for expression (16.1's each.key/each.value pattern, here as template text instead): %{ for name, port in services ~} ${name}: ${port} %{ endfor ~} %{ if } / %{ else } — conditional content # userdata.tftpl #!/bin/bash yum install -y httpd %{ if enable_monitoring ~} yum install -y amazon-cloudwatch-agent systemctl enable --now amazon-cloudwatch-agent %{ else ~} echo \"monitoring agent skipped\" %{ endif ~} systemctl start httpd user_data = templatefile(\"${path.module}/userdata.tftpl\", { enable_monitoring = var.enable_monitoring # a plain bool }) %{ else } is optional — a bare %{ if ... }/%{ endif } with nothing in between the false branch and endif simply renders nothing when the condition is false, the same as an if with no else in most languages. Indenting a directive line leaks whitespace into the output Indenting %{ if }/%{ for } to match the surrounding code is natural — but ~ only strips whitespace on the side of the directive it's placed on. A trailing-only ~} does nothing about the spaces typed before %{ on that same line: #!/bin/bash %{ if enable ~} yum install -y agent %{ endif ~} systemctl start httpd # actual rendered output -- verified directly #!/bin/bash yum install -y agent systemctl start httpd The indentation doesn't just appear, it accumulatesyum install -y agent rendered with 8 leading spaces, not 4 — its own indentation plus the 4 unstripped spaces sitting before %{ if enable ~} on the line above, since only that directive's trailing newline was stripped, not its leading whitespace. systemctl start httpd — which had no indentation in the source at all — picked up 4 leading spaces leaked from the %{ endif ~} line the same way. The whitespace doesn't just leak, it silently shifts onto lines that were never indented in the source. The fix is ~ on both sides of a directive-only line — %{~ if enable ~} — which strips the leading whitespace/newline too: #!/bin/bash %{~ if enable ~} yum install -y agent %{~ endif ~} systemctl start httpd # rendered output -- clean #!/bin/bash yum install -y agent systemctl start httpd Default to both-sided ~ on directive-only linesA line that contains nothing but a %{ if }/%{ for }/%{ endif }/%{ endfor } directive is never meant to produce output itself — it exists purely for control flow. %{~ ... ~} on every such line is the safe default; reach for one-sided ~ only when a directive shares a line with real content you specifically want to keep. %{ for }/%{ if } aren't a templatefile()-only feature — they're part of HCL's general template syntax, usable inside any string expression, including a heredoc written directly in a resource block, with no separate file at all: user_data = <<-EOF #!/bin/bash %{ if enable_monitoring ~} yum install -y amazon-cloudwatch-agent %{ endif ~} EOF templatefile() only changes where the text livesThe directive syntax above is identical whether it's inline in a heredoc or inside a file read by templatefile() — the only thing templatefile() adds is reading that text from a separate file instead of writing it directly in the resource block. jsonencode({...})templatefile(\"...tftpl\", {...}) Where the content livesInline, inside the .tf file, as real HCL valuesA separate file, with ${...} placeholders Syntax correctnessGuaranteed valid JSON — it's built mechanically, never hand-typedOnly as correct as whatever's typed in the template file — a missing comma is a real, hand-made mistake Good forPolicies and structures that are naturally HCL-shaped already (lists, maps you're already building)Long or reused text — multi-line scripts, large policies, anything an editor should syntax-highlight properly Failure modeA typo is a normal HCL error, caught early and clearlyA typo can be a raw JSON parse error with a byte offset (15.5-style plan-time failure, but far less readable) This is the exact error a missing comma producesA real plan against the template above (before its comma was added) failed with \"policy\" contains an invalid JSON policy: invalid character '\"' after object key:value pair, at byte offset 35. That's what templatefile()'s tradeoff looks like in practice: the file is plain text as far as Terraform's HCL parser is concerned, so a hand-typed JSON mistake surfaces as a raw JSON parser complaint, not a clean, typed HCL error the way a mistake inside jsonencode({...}) would. Neither approach checks that the action names are realFixing the JSON syntax doesn't mean the policy is correct — \"ec2:RequestSpotInstance\" (singular) parsed as perfectly valid JSON and passed templatefile() without complaint, but AWS itself rejected it: the real action is RequestSpotInstances (plural). Neither jsonencode() nor templatefile() validates that an IAM action actually exists — that check only happens once AWS evaluates the policy, at apply time, regardless of which method built the JSON."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-9","title":"null_resource — Provisioners With No Real Infrastructure Attached","body":"Every provisioner (16.7) has to live inside some resource block — but sometimes the action you want (run a script, hit a webhook, invalidate a cache) doesn't correspond to creating any real cloud resource at all. null_resource is a resource that creates nothing in AWS — it exists purely to give a provisioner (or a dependency edge) somewhere to attach. resource \"aws_instance\" \"app_server\" { ami = \"ami-08188a5a4dfdbd573\" instance_type = \"t3.micro\" tags = { Name = \"app-server\" } } resource \"null_resource\" \"notify_on_change\" { triggers = { instance_id = aws_instance.app_server.id } provisioner \"local-exec\" { command = \"echo Hello World\" } } Why triggers existsA null_resource has no real attributes of its own, so on a normal apply nothing about it ever looks \"changed\" — Terraform would never know to re-run its provisioner again. triggers is the workaround: any change to a value inside that map (here, aws_instance.app_server.id, which changes whenever that instance is replaced) marks the null_resource itself as needing replacement, which re-runs its provisioner. Without a matching entry in triggers, the provisioner would only ever run once, on the very first apply, no matter what changed afterward. When to reach for it Running a script that depends on more than one resource finishing, with no single natural resource to attach the provisioner to Invalidating a CDN cache (CloudFront) right after a new S3 deployment Running a one-off database migration or seed script once the app server and database both exist Sending a deploy notification (Slack, a webhook) after other resources finish applying Re-running a local script whenever an unrelated value changes — e.g. triggers = { script_hash = filemd5(\"deploy.sh\") }, re-running only when that specific file's contents change Still bound by 16.7's caution about provisioners generallynull_resource doesn't make provisioners any less of a last resort — it just removes the \"which resource do I attach this to\" obstacle. Everything from 16.7 still applies: it needs Terraform to hold a connection/process open during apply, and it can fail for reasons that have nothing to do with real infrastructure being wrong. terraform_data is the newer, provider-free equivalentTerraform 1.4+ ships terraform_data as a built-in resource (no separate null provider required) covering the same role — a resource with no real infrastructure, driven by a triggers_replace argument instead of triggers. null_resource still works and remains extremely common in existing code, but new code on a recent Terraform version has one fewer provider to declare by reaching for terraform_data instead."},{"page":"week4.html","pageLabel":"Week 4","id":"d16-10","title":"depends_on — Use Cases, Pros and Cons","body":"16.3 introduced depends_on for the case a reference can't express — an IAM policy attachment finishing before something that needs it starts. That's the only reason it exists at all: everywhere Terraform can see a dependency (one resource's argument reading another resource's attribute, 15.3), it already orders things correctly on its own. depends_on only matters for the remaining cases where the real-world ordering requirement leaves no trace in any argument. Real use cases A provisioner or user_data script that needs another resource to exist first, but only refers to it by a hardcoded name or convention — never a real attribute reference (an S3 bucket a startup script downloads from, named as a plain string rather than aws_s3_bucket.x.id) IAM permissions that must be attached before a dependent resource starts using them (16.3's example) An AWS-side ordering requirement that isn't visible through any argument at all — something that only shows up as a real API error when created in the wrong order Forcing a data source to read after a resource is created, when the data source's own arguments don't reference that resource directly Ordering an entire module block relative to another resource or module — depends_on works on modules too, waiting for every resource inside the whole module to finish A real file with both a correct use and a redundant one resource \"aws_instance\" \"app\" { # ... vpc_security_group_ids = [aws_security_group.main.id] key_name = aws_key_pair.deployer.key_name depends_on = [ aws_s3_bucket.bucket, # correct -- nothing above references this bucket at all aws_security_group.main, # redundant -- already inferred from vpc_security_group_ids aws_key_pair.deployer, # redundant -- already inferred from key_name ] } Spot the redundant entriesOnly aws_s3_bucket.bucket belongs in this depends_on — nothing in the resource block references any attribute of that bucket, so Terraform has no other way to know it should exist first. aws_security_group.main and aws_key_pair.deployer are already ordered correctly by vpc_security_group_ids and key_name — listing them again in depends_on changes nothing about how apply behaves, it's just dead weight that makes a reader wonder if there's a hidden reason for the dependency beyond what's already visible in the arguments above it. Pros Makes a real ordering requirement explicit and visible in code, for the cases where no argument reference could express it One depends_on can list several resources at once for a single combined ordering constraint Works on data sources and whole module blocks, not just resources Cons Operates on the whole resource, not a specific attribute — Terraform waits for everything about the other resource to finish, even if only one small part of it actually matters, which can serialize applies more than necessary Explains that two resources are related, never why — a reference shows the actual attribute being used; a bare depends_on entry needs a comment to mean anything to the next reader Easy to add \"just in case\" without confirming it's actually needed — as the redundant entries above show, once one is added nothing forces anyone to remove it later, even after it stops meaning anything Silently masks a design that could be improved — the \"reach for it last\" principle from 16.3 exists because restructuring toward a real reference is often possible and stays correct automatically through future refactors, where a depends_on entry has to be remembered and kept in sync by hand"},{"page":"week4.html","pageLabel":"Week 4","id":"d17-1","title":"What State Holds","body":"Terraform can't ask AWS \"what does my config manage?\" — AWS just has resources, with no concept of which ones \"belong\" to a given .tf file. The state file is Terraform's own record of that mapping: for every resource block, it stores the real resource ID and every attribute AWS returned when it was created. { \"resources\": [ { \"type\": \"aws_instance\", \"name\": \"app\", \"instances\": [ { \"attributes\": { \"id\": \"i-0aaa1111aaaa11111\", \"ami\": \"ami-0111111111111111\", \"instance_type\": \"t3.micro\", \"public_ip\": \"203.0.113.10\" } } ] } ] } Every plan is really a three-way comparison: your .tf files (desired), the state file (what Terraform believes it last created), and a fresh read of the real AWS API (what actually exists). A mismatch between the last two is drift — someone changed the security group by hand in the console, and state doesn't know yet. Never hand-edit the state fileIt's JSON, so it's tempting to fix a stuck reference by hand. Don't — a malformed edit corrupts every future plan silently. Use terraform state subcommands (Day 19) for anything that looks like a state surgery. State contains secrets in plaintextAn RDS password passed as a resource argument ends up readable in plaintext inside the state file, even if the variable itself is marked sensitive (that only hides it from CLI output, not from the file). This is the whole reason state must live in a backend with access control, never committed to git."},{"page":"week4.html","pageLabel":"Week 4","id":"d17-2","title":"Remote Backend with Locking","body":"By default, state lives in a local terraform.tfstate file next to your config — fine solo, broken the moment a second engineer runs apply from their own laptop against their own copy of that file. A remote backend moves state to shared storage; locking stops two applies from racing against it at the same time. # backend.tf terraform { backend \"s3\" { bucket = \"fahad-terraform-state\" key = \"dev/network/terraform.tfstate\" region = \"ap-south-1\" dynamodb_table = \"terraform-locks\" # locking, pre-2023 syntax encrypt = true } } The backend block doesn't inherit the provider's profileA real terraform init -migrate-state failed with 403 Forbidden on the S3 bucket, even though the exact same bucket worked fine for every other command. Cause: the backend \"s3\" { } block above has no profile argument, so it never inherits profile = \"fahad\" from the provider \"aws\" { } block below it — backend initialization happens before providers are even loaded, so nothing carries over automatically. With no explicit profile, it silently fell back to this machine's default AWS credential chain, which resolved to a completely different AWS account that had no access to the bucket at all. The fix is adding profile = \"fahad\" directly inside the backend block — it needs its own credentials, stated explicitly, every time. Local stateS3 remote state CollaborationBreaks the moment a second person runs apply from their own copyOne shared source of truth for the whole team LockingNone — two concurrent applies can corrupt itDynamoDB or S3-native locking (above) prevents the race DurabilityOne laptop's disk — no backup, one rm away from goneS3's own durability, plus optional bucket versioning for point-in-time recovery Setup complexityZero — works with no configurationNeeds a bucket, IAM permissions, and its own credential config (see the fact box above) Credential scopeSame identity as everything else automaticallyResolved independently of the provider block — easy to misconfigure, as above CostFreeSmall S3 storage/request cost, plus DynamoDB cost only if not using native locking S3 native locking (Terraform ≥ 1.10 / AWS provider ≥ 5.x)Newer Terraform versions can lock directly on the S3 object using conditional writes — no separate DynamoDB table required. Either approach solves the same problem: without locking, two apply runs starting seconds apart both read the same \"before\" state and can each write back a \"after\" that discards the other's changes. Migrating dynamodb_table to use_lockfile A real apply on Terraform 1.16 produced a live deprecation warning: │ Warning: Deprecated Parameter │ │ The parameter \"dynamodb_table\" is deprecated. │ Use parameter \"use_lockfile\" instead. The fix is a one-line swap in the backend block: backend \"s3\" { bucket = \"fmk-terraform-backup\" key = \"tf-state/locking/terraform.tfstate\" region = \"ap-south-1\" use_lockfile = true # was: dynamodb_table = \"dynamodb-state-locking\" profile = \"fahad\" } terraform init -reconfigure # not -migrate-state -- the bucket/key didn't move, # only the locking mechanism did -reconfigure vs -migrate-stateChanging any backend argument makes Terraform refuse to proceed until you tell it what to do with existing state — but which flag depends on what changed. -migrate-state is for when the state's actual location changed (a different bucket or key) and the old state needs copying into the new location. -reconfigure is for when the location is identical and only settings like this one changed — it just re-applies the new config against the state that's already sitting right there, no copying needed. Using -migrate-state when nothing moved, or vice versa, is a common way to get a confusing error instead of the migration you meant. The old DynamoDB table isn't automatically cleaned upSwitching to use_lockfile just stops this config from using the table — it doesn't delete it, and doesn't check whether anything else still depends on it. Safe to delete only after confirming no other project's backend still points at the same dynamodb_table. # What locking looks like when it's doing its job $ terraform apply Acquiring state lock. This may take a few moments... Error: Error acquiring the state lock Lock Info: ID: 7c2f9e1a-... Path: dev/network/terraform.tfstate Operation: OperationTypeApply Who: teammate@laptop This is what makes CI safeRemote state with locking is the prerequisite for the whole Week 5 pipeline — a Jenkins job applying on merge and an engineer running plan locally have to share one source of truth, or the pipeline's idea of \"current state\" silently diverges from reality. Reading and overwriting remote state directly: pull and push # Read-only -- download the current remote state, print it to stdout terraform state pull > state-snapshot.json # Inspect it without needing raw S3/console access terraform state pull | jq '.resources[].name' # Dangerous -- unconditionally overwrite the remote state with a local file terraform state push state-snapshot.json state pullstate push DirectionRemote → local (a copy)Local → remote (overwrites it) Touches real state?No — read-only, always safe to runYes — replaces whatever the remote currently holds Good forInspecting or scripting against state (piping into jq) without console/S3 accessRecovering from a known-good backup, or a manual fix nothing else can do The catchA point-in-time snapshot — stale the moment anyone else applies after you pulled itNo diff, no plan, no confirmation prompt shown first — a stale or wrong local file silently erases what Terraform knows about real infrastructure push is a last resort, not a routine commandstate push normally refuses to run if the remote state has moved on since your local copy was pulled (compared by an internal serial number) — that check exists specifically to stop exactly the accident this command makes possible. Overriding it with -force removes that last safety check entirely. Reach for terraform state mv/rm (19.4) for routine state surgery; push is for genuine disaster recovery, not day-to-day use."},{"page":"week4.html","pageLabel":"Week 4","id":"d17-3","title":"Environment Isolation — Directories vs Workspaces","body":"Two ways to run the same code against dev and stg without duplicating the resource definitions themselves. Separate directoriesTerraform workspaces Structureenvironments/dev/, environments/stg/, each with their own backend.tf and .tfvarsOne directory, terraform workspace new stg creates a separate state within the same backend State isolationFully separate state files, separate backend keys — a mistake in one physically cannot touch the otherSeparate state, same backend config — one bad backend change affects every workspace Blast radius of a bugA typo in environments/dev/main.tf can't reach staging at allA bug in the single shared main.tf reaches every workspace next apply Good fitEnvironments that should be able to diverge (different instance sizes, different modules even) and where mistakes must stay containedTruly identical environments, or short-lived ones (a workspace per PR preview) # Workspaces, for comparison terraform workspace new stg terraform workspace select stg terraform apply -var-file=stg.tfvars terraform workspace show # confirm which one you're about to apply into The full terraform workspace command set terraform workspace new <name> Creates a new workspace with its own empty state, and switches to it immediately. Every project starts with one workspace already, called default — it's there even if you never run this command. terraform workspace select <name> Switches to an existing workspace. This is the step it's easy to forget before an apply — there's no prompt or warning, it just silently applies into whichever workspace was already selected. terraform workspace list Lists every workspace that exists for this configuration, with an asterisk marking the one currently active. terraform workspace show Prints just the name of the currently active workspace — the fast way to confirm what apply is actually about to touch, worth running as a habit right before it. terraform workspace delete <name> Removes a workspace and its state file. Terraform refuses if that workspace's state still has resources in it — run destroy inside that workspace first, or the delete fails rather than silently orphaning real infrastructure. The workspace footgunNothing in the terminal loudly reminds you which workspace is selected — running terraform apply after forgetting a workspace select applies into whichever one you left active. This repo uses separate directories (see the layout in 16.6) specifically to make that mistake structurally impossible: the environment is which directory you cdinto, not invisible session state. Pros and cons, specifically for separating credentials One question workspaces can't answer: do they separate which AWS account or credentials an environment uses? No — the provider \"aws\" { ... } block is one static config for the entire run, untouched by which workspace is active. Pro: fast to spin up — terraform workspace new is one command, no new directory or backend config to write. Pro: state stays genuinely isolated per workspace, even though the config is shared. Pro: a good fit for short-lived, truly identical environments — a workspace per PR preview, disposable by design. Con: no credential or account separation at all — every workspace shares the exact same provider block, so it can't be the mechanism that keeps dev and prod on separate AWS accounts. Con: nothing in the terminal shows which workspace is active — a forgotten workspace select applies into whichever one was last left selected. Con: one shared main.tf means one bug reaches every workspace on the next apply — no structural containment the way separate directories give you."},{"page":"week4.html","pageLabel":"Week 4","id":"d17-4","title":"Remote State Data Sources Between Layers","body":"Splitting a platform into layers (network, then compute, then application) means the compute layer needs values — a VPC ID, a subnet list — that only the network layer's state actually holds. terraform_remote_state reads another layer's state as a data source, read-only. # in the compute layer data \"terraform_remote_state\" \"network\" { backend = \"s3\" config = { bucket = \"fahad-terraform-state\" key = \"dev/network/terraform.tfstate\" region = \"ap-south-1\" } } resource \"aws_instance\" \"app\" { subnet_id = data.terraform_remote_state.network.outputs.private_subnet_id # ... } Only outputs cross the boundaryA layer can only read another layer's declared output values, not its internal resources — which is exactly the module-interface discipline from 16.4, applied between whole layers instead of between a module and its caller. If compute needs a value the network layer hasn't output yet, the fix is adding that output, not reaching around it. A layer boundary is also a blast-radius boundarySplitting into layers means a network change requires its own plan/apply, separate from the compute layer's — you can no longer apply the whole platform in one command. That's the tradeoff: smaller, safer applies, at the cost of the multi-layer apply sequencing this buys you."},{"page":"week4.html","pageLabel":"Week 4","id":"d17-5","title":"Provider Aliases & a Second Region as a Variable","body":"A provider block configures one region (or account). alias lets a single config talk to a second one — the mechanism this week's deliverable (\"the same code applied to a second region by changing variables only\") actually rests on. # providers.tf provider \"aws\" { region = var.primary_region # e.g. \"ap-south-1\" } provider \"aws\" { alias = \"secondary\" region = var.secondary_region # e.g. \"eu-west-1\" } # usage -- everything else about the resource is identical resource \"aws_s3_bucket\" \"backup\" { provider = aws.secondary bucket = \"${var.name_prefix}-backup-${var.secondary_region}\" } For a full second-region deployment (not just one bucket), the cleaner pattern is calling the same module twice, once per provider: module \"platform_primary\" { source = \"../modules/platform\" providers = { aws = aws } region = var.primary_region } module \"platform_secondary\" { source = \"../modules/platform\" providers = { aws = aws.secondary } region = var.secondary_region } Why this satisfies the deliverableNothing inside modules/platform hardcodes a region anywhere — every AZ, AMI lookup, and CIDR is already parameterised (this is what \"the whole platform in code\" in Day 18 actually depends on). Standing up eu-west-1 becomes calling the module a second time with a different provider alias, not writing new resource blocks."},{"page":"week4.html","pageLabel":"Week 4","id":"d17-6","title":"fmt, validate, a Linter and a Scanner — From the First Commit","body":"Four checks, in order of how cheap they are to run, all before a plan ever touches real AWS: # 1. Formatting -- purely cosmetic, zero false positives terraform fmt -recursive -check # 2. Syntax and internal consistency -- catches typos, type mismatches, missing required args terraform validate # 3. Linting -- style and correctness rules validate can't see tflint # 4. Security scanning -- known-bad patterns (open SG, unencrypted volume, public S3) tfsec . # or: checkov -d . terraform fmt Rewrites files to canonical indentation and alignment. -check makes it exit non-zero instead of rewriting — the form to run in CI, so a PR fails instead of silently reformatting someone's branch. terraform validate Checks the config is internally consistent — references resolve, types match, required arguments are present. It does not check against real AWS state; a config can validate cleanly and still fail at apply because of an actual AWS-side constraint. tflint Catches things that are syntactically valid but wrong — an invalid instance type for the AWS provider, an unused variable, a deprecated argument. tfsec / checkov Static security scanners: flag a security group open to 0.0.0.0/0, an unencrypted EBS volume, a public S3 bucket, before any of it is ever created. This is what \"posted as a comment\" means in Week 5These four checks are exactly the pull-request gate this week's Assignment A4 and next week's Jenkins pipeline both describe — running them locally now is rehearsal for wiring the identical commands into CI, not a separate step."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-1","title":"ALB — Listeners and Rules in Code","body":"The four-resource shape from week2.html 6.1 (load balancer, target group, listener, listener rule) as a module, driven by a map so adding a second path-routed service is a new map entry, not a new resource block. resource \"aws_lb\" \"app\" { name = \"${var.name_prefix}-alb\" load_balancer_type = \"application\" subnets = var.public_subnet_ids security_groups = [aws_security_group.alb.id] } resource \"aws_lb_target_group\" \"service\" { for_each = var.services # map(object({ port = number, health_check_path = string })) name = \"${var.name_prefix}-${each.key}-tg\" port = each.value.port protocol = \"HTTP\" vpc_id = var.vpc_id health_check { path = each.value.health_check_path } } resource \"aws_lb_listener\" \"https\" { load_balancer_arn = aws_lb.app.arn port = 443 protocol = \"HTTPS\" certificate_arn = var.acm_certificate_arn default_action { type = \"forward\" target_group_arn = aws_lb_target_group.service[var.default_service].arn } } resource \"aws_lb_listener_rule\" \"path\" { for_each = { for k, v in var.services : k => v if k != var.default_service } listener_arn = aws_lb_listener.https.arn priority = index(keys(var.services), each.key) + 1 action { type = \"forward\" target_group_arn = aws_lb_target_group.service[each.key].arn } condition { path_pattern { values = [\"/${each.key}\", \"/${each.key}/*\"] } } } The 404 from week2.html, encoded as a fixed patternThe path_pattern values include both the exact path and the wildcard (/bar and /bar/*) — this is the fix from the live /bar 404 investigation (week2.html 6.1), written once into the module instead of remembered per rule."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-2","title":"Auto Scaling Group in Code","body":"resource \"aws_launch_template\" \"app\" { name_prefix = \"${var.name_prefix}-\" image_id = var.golden_ami_id instance_type = var.instance_type vpc_security_group_ids = [aws_security_group.app.id] lifecycle { create_before_destroy = true } } resource \"aws_autoscaling_group\" \"app\" { name = \"${var.name_prefix}-asg\" vpc_zone_identifier = var.private_subnet_ids min_size = var.min_size max_size = var.max_size desired_capacity = var.desired_capacity target_group_arns = [aws_lb_target_group.service[var.default_service].arn] launch_template { id = aws_launch_template.app.id version = \"$Latest\" } } resource \"aws_autoscaling_policy\" \"cpu_target\" { name = \"${var.name_prefix}-cpu-target\" autoscaling_group_name = aws_autoscaling_group.app.name policy_type = \"TargetTrackingScaling\" target_tracking_configuration { predefined_metric_specification { predefined_metric_type = \"ASGAverageCPUUtilization\" } target_value = 60 } } create_before_destroy earns its keep hereChanging golden_ami_id (a new Golden AMI baked from week2.html 5.3) replaces the launch template. Without create_before_destroy (16.3), the ASG would briefly reference a deleted template if the new one failed to create — with it, the new template exists before the old one is removed."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-3","title":"ACM and Route 53 in Code","body":"resource \"aws_acm_certificate\" \"app\" { domain_name = var.domain_name validation_method = \"DNS\" lifecycle { create_before_destroy = true } } resource \"aws_route53_record\" \"cert_validation\" { for_each = { for dvo in aws_acm_certificate.app.domain_validation_options : dvo.domain_name => dvo } zone_id = var.hosted_zone_id name = each.value.resource_record_name type = each.value.resource_record_type records = [each.value.resource_record_value] ttl = 60 } resource \"aws_acm_certificate_validation\" \"app\" { certificate_arn = aws_acm_certificate.app.arn validation_record_fqdns = [for r in aws_route53_record.cert_validation : r.fqdn] } resource \"aws_route53_record\" \"app\" { zone_id = var.hosted_zone_id name = var.domain_name type = \"A\" alias { name = aws_lb.app.dns_name zone_id = aws_lb.app.zone_id evaluate_target_health = true } } The manual CNAME step from week2.html 6.3, now self-completingaws_acm_certificate_validation is what makes apply actually wait for DNS validation to finish before moving on — by hand, this was watching describe-certificates for ISSUED. Here it's one dependency edge, and it means destroy then apply from empty (this week's deliverable) needs no manual pause at all."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-4","title":"RDS with Parameter and Subnet Groups, Multi-AZ","body":"resource \"aws_db_subnet_group\" \"app\" { name = \"${var.name_prefix}-db-subnets\" subnet_ids = var.private_data_subnet_ids # a distinct subnet tier from app (week2.html 7.1) } resource \"aws_db_parameter_group\" \"app\" { family = \"postgres16\" parameter { name = \"log_min_duration_statement\" value = \"500\" # ms -- feeds the slow-query workflow from week2.html 7.5 } } resource \"aws_db_instance\" \"primary\" { identifier = \"${var.name_prefix}-db\" engine = \"postgres\" engine_version = \"16.4\" instance_class = var.db_instance_class allocated_storage = 20 multi_az = var.environment == \"stg\" # Multi-AZ only where the deliverable calls for it db_subnet_group_name = aws_db_subnet_group.app.name parameter_group_name = aws_db_parameter_group.app.name vpc_security_group_ids = [aws_security_group.rds.id] manage_master_user_password = true # password generated and rotated in Secrets Manager -- see 18.5 lifecycle { prevent_destroy = true } } prevent_destroy is deliberate here, not everywhereEvery other resource in this module accepts destroy then apply from empty (this week's deliverable). The database is the one exception — real data must never disappear because a teardown script ran against the wrong workspace. Tearing down for real means removing this lifecycle block first, as an explicit, separate step (16.3)."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-5","title":"Secrets Referenced from Secrets Manager, Never Stored","body":"manage_master_user_password = true above already keeps the RDS password out of state and out of config entirely — AWS generates it and stores it in Secrets Manager directly. The application still needs to read that secret at runtime, without it ever appearing in a .tf file or a plan. data \"aws_secretsmanager_secret_version\" \"db\" { secret_id = aws_db_instance.primary.master_user_secret[0].secret_arn } # passed to the app as an environment variable pointing at the secret ARN, # never as the decrypted value -- the app fetches and decrypts it at boot resource \"aws_ecs_task_definition\" \"order_service\" { # ... container_definitions = jsonencode([{ name = \"order-service\" secrets = [ { name = \"DB_PASSWORD\", valueFrom = aws_db_instance.primary.master_user_secret[0].secret_arn } ] }]) } A data source that reads a secret still writes it to stateThe aws_secretsmanager_secret_version data source above pulls the plaintext value into Terraform state the moment anything references its secret_string attribute — same exposure as 17.1's warning about sensitive variables. Prefer passing the secret's ARN to the running service (as the ECS task definition does above) and letting the container fetch and decrypt it at boot, so the plaintext value never has a reason to pass through Terraform at all."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-6","title":"A Reusable ECS Service Module","body":"week3.html's order service, task definition and all, as a module — so that a second microservice is one more module call, not a copy-pasted block of ECS resources. # modules/ecs-service/variables.tf variable \"name\" {} variable \"image\" {} variable \"container_port\" { type = number } variable \"cluster_arn\" {} variable \"target_group_arn\" {} variable \"desired_count\" { type = number default = 2 } # modules/ecs-service/main.tf resource \"aws_ecs_task_definition\" \"this\" { family = var.name requires_compatibilities = [\"FARGATE\"] network_mode = \"awsvpc\" cpu = 256 memory = 512 container_definitions = jsonencode([{ name = var.name image = var.image portMappings = [{ containerPort = var.container_port }] }]) } resource \"aws_ecs_service\" \"this\" { name = var.name cluster = var.cluster_arn task_definition = aws_ecs_task_definition.this.arn desired_count = var.desired_count launch_type = \"FARGATE\" load_balancer { target_group_arn = var.target_group_arn container_name = var.name container_port = var.container_port } } # root: adding a microservice is one block module \"order_service\" { source = \"./modules/ecs-service\" name = \"order-service\" image = \"${var.ecr_repo_url}:order-service-latest\" container_port = 8080 cluster_arn = aws_ecs_cluster.main.arn target_group_arn = aws_lb_target_group.service[\"order\"].arn } This is the payoff from Day 16The module's interface (16.4) exposes exactly five inputs — everything about clusters, task CPU/memory defaults, and Fargate wiring stays inside the module. A second service, say payments-service, is a second module block with different values for those five inputs, not a second copy of the task-definition JSON."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-7","title":"The EKS Cluster, Node Group and IRSA","body":"week3.html's second runtime for the same order service, declared. More moving parts than ECS: the control plane, a node group of worker EC2 instances, and IRSA (IAM Roles for Service Accounts) so pods get scoped AWS permissions without static credentials baked into a container image. resource \"aws_eks_cluster\" \"main\" { name = \"${var.name_prefix}-eks\" role_arn = aws_iam_role.eks_cluster.arn vpc_config { subnet_ids = var.private_subnet_ids } } resource \"aws_eks_node_group\" \"main\" { cluster_name = aws_eks_cluster.main.name node_group_name = \"${var.name_prefix}-ng\" node_role_arn = aws_iam_role.eks_node.arn subnet_ids = var.private_subnet_ids scaling_config { desired_size = 2 min_size = 2 max_size = 4 } } # IRSA -- lets the order-service pod assume an IAM role scoped to just its own needs resource \"aws_iam_openid_connect_provider\" \"eks\" { url = aws_eks_cluster.main.identity[0].oidc[0].issuer client_id_list = [\"sts.amazonaws.com\"] thumbprint_list = [data.tls_certificate.eks.certificates[0].sha1_fingerprint] } resource \"aws_iam_role\" \"order_service_pod\" { name = \"order-service-irsa\" assume_role_policy = jsonencode({ Version = \"2012-10-17\" Statement = [{ Effect = \"Allow\" Principal = { Federated = aws_iam_openid_connect_provider.eks.arn } Action = \"sts:AssumeRoleWithWebIdentity\" Condition = { StringEquals = { \"${replace(aws_iam_openid_connect_provider.eks.url, \"https://\", \"\")}:sub\" = \"system:serviceaccount:default:order-service\" } } }] }) } Why IRSA over a node-wide IAM roleWithout IRSA, every pod on a node inherits the node group's own IAM role — the order-service pod and any other pod scheduled on that same worker would share identical AWS permissions. IRSA scopes credentials to the individual Kubernetes service account instead, matching the least-privilege principle the customer's readiness review (week5.html Day 22) explicitly checks for."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-8","title":"Worked Examples","body":"Two full case studies, distinct in kind from 18.1–18.7's single-topic explainers: a complete config built and verified end to end, then that same config refactored into modules. Everything from Days 15–18 assembled into one real, applied setup — a VPC with public and private subnets, an EC2 pair reachable through both an ALB and an NLB. Every block in both is validated and planned against a real AWS account. 18.8.1Building It: A VPC With Both an ALB and an NLB, Story-Book Style Built up in the order each piece actually depends on the one before it. Read it top to bottom the way it's written: why the piece exists, the code, then what it accomplished and what it unlocks next. Why each piece exists, before any code Each component below solves exactly one problem the one before it doesn't — this is the dependency chain, not the network layout (that's the next diagram). flowchart TD VPC[\"VPCYour own isolated network\"] --> Subnet[\"SubnetA slice of that network, pinned to one AZ\"] Subnet --> IGW[\"Internet GatewayThe door to the internet\"] IGW --> RT[\"Route Table + Route0.0.0.0/0 -> IGW is what actually makes a subnet public\"] RT --> SG[\"Security GroupFirewall -- explicitly allows specific traffic in\"] SG --> AMI[\"AMI (data source)The OS image an instance boots from\"] AMI --> EC2[\"EC2 InstanceThe actual server doing the work\"] EC2 --> TG[\"Target GroupA named, health-checked pool of backends\"] TG --> LB[\"ALB / NLBThe entry point -- HTTP-aware vs raw TCP passthrough\"] LB --> Listener[\"ListenerOpens a port, defines what to do with what arrives\"] Each layer only becomes meaningful once the one above it exists — a security group rule is meaningless without a route letting traffic reach the subnet at all; a listener is meaningless without a target group to forward into. VPC Your own private, isolated network inside AWS. Nothing else exists outside of one. Subnet A slice of the VPC's address space, pinned to one Availability Zone. \"Public\" vs \"private\" isn't inherent to the subnet — it's just which route table it ends up tied to. Internet Gateway (IGW) The door between the VPC and the internet. Without it, nothing inside the VPC can reach, or be reached from, outside AWS at all. Route Table + Route The actual switch that makes a subnet public: a 0.0.0.0/0 → IGW route plus an association is the entire mechanism. No route table entry, no internet, regardless of anything else. Security Group The firewall. AWS denies all inbound by default — this is what explicitly allows specific traffic (port 80, from where) to reach a specific resource. AMI (data source) The OS disk image an EC2 instance boots from. Looked up live instead of hardcoded so it never goes stale. EC2 Instance The actual server doing the work — running Apache, serving /, /foo, /bar. Target Group A named, health-checked pool of backends. A load balancer never points at instances directly — it always points at a target group, and instances register into it separately. ALB (Application Load Balancer) Layer 7, understands HTTP (paths, headers). The entry point for web traffic. NLB (Network Load Balancer) Layer 4, raw TCP passthrough, no HTTP awareness, much higher throughput, gives static IPs. Used here as a second, independent path to the same servers. Listener Opens an actual port on a load balancer and says what to do with what arrives. Without one, the load balancer exists but accepts nothing. What it looks like assembled flowchart TD Users([\"Users\"]) Users -->|\"https://.../foohttps://.../bar\"| ALB Users -->|\"tcp://nlb-url\"| NLB subgraph VPC[\"VPC 11.0.0.0/16 — fmk_vpc\"] IGW[\"Internet Gatewayfmk_igw\"] ALB[\"ALBfmk_alb : 80\"] NLB[\"NLBfmk_nlb : 80\"] TGpub[\"Target Groupfmk-tg-public (HTTP)\"] TGpriv[\"Target Groupfmk-tg-private (TCP)\"] subgraph AZa[\"Availability Zone — ap-south-1a\"] PubA[\"Public Subnet 11.0.1.0/24fmk-public-1a\"] EC2A[\"EC2ec2-A\"] PrivA[\"Private Subnet 11.0.3.0/24fmk-private-1a — empty\"] PubA --- EC2A end subgraph AZb[\"Availability Zone — ap-south-1b\"] PubB[\"Public Subnet 11.0.2.0/24fmk-public-1b\"] EC2B[\"EC2ec2-B\"] PrivB[\"Private Subnet 11.0.4.0/24fmk-private-1b — empty\"] PubB --- EC2B end ALB --> TGpub NLB --> TGpriv TGpub --> EC2A TGpub --> EC2B TGpriv --> EC2A TGpriv --> EC2B PubA -. \"route 0.0.0.0/0\" .-> IGW PubB -. \"route 0.0.0.0/0\" .-> IGW end classDef empty fill:transparent,stroke-dasharray: 4 3,color:#888; class PrivA,PrivB empty; Both AZs carry a public/private pair (AZ pinning from step 3); only the public side is populated so far — the private subnets (dashed) stay empty until a database moves in. 1. Provider — connect Terraform to the account Why: every resource below needs to know which AWS account and region to talk to. Nothing else in the file works without this. provider \"aws\" { region = \"ap-south-1\" profile = \"fahad\" } What it did: pointed Terraform at the fahad AWS CLI profile, in ap-south-1. Next: before anything can be created, it needs somewhere to live — a VPC. 2. VPC — the network boundary everything else lives inside Why: every subnet, load balancer, and instance below has to be created inside some VPC. 11.0.0.0/16 gives ~65,000 usable private addresses to divide up. resource \"aws_vpc\" \"fmk_vpc\" { cidr_block = \"11.0.0.0/16\" } What it did: reserved the address range. It's still empty — no subnets, no routing, nothing launchable yet. Next: a VPC this size is too undifferentiated to launch into directly — it needs dividing into subnets. 3. Subnets — dividing the VPC into public and private zones, across two AZs Why: \"public\" and \"private\" aren't a property of the VPC as a whole — they're a property of which route table a subnet ends up associated with (steps 5/6 decide that). These four blocks carve out four non-overlapping ranges, one pair per Availability Zone: a public and a private subnet in ap-south-1a, a public and a private subnet in ap-south-1b. Pinning availability_zone explicitly (rather than letting AWS auto-assign one) is what guarantees a public/private pair actually shares an AZ — the pairing this diagram's two-AZ layout depends on, and the shape a database subnet group (week2.html 7.1) will need once the private subnets are actually used. resource \"aws_subnet\" \"fmk_public_1a\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.1.0/24\" availability_zone = \"ap-south-1a\" tags = { Name = \"fmk-public-1a\" } } resource \"aws_subnet\" \"fmk_public_1b\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.2.0/24\" availability_zone = \"ap-south-1b\" tags = { Name = \"fmk-public-1b\" } } resource \"aws_subnet\" \"fmk_private_1a\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.3.0/24\" availability_zone = \"ap-south-1a\" tags = { Name = \"fmk-private-1a\" } } resource \"aws_subnet\" \"fmk_private_1b\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.4.0/24\" availability_zone = \"ap-south-1b\" tags = { Name = \"fmk-private-1b\" } } What it did: sliced 11.0.0.0/16 into four /24s, each pinned to a specific AZ — two tagged \"public\" and two \"private,\" but the AZ pinning is a real constraint, not just a label. Next: a subnet needs a door to the internet before anything inside it is reachable from outside AWS. The private subnets stay empty, on purposeThis build only ever launches EC2 instances into the two public subnets — fmk-private-1a/fmk-private-1b exist with their own route table (step 6) but hold nothing yet. That's deliberate: they're reserved for whatever shouldn't be directly internet-reachable, most likely an RDS instance once Week 2's database patterns get revisited here. An empty subnet costs nothing to leave declared. 4. Internet Gateway — the VPC's door to the internet Why: a VPC is fully isolated by default. An Internet Gateway makes reaching the internet possible, but only for whichever subnets are explicitly routed through it (step 5). resource \"aws_internet_gateway\" \"fmk_igw\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-igw\" } } What it did: created the gateway and attached it to fmk_vpc — the vpc_id argument itself does the attaching, no separate resource needed. Next: attachment alone routes no traffic. Each subnet still needs its own route table pointing at it. 5. Public routing — route table, its route to the IGW, and its subnets Why: this is the piece that actually makes \"public\" mean something. Every route table gets an in-VPC \"local\" route for free; adding a 0.0.0.0/0 → Internet Gateway rule, then associating a subnet with this table, is the entire mechanism that makes that subnet public. Nothing else about the subnet changes. resource \"aws_route_table\" \"fmk_public_rt\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-public-rt\" } } resource \"aws_route\" \"fmk_public_default\" { route_table_id = aws_route_table.fmk_public_rt.id destination_cidr_block = \"0.0.0.0/0\" gateway_id = aws_internet_gateway.fmk_igw.id } resource \"aws_route_table_association\" \"fmk_public_1a\" { subnet_id = aws_subnet.fmk_public_1a.id route_table_id = aws_route_table.fmk_public_rt.id } resource \"aws_route_table_association\" \"fmk_public_1b\" { subnet_id = aws_subnet.fmk_public_1b.id route_table_id = aws_route_table.fmk_public_rt.id } What it did: gave the table one rule (everything not bound for inside the VPC goes to the IGW), then associated both public subnets with it — they're now genuinely public. Next: the private subnets need routing too, just without that 0.0.0.0/0 rule. 6. Private routing — a route table with no path to the internet Why: every subnet needs an explicit association, or it silently falls back to the VPC's implicit main route table. Giving the private subnets their own table with no internet route makes \"these are private\" a visible, deliberate fact in the code, not an accident of what wasn't configured. resource \"aws_route_table\" \"fmk_private_rt\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-private-rt\" } } resource \"aws_route_table_association\" \"fmk_private_1a\" { subnet_id = aws_subnet.fmk_private_1a.id route_table_id = aws_route_table.fmk_private_rt.id } resource \"aws_route_table_association\" \"fmk_private_1b\" { subnet_id = aws_subnet.fmk_private_1b.id route_table_id = aws_route_table.fmk_private_rt.id } What it did: associated both private subnets with a table that has no internet route — they keep the automatic in-VPC \"local\" route, so they can still reach the rest of the VPC, just never the internet directly. Next: the network layout is done. Now security — what's actually allowed to reach what, regardless of how traffic is routed. 7. Security group — the firewall in front of the EC2 instances Why: routing decides whether traffic can reach a subnet. A security group decides whether it's actually allowed to reach a specific resource, port by port. AWS denies all inbound by default — without this, the instances below would be unreachable even sitting in a public, internet-routed subnet. resource \"aws_security_group\" \"fmk_tls_sg\" { name = \"fmk_tls_sg\" description = \"Allow TLS inbound traffic and all outbound traffic\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_vpc_security_group_ingress_rule\" \"allow_tls_ipv4\" { security_group_id = aws_security_group.fmk_tls_sg.id cidr_ipv4 = \"0.0.0.0/0\" from_port = 80 ip_protocol = \"tcp\" to_port = 80 } resource \"aws_vpc_security_group_egress_rule\" \"allow_all_traffic_ipv4\" { security_group_id = aws_security_group.fmk_tls_sg.id cidr_ipv4 = \"0.0.0.0/0\" ip_protocol = \"-1\" # semantically equivalent to all ports } What it did: one group, two rules — inbound TCP 80 from anywhere (matching what Apache actually serves), and all outbound. This same group gets reused for the ALB, the NLB's targets, and the instances themselves — no separate front-door group here. Next: the actual compute, and the AMI it boots from. 8. AMI lookup — which OS image the instances boot from Why: an instance needs an AMI to boot from. Querying for \"whichever Amazon Linux 2023 AMI is newest right now\" instead of hardcoding an ID avoids that ID going stale the moment AWS ships a patched version. data \"aws_ami\" \"amzn_linux\" { most_recent = true owners = [\"amazon\"] filter { name = \"name\" values = [\"al2023-ami-2023.*-x86_64\"] } } What it did: nothing created — data blocks only read (15.3). This resolves to today's latest Amazon Linux 2023 AMI ID for ap-south-1 at plan/apply time. Next: the EC2 instances themselves. 9. EC2 instances — the actual servers Why: the real compute — two servers that will sit behind both load balancers. Each launches into a different public subnet so the pair survives a single AZ failure. resource \"aws_instance\" \"ec2-A\" { ami = data.aws_ami.amzn_linux.id instance_type = \"t3.micro\" subnet_id = aws_subnet.fmk_public_1a.id user_data = file(\"${path.module}/userdata.sh\") vpc_security_group_ids = [aws_security_group.fmk_tls_sg.id] tags = { Name = \"fmk-ec2-A\" } } resource \"aws_instance\" \"ec2-B\" { ami = data.aws_ami.amzn_linux.id instance_type = \"t3.micro\" subnet_id = aws_subnet.fmk_public_1b.id user_data = file(\"${path.module}/userdata.sh\") vpc_security_group_ids = [aws_security_group.fmk_tls_sg.id] tags = { Name = \"fmk-ec2-B\" } } What it did: launched both instances, each bootstrapped by userdata.sh (installs Apache, writes /, /foo, and /bar pages showing that instance's own hostname and IP), each attached to fmk_tls_sg so port 80 can actually reach them. Next: the servers are individually reachable if you knew their IPs — but nothing distributes traffic across the two yet. That's the ALB's job first. user_data has to match the AMI family, not just the SSH user16.7 already covers the wrong-SSH-user version of this mistake (ubuntu vs ec2-user) — the same AMI-dependence bites the script itself, not just the login. A real user_data script written with apt-get update / apt-get install -y apache2 against this exact al2023-... AMI failed completely: Amazon Linux has no apt-get at all (it uses dnf/yum), so the very first line errors with command not found and nothing after it ever runs. Even fixed to the right package manager, the package name is also OS-specific — Amazon Linux's Apache is called httpd, never apache2 (that's Debian/Ubuntu's name for the same thing). Matching userdata.sh's commands to the actual AMI family is exactly as necessary as matching the SSH username to it. Check /var/log/cloud-init-output.log before rewriting blindA failed user_data script gives no error anywhere in terraform apply — the instance still reaches running either way, since AWS considers the instance successfully launched regardless of what its boot script did. The only place the actual failure (apt-get: command not found, in this case) shows up is /var/log/cloud-init-output.log on the instance itself, over SSH — worth checking directly rather than guessing at what a silent script failure was. 10. ALB path — target group, then the ALB, then its listener Why a target group first: an ALB never points at instances directly — a target group is the indirection layer in between, a named, health-checked pool the ALB forwards to. Declaring the group and its membership separately from the ALB means instances can be added or removed without touching the ALB at all. resource \"aws_lb_target_group\" \"fmk-tg-public\" { name = \"fmk-tg-public-1a\" target_type = \"instance\" port = 80 protocol = \"HTTP\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_lb_target_group_attachment\" \"ec2_a\" { target_group_arn = aws_lb_target_group.fmk-tg-public.arn target_id = aws_instance.ec2-A.id port = 80 } resource \"aws_lb_target_group_attachment\" \"ec2_b\" { target_group_arn = aws_lb_target_group.fmk-tg-public.arn target_id = aws_instance.ec2-B.id port = 80 } Why the ALB: the actual internet-facing entry point — what a browser connects to. It needs the public subnets (to be internet-reachable) and the security group governing what can reach it. resource \"aws_alb\" \"fmk_alb\" { name = \"fmk-alb-tf\" internal = false load_balancer_type = \"application\" security_groups = [aws_security_group.fmk_tls_sg.id] subnets = [aws_subnet.fmk_public_1a.id, aws_subnet.fmk_public_1b.id] } Why the listener: this is what actually opens a port on the ALB and defines what to do with what arrives on it — without one, the ALB accepts nothing and every connection times out. resource \"aws_lb_listener\" \"fmk_alb_http\" { load_balancer_arn = aws_alb.fmk_alb.arn port = 80 protocol = \"HTTP\" default_action { type = \"forward\" target_group_arn = aws_lb_target_group.fmk-tg-public.arn } } What it did: opened port 80 on the ALB, forwarding everything into fmk-tg-public. The ALB path is complete: internet → ALB:80 → fmk-tg-public → ec2-A/ec2-B. No separate listener rules for /foo//bar are needed — both instances already serve those paths themselves (userdata.sh), so one forward action reaches either path on whichever instance the ALB picks. Next: the second path — the same instances, reached through an NLB instead. 11. NLB path — a second target group, the NLB, and its listener Why a second target group: an ALB target group and an NLB target group aren't interchangeable — this one is TCP, matching what an NLB actually forwards (raw connections, no HTTP awareness at all). The same two instances register here too — one pool of servers, reachable through two independent paths, not two separate pools. resource \"aws_lb_target_group\" \"fmk-tg-private\" { name = \"fmk-tg-private-1a\" port = 80 protocol = \"TCP\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_lb_target_group_attachment\" \"ec2_a_nlb\" { target_group_arn = aws_lb_target_group.fmk-tg-private.arn target_id = aws_instance.ec2-A.id port = 80 } resource \"aws_lb_target_group_attachment\" \"ec2_b_nlb\" { target_group_arn = aws_lb_target_group.fmk-tg-private.arn target_id = aws_instance.ec2-B.id port = 80 } Why the NLB: the same role as the ALB — the entry point — but Layer 4 instead of Layer 7. Notice there's no security_groups argument: an NLB has no security group of its own, being pure passthrough. Access control for this path lives entirely on the target's own SG (fmk_tls_sg, step 7), which is why that rule had to allow 0.0.0.0/0 rather than being scoped to one specific load balancer. resource \"aws_lb\" \"fmk_nlb\" { name = \"fmk-nlb-tf\" internal = false load_balancer_type = \"network\" subnets = [aws_subnet.fmk_public_1a.id, aws_subnet.fmk_public_1b.id] } resource \"aws_lb_listener\" \"fmk_nlb_tcp\" { load_balancer_arn = aws_lb.fmk_nlb.arn port = 80 protocol = \"TCP\" default_action { type = \"forward\" target_group_arn = aws_lb_target_group.fmk-tg-private.arn } } What it did: opened port 80 on the NLB, forwarding every raw TCP connection into fmk-tg-private. Both paths are now complete: internet → ALB:80 → fmk-tg-public → the instances, and internet → NLB:80 → fmk-tg-private → the same instances. Verified, not just writtenThis exact config — 28 resources — ran clean through terraform validate and terraform plan against a real AWS account with zero errors. Building it surfaced real mistakes worth recognizing on sight: a security_groups argument pointed at rule resources instead of the group itself, a missing subnets argument on an ALB, an underscore in a load-balancer name (AWS only allows hyphens), a security group rule scoped to 443/VPC-only when the app actually serves 80 to the internet, a target_type of \"alb\" left over on a target group meant to hold EC2 instances, and two EC2 instances launched with no security group attached at all — each one invisible until either validate caught it structurally, or reasoning through what the diagram actually required caught it semantically. 18.8.2The Same 28 Resources, Refactored Into Modules 18.8.1's single main.tf pulled apart into four modules — network, security, compute, loadbalancing — following exactly the module-design principles from 16.4/16.6. Worth being honest about the trade-off up front: 16.6's own \"rule of three\" says don't extract a module until something's genuinely reused a third time, and this only has one caller. Doing it anyway here is a deliberate learning exercise — practicing the shape of a real module before there's a second environment forcing the issue — not a case where the rule stopped applying. day02-modules/ ├── versions.tf -- Terraform + provider version constraints ├── variables.tf -- root inputs: region, profile, vpc_cidr, name_prefix ├── main.tf -- four `module` calls, wiring outputs into inputs. No resource blocks. ├── outputs.tf -- vpc_id, alb_dns_name, nlb_dns_name └── modules/ ├── network/ (VPC, 4 subnets, IGW, both route tables) ├── security/ (the shared security group + its two rules) ├── compute/ (AMI lookup, EC2 instances, userdata.sh) └── loadbalancing/ (both target groups, ALB, NLB, both listeners) each containing: versions.tf, variables.tf, main.tf, outputs.tf What each file type holds, and why it's separate versions.tf Terraform and provider version constraints (15.2) — nothing here creates anything, it's checked once at init. Every module carries its own copy: a module should be explicit about what it needs regardless of what the root happens to declare, so it stays correct if it's ever pulled into a different project. variables.tf The module's inputs — its public interface (16.4). Reading this file alone should tell you everything needed to call the module, without opening main.tf at all. modules/security/variables.tf, for example, says \"give me a vpc_id; I'll assume port 80 unless you override ingress_port.\" main.tf The actual resource/data blocks — the implementation. A caller never needs to read this; it's free to change internally as long as variables.tf and outputs.tf stay the same contract. outputs.tf The module's return values — whatever the next module in the chain needs back. modules/network/outputs.tf hands back vpc_id and both subnet maps because security, compute, and loadbalancing all need those downstream. Root main.tf contains zero resource blocksOnce split, the root main.tf is just four module calls passing outputs forward — module.network.vpc_id into security, both of those into compute, all three into loadbalancing. Every actual resource lives one level down, inside a module. This is what \"the root just wires things together\" looks like in a real file, not just as a description. One resource, before and after Splitting into modules was also a chance to apply 16.1's for_each guidance retroactively. 18.8.1's four separately-named subnet resources became one for_each block over a map: ## Before (18.8.1) -- four separate resources resource \"aws_subnet\" \"fmk_public_1a\" { cidr_block = \"11.0.1.0/24\" ... } resource \"aws_subnet\" \"fmk_public_1b\" { cidr_block = \"11.0.2.0/24\" ... } # state addresses: aws_subnet.fmk_public_1a, aws_subnet.fmk_public_1b ## After (day02-modules) -- one resource, driven by the caller's map resource \"aws_subnet\" \"public\" { for_each = var.public_subnets cidr_block = each.value.cidr_block availability_zone = each.value.availability_zone } # state addresses: module.network.aws_subnet.public[\"1a\"], [\"1b\"] Same two subnets get created either way — terraform plan on the modularized version still shows 28 to add, 0 to change, 0 to destroy, identical to 18.8.1. What changed is that adding a third AZ later means adding one entry to the public_subnets map passed into the module, not writing a fifth resource block by hand."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-8-1","title":"Building It: A VPC With Both an ALB and an NLB, Story-Book Style","body":"Built up in the order each piece actually depends on the one before it. Read it top to bottom the way it's written: why the piece exists, the code, then what it accomplished and what it unlocks next. Why each piece exists, before any code Each component below solves exactly one problem the one before it doesn't — this is the dependency chain, not the network layout (that's the next diagram). flowchart TD VPC[\"VPCYour own isolated network\"] --> Subnet[\"SubnetA slice of that network, pinned to one AZ\"] Subnet --> IGW[\"Internet GatewayThe door to the internet\"] IGW --> RT[\"Route Table + Route0.0.0.0/0 -> IGW is what actually makes a subnet public\"] RT --> SG[\"Security GroupFirewall -- explicitly allows specific traffic in\"] SG --> AMI[\"AMI (data source)The OS image an instance boots from\"] AMI --> EC2[\"EC2 InstanceThe actual server doing the work\"] EC2 --> TG[\"Target GroupA named, health-checked pool of backends\"] TG --> LB[\"ALB / NLBThe entry point -- HTTP-aware vs raw TCP passthrough\"] LB --> Listener[\"ListenerOpens a port, defines what to do with what arrives\"] Each layer only becomes meaningful once the one above it exists — a security group rule is meaningless without a route letting traffic reach the subnet at all; a listener is meaningless without a target group to forward into. VPC Your own private, isolated network inside AWS. Nothing else exists outside of one. Subnet A slice of the VPC's address space, pinned to one Availability Zone. \"Public\" vs \"private\" isn't inherent to the subnet — it's just which route table it ends up tied to. Internet Gateway (IGW) The door between the VPC and the internet. Without it, nothing inside the VPC can reach, or be reached from, outside AWS at all. Route Table + Route The actual switch that makes a subnet public: a 0.0.0.0/0 → IGW route plus an association is the entire mechanism. No route table entry, no internet, regardless of anything else. Security Group The firewall. AWS denies all inbound by default — this is what explicitly allows specific traffic (port 80, from where) to reach a specific resource. AMI (data source) The OS disk image an EC2 instance boots from. Looked up live instead of hardcoded so it never goes stale. EC2 Instance The actual server doing the work — running Apache, serving /, /foo, /bar. Target Group A named, health-checked pool of backends. A load balancer never points at instances directly — it always points at a target group, and instances register into it separately. ALB (Application Load Balancer) Layer 7, understands HTTP (paths, headers). The entry point for web traffic. NLB (Network Load Balancer) Layer 4, raw TCP passthrough, no HTTP awareness, much higher throughput, gives static IPs. Used here as a second, independent path to the same servers. Listener Opens an actual port on a load balancer and says what to do with what arrives. Without one, the load balancer exists but accepts nothing. What it looks like assembled flowchart TD Users([\"Users\"]) Users -->|\"https://.../foohttps://.../bar\"| ALB Users -->|\"tcp://nlb-url\"| NLB subgraph VPC[\"VPC 11.0.0.0/16 — fmk_vpc\"] IGW[\"Internet Gatewayfmk_igw\"] ALB[\"ALBfmk_alb : 80\"] NLB[\"NLBfmk_nlb : 80\"] TGpub[\"Target Groupfmk-tg-public (HTTP)\"] TGpriv[\"Target Groupfmk-tg-private (TCP)\"] subgraph AZa[\"Availability Zone — ap-south-1a\"] PubA[\"Public Subnet 11.0.1.0/24fmk-public-1a\"] EC2A[\"EC2ec2-A\"] PrivA[\"Private Subnet 11.0.3.0/24fmk-private-1a — empty\"] PubA --- EC2A end subgraph AZb[\"Availability Zone — ap-south-1b\"] PubB[\"Public Subnet 11.0.2.0/24fmk-public-1b\"] EC2B[\"EC2ec2-B\"] PrivB[\"Private Subnet 11.0.4.0/24fmk-private-1b — empty\"] PubB --- EC2B end ALB --> TGpub NLB --> TGpriv TGpub --> EC2A TGpub --> EC2B TGpriv --> EC2A TGpriv --> EC2B PubA -. \"route 0.0.0.0/0\" .-> IGW PubB -. \"route 0.0.0.0/0\" .-> IGW end classDef empty fill:transparent,stroke-dasharray: 4 3,color:#888; class PrivA,PrivB empty; Both AZs carry a public/private pair (AZ pinning from step 3); only the public side is populated so far — the private subnets (dashed) stay empty until a database moves in. 1. Provider — connect Terraform to the account Why: every resource below needs to know which AWS account and region to talk to. Nothing else in the file works without this. provider \"aws\" { region = \"ap-south-1\" profile = \"fahad\" } What it did: pointed Terraform at the fahad AWS CLI profile, in ap-south-1. Next: before anything can be created, it needs somewhere to live — a VPC. 2. VPC — the network boundary everything else lives inside Why: every subnet, load balancer, and instance below has to be created inside some VPC. 11.0.0.0/16 gives ~65,000 usable private addresses to divide up. resource \"aws_vpc\" \"fmk_vpc\" { cidr_block = \"11.0.0.0/16\" } What it did: reserved the address range. It's still empty — no subnets, no routing, nothing launchable yet. Next: a VPC this size is too undifferentiated to launch into directly — it needs dividing into subnets. 3. Subnets — dividing the VPC into public and private zones, across two AZs Why: \"public\" and \"private\" aren't a property of the VPC as a whole — they're a property of which route table a subnet ends up associated with (steps 5/6 decide that). These four blocks carve out four non-overlapping ranges, one pair per Availability Zone: a public and a private subnet in ap-south-1a, a public and a private subnet in ap-south-1b. Pinning availability_zone explicitly (rather than letting AWS auto-assign one) is what guarantees a public/private pair actually shares an AZ — the pairing this diagram's two-AZ layout depends on, and the shape a database subnet group (week2.html 7.1) will need once the private subnets are actually used. resource \"aws_subnet\" \"fmk_public_1a\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.1.0/24\" availability_zone = \"ap-south-1a\" tags = { Name = \"fmk-public-1a\" } } resource \"aws_subnet\" \"fmk_public_1b\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.2.0/24\" availability_zone = \"ap-south-1b\" tags = { Name = \"fmk-public-1b\" } } resource \"aws_subnet\" \"fmk_private_1a\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.3.0/24\" availability_zone = \"ap-south-1a\" tags = { Name = \"fmk-private-1a\" } } resource \"aws_subnet\" \"fmk_private_1b\" { vpc_id = aws_vpc.fmk_vpc.id cidr_block = \"11.0.4.0/24\" availability_zone = \"ap-south-1b\" tags = { Name = \"fmk-private-1b\" } } What it did: sliced 11.0.0.0/16 into four /24s, each pinned to a specific AZ — two tagged \"public\" and two \"private,\" but the AZ pinning is a real constraint, not just a label. Next: a subnet needs a door to the internet before anything inside it is reachable from outside AWS. The private subnets stay empty, on purposeThis build only ever launches EC2 instances into the two public subnets — fmk-private-1a/fmk-private-1b exist with their own route table (step 6) but hold nothing yet. That's deliberate: they're reserved for whatever shouldn't be directly internet-reachable, most likely an RDS instance once Week 2's database patterns get revisited here. An empty subnet costs nothing to leave declared. 4. Internet Gateway — the VPC's door to the internet Why: a VPC is fully isolated by default. An Internet Gateway makes reaching the internet possible, but only for whichever subnets are explicitly routed through it (step 5). resource \"aws_internet_gateway\" \"fmk_igw\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-igw\" } } What it did: created the gateway and attached it to fmk_vpc — the vpc_id argument itself does the attaching, no separate resource needed. Next: attachment alone routes no traffic. Each subnet still needs its own route table pointing at it. 5. Public routing — route table, its route to the IGW, and its subnets Why: this is the piece that actually makes \"public\" mean something. Every route table gets an in-VPC \"local\" route for free; adding a 0.0.0.0/0 → Internet Gateway rule, then associating a subnet with this table, is the entire mechanism that makes that subnet public. Nothing else about the subnet changes. resource \"aws_route_table\" \"fmk_public_rt\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-public-rt\" } } resource \"aws_route\" \"fmk_public_default\" { route_table_id = aws_route_table.fmk_public_rt.id destination_cidr_block = \"0.0.0.0/0\" gateway_id = aws_internet_gateway.fmk_igw.id } resource \"aws_route_table_association\" \"fmk_public_1a\" { subnet_id = aws_subnet.fmk_public_1a.id route_table_id = aws_route_table.fmk_public_rt.id } resource \"aws_route_table_association\" \"fmk_public_1b\" { subnet_id = aws_subnet.fmk_public_1b.id route_table_id = aws_route_table.fmk_public_rt.id } What it did: gave the table one rule (everything not bound for inside the VPC goes to the IGW), then associated both public subnets with it — they're now genuinely public. Next: the private subnets need routing too, just without that 0.0.0.0/0 rule. 6. Private routing — a route table with no path to the internet Why: every subnet needs an explicit association, or it silently falls back to the VPC's implicit main route table. Giving the private subnets their own table with no internet route makes \"these are private\" a visible, deliberate fact in the code, not an accident of what wasn't configured. resource \"aws_route_table\" \"fmk_private_rt\" { vpc_id = aws_vpc.fmk_vpc.id tags = { Name = \"fmk-private-rt\" } } resource \"aws_route_table_association\" \"fmk_private_1a\" { subnet_id = aws_subnet.fmk_private_1a.id route_table_id = aws_route_table.fmk_private_rt.id } resource \"aws_route_table_association\" \"fmk_private_1b\" { subnet_id = aws_subnet.fmk_private_1b.id route_table_id = aws_route_table.fmk_private_rt.id } What it did: associated both private subnets with a table that has no internet route — they keep the automatic in-VPC \"local\" route, so they can still reach the rest of the VPC, just never the internet directly. Next: the network layout is done. Now security — what's actually allowed to reach what, regardless of how traffic is routed. 7. Security group — the firewall in front of the EC2 instances Why: routing decides whether traffic can reach a subnet. A security group decides whether it's actually allowed to reach a specific resource, port by port. AWS denies all inbound by default — without this, the instances below would be unreachable even sitting in a public, internet-routed subnet. resource \"aws_security_group\" \"fmk_tls_sg\" { name = \"fmk_tls_sg\" description = \"Allow TLS inbound traffic and all outbound traffic\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_vpc_security_group_ingress_rule\" \"allow_tls_ipv4\" { security_group_id = aws_security_group.fmk_tls_sg.id cidr_ipv4 = \"0.0.0.0/0\" from_port = 80 ip_protocol = \"tcp\" to_port = 80 } resource \"aws_vpc_security_group_egress_rule\" \"allow_all_traffic_ipv4\" { security_group_id = aws_security_group.fmk_tls_sg.id cidr_ipv4 = \"0.0.0.0/0\" ip_protocol = \"-1\" # semantically equivalent to all ports } What it did: one group, two rules — inbound TCP 80 from anywhere (matching what Apache actually serves), and all outbound. This same group gets reused for the ALB, the NLB's targets, and the instances themselves — no separate front-door group here. Next: the actual compute, and the AMI it boots from. 8. AMI lookup — which OS image the instances boot from Why: an instance needs an AMI to boot from. Querying for \"whichever Amazon Linux 2023 AMI is newest right now\" instead of hardcoding an ID avoids that ID going stale the moment AWS ships a patched version. data \"aws_ami\" \"amzn_linux\" { most_recent = true owners = [\"amazon\"] filter { name = \"name\" values = [\"al2023-ami-2023.*-x86_64\"] } } What it did: nothing created — data blocks only read (15.3). This resolves to today's latest Amazon Linux 2023 AMI ID for ap-south-1 at plan/apply time. Next: the EC2 instances themselves. 9. EC2 instances — the actual servers Why: the real compute — two servers that will sit behind both load balancers. Each launches into a different public subnet so the pair survives a single AZ failure. resource \"aws_instance\" \"ec2-A\" { ami = data.aws_ami.amzn_linux.id instance_type = \"t3.micro\" subnet_id = aws_subnet.fmk_public_1a.id user_data = file(\"${path.module}/userdata.sh\") vpc_security_group_ids = [aws_security_group.fmk_tls_sg.id] tags = { Name = \"fmk-ec2-A\" } } resource \"aws_instance\" \"ec2-B\" { ami = data.aws_ami.amzn_linux.id instance_type = \"t3.micro\" subnet_id = aws_subnet.fmk_public_1b.id user_data = file(\"${path.module}/userdata.sh\") vpc_security_group_ids = [aws_security_group.fmk_tls_sg.id] tags = { Name = \"fmk-ec2-B\" } } What it did: launched both instances, each bootstrapped by userdata.sh (installs Apache, writes /, /foo, and /bar pages showing that instance's own hostname and IP), each attached to fmk_tls_sg so port 80 can actually reach them. Next: the servers are individually reachable if you knew their IPs — but nothing distributes traffic across the two yet. That's the ALB's job first. user_data has to match the AMI family, not just the SSH user16.7 already covers the wrong-SSH-user version of this mistake (ubuntu vs ec2-user) — the same AMI-dependence bites the script itself, not just the login. A real user_data script written with apt-get update / apt-get install -y apache2 against this exact al2023-... AMI failed completely: Amazon Linux has no apt-get at all (it uses dnf/yum), so the very first line errors with command not found and nothing after it ever runs. Even fixed to the right package manager, the package name is also OS-specific — Amazon Linux's Apache is called httpd, never apache2 (that's Debian/Ubuntu's name for the same thing). Matching userdata.sh's commands to the actual AMI family is exactly as necessary as matching the SSH username to it. Check /var/log/cloud-init-output.log before rewriting blindA failed user_data script gives no error anywhere in terraform apply — the instance still reaches running either way, since AWS considers the instance successfully launched regardless of what its boot script did. The only place the actual failure (apt-get: command not found, in this case) shows up is /var/log/cloud-init-output.log on the instance itself, over SSH — worth checking directly rather than guessing at what a silent script failure was. 10. ALB path — target group, then the ALB, then its listener Why a target group first: an ALB never points at instances directly — a target group is the indirection layer in between, a named, health-checked pool the ALB forwards to. Declaring the group and its membership separately from the ALB means instances can be added or removed without touching the ALB at all. resource \"aws_lb_target_group\" \"fmk-tg-public\" { name = \"fmk-tg-public-1a\" target_type = \"instance\" port = 80 protocol = \"HTTP\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_lb_target_group_attachment\" \"ec2_a\" { target_group_arn = aws_lb_target_group.fmk-tg-public.arn target_id = aws_instance.ec2-A.id port = 80 } resource \"aws_lb_target_group_attachment\" \"ec2_b\" { target_group_arn = aws_lb_target_group.fmk-tg-public.arn target_id = aws_instance.ec2-B.id port = 80 } Why the ALB: the actual internet-facing entry point — what a browser connects to. It needs the public subnets (to be internet-reachable) and the security group governing what can reach it. resource \"aws_alb\" \"fmk_alb\" { name = \"fmk-alb-tf\" internal = false load_balancer_type = \"application\" security_groups = [aws_security_group.fmk_tls_sg.id] subnets = [aws_subnet.fmk_public_1a.id, aws_subnet.fmk_public_1b.id] } Why the listener: this is what actually opens a port on the ALB and defines what to do with what arrives on it — without one, the ALB accepts nothing and every connection times out. resource \"aws_lb_listener\" \"fmk_alb_http\" { load_balancer_arn = aws_alb.fmk_alb.arn port = 80 protocol = \"HTTP\" default_action { type = \"forward\" target_group_arn = aws_lb_target_group.fmk-tg-public.arn } } What it did: opened port 80 on the ALB, forwarding everything into fmk-tg-public. The ALB path is complete: internet → ALB:80 → fmk-tg-public → ec2-A/ec2-B. No separate listener rules for /foo//bar are needed — both instances already serve those paths themselves (userdata.sh), so one forward action reaches either path on whichever instance the ALB picks. Next: the second path — the same instances, reached through an NLB instead. 11. NLB path — a second target group, the NLB, and its listener Why a second target group: an ALB target group and an NLB target group aren't interchangeable — this one is TCP, matching what an NLB actually forwards (raw connections, no HTTP awareness at all). The same two instances register here too — one pool of servers, reachable through two independent paths, not two separate pools. resource \"aws_lb_target_group\" \"fmk-tg-private\" { name = \"fmk-tg-private-1a\" port = 80 protocol = \"TCP\" vpc_id = aws_vpc.fmk_vpc.id } resource \"aws_lb_target_group_attachment\" \"ec2_a_nlb\" { target_group_arn = aws_lb_target_group.fmk-tg-private.arn target_id = aws_instance.ec2-A.id port = 80 } resource \"aws_lb_target_group_attachment\" \"ec2_b_nlb\" { target_group_arn = aws_lb_target_group.fmk-tg-private.arn target_id = aws_instance.ec2-B.id port = 80 } Why the NLB: the same role as the ALB — the entry point — but Layer 4 instead of Layer 7. Notice there's no security_groups argument: an NLB has no security group of its own, being pure passthrough. Access control for this path lives entirely on the target's own SG (fmk_tls_sg, step 7), which is why that rule had to allow 0.0.0.0/0 rather than being scoped to one specific load balancer. resource \"aws_lb\" \"fmk_nlb\" { name = \"fmk-nlb-tf\" internal = false load_balancer_type = \"network\" subnets = [aws_subnet.fmk_public_1a.id, aws_subnet.fmk_public_1b.id] } resource \"aws_lb_listener\" \"fmk_nlb_tcp\" { load_balancer_arn = aws_lb.fmk_nlb.arn port = 80 protocol = \"TCP\" default_action { type = \"forward\" target_group_arn = aws_lb_target_group.fmk-tg-private.arn } } What it did: opened port 80 on the NLB, forwarding every raw TCP connection into fmk-tg-private. Both paths are now complete: internet → ALB:80 → fmk-tg-public → the instances, and internet → NLB:80 → fmk-tg-private → the same instances. Verified, not just writtenThis exact config — 28 resources — ran clean through terraform validate and terraform plan against a real AWS account with zero errors. Building it surfaced real mistakes worth recognizing on sight: a security_groups argument pointed at rule resources instead of the group itself, a missing subnets argument on an ALB, an underscore in a load-balancer name (AWS only allows hyphens), a security group rule scoped to 443/VPC-only when the app actually serves 80 to the internet, a target_type of \"alb\" left over on a target group meant to hold EC2 instances, and two EC2 instances launched with no security group attached at all — each one invisible until either validate caught it structurally, or reasoning through what the diagram actually required caught it semantically."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-8-2","title":"The Same 28 Resources, Refactored Into Modules","body":"18.8.1's single main.tf pulled apart into four modules — network, security, compute, loadbalancing — following exactly the module-design principles from 16.4/16.6. Worth being honest about the trade-off up front: 16.6's own \"rule of three\" says don't extract a module until something's genuinely reused a third time, and this only has one caller. Doing it anyway here is a deliberate learning exercise — practicing the shape of a real module before there's a second environment forcing the issue — not a case where the rule stopped applying. day02-modules/ ├── versions.tf -- Terraform + provider version constraints ├── variables.tf -- root inputs: region, profile, vpc_cidr, name_prefix ├── main.tf -- four `module` calls, wiring outputs into inputs. No resource blocks. ├── outputs.tf -- vpc_id, alb_dns_name, nlb_dns_name └── modules/ ├── network/ (VPC, 4 subnets, IGW, both route tables) ├── security/ (the shared security group + its two rules) ├── compute/ (AMI lookup, EC2 instances, userdata.sh) └── loadbalancing/ (both target groups, ALB, NLB, both listeners) each containing: versions.tf, variables.tf, main.tf, outputs.tf What each file type holds, and why it's separate versions.tf Terraform and provider version constraints (15.2) — nothing here creates anything, it's checked once at init. Every module carries its own copy: a module should be explicit about what it needs regardless of what the root happens to declare, so it stays correct if it's ever pulled into a different project. variables.tf The module's inputs — its public interface (16.4). Reading this file alone should tell you everything needed to call the module, without opening main.tf at all. modules/security/variables.tf, for example, says \"give me a vpc_id; I'll assume port 80 unless you override ingress_port.\" main.tf The actual resource/data blocks — the implementation. A caller never needs to read this; it's free to change internally as long as variables.tf and outputs.tf stay the same contract. outputs.tf The module's return values — whatever the next module in the chain needs back. modules/network/outputs.tf hands back vpc_id and both subnet maps because security, compute, and loadbalancing all need those downstream. Root main.tf contains zero resource blocksOnce split, the root main.tf is just four module calls passing outputs forward — module.network.vpc_id into security, both of those into compute, all three into loadbalancing. Every actual resource lives one level down, inside a module. This is what \"the root just wires things together\" looks like in a real file, not just as a description. One resource, before and after Splitting into modules was also a chance to apply 16.1's for_each guidance retroactively. 18.8.1's four separately-named subnet resources became one for_each block over a map: ## Before (18.8.1) -- four separate resources resource \"aws_subnet\" \"fmk_public_1a\" { cidr_block = \"11.0.1.0/24\" ... } resource \"aws_subnet\" \"fmk_public_1b\" { cidr_block = \"11.0.2.0/24\" ... } # state addresses: aws_subnet.fmk_public_1a, aws_subnet.fmk_public_1b ## After (day02-modules) -- one resource, driven by the caller's map resource \"aws_subnet\" \"public\" { for_each = var.public_subnets cidr_block = each.value.cidr_block availability_zone = each.value.availability_zone } # state addresses: module.network.aws_subnet.public[\"1a\"], [\"1b\"] Same two subnets get created either way — terraform plan on the modularized version still shows 28 to add, 0 to change, 0 to destroy, identical to 18.8.1. What changed is that adding a third AZ later means adding one entry to the public_subnets map passed into the module, not writing a fifth resource block by hand."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-9","title":"AWS Lambda: IAM, Packaging & the S3 Deployment Pattern","body":"A Lambda function needs three things before it can run at all: an IAM role it assumes, permission to actually write logs, and its code packaged into a deployment artifact. None of these are optional — skip the role and the function has no identity to run as; skip the logging permission and it runs but you can never see why it failed. # 1. The role Lambda itself assumes when invoking the function resource \"aws_iam_role\" \"lambda_role\" { name = \"tf-aws-lambda-role\" assume_role_policy = jsonencode({ Version = \"2012-10-17\" Statement = [{ Action = \"sts:AssumeRole\" Effect = \"Allow\" Principal = { Service = \"lambda.amazonaws.com\" } }] }) } # 2. What that role is actually allowed to do -- here, just write logs resource \"aws_iam_policy\" \"iam_policy_for_lambda\" { name = \"aws_iam_policy_for_tf_aws_lambda_role\" policy = jsonencode({ Version = \"2012-10-17\" Statement = [{ Action = [\"logs:CreateLogGroup\", \"logs:CreateLogStream\", \"logs:PutLogEvents\"] Resource = \"arn:aws:logs:*:*:*\" Effect = \"Allow\" }] }) } # 3. Attach the policy to the role -- two separate resources, same pattern as # every other IAM role+policy pairing (16.4's \"declare the parent, then the membership\") resource \"aws_iam_role_policy_attachment\" \"attach_iam_policy_to_role\" { role = aws_iam_role.lambda_role.name policy_arn = aws_iam_policy.iam_policy_for_lambda.arn } # 4. Zip the function code -- a data source, not a resource: it reads # local files and produces an archive, it doesn't create anything in AWS data \"archive_file\" \"zip_the_lambda_code\" { type = \"zip\" source_dir = \"${path.module}/lambda\" output_path = \"${path.module}/lambda/lambda-func.zip\" } # 5. The function itself, uploaded directly resource \"aws_lambda_function\" \"tf_lambda_func\" { filename = data.archive_file.zip_the_lambda_code.output_path function_name = \"fmk-tf-lambda-function\" role = aws_iam_role.lambda_role.arn handler = \"main.lambda_handler\" runtime = \"python3.10\" depends_on = [aws_iam_role_policy_attachment.attach_iam_policy_to_role] } jsonencode() over a raw heredocBoth work, but jsonencode({...}) builds the JSON from real HCL values — Terraform catches a typo'd key or a missing comma at plan time, the same way validation blocks (16.4) catch mistakes before apply does. A policy document written as a <<EOF heredoc string is just text as far as Terraform's concerned — a malformed JSON heredoc doesn't fail until AWS itself rejects the policy. depends_on earns its place hereNothing about aws_lambda_function references the policy attachment directly, so Terraform can't infer the ordering from a value reference (15.3) the way it does for role = aws_iam_role.lambda_role.arn. Without the explicit depends_on, the function could attempt to create before the role actually has its logging permission attached, and either fail outright or succeed with a role that briefly can't write logs. Large functions: deploying from S3 instead of a direct upload filename above uploads the zip directly as part of the CreateFunction/UpdateFunctionCode API call. That has a hard ceiling: AWS caps a direct deployment package at 50 MB zipped. Past that, or as a matter of course in most real pipelines regardless of size, the function's code is uploaded to S3 first, and the Lambda resource just points at where it landed: resource \"aws_s3_object\" \"lambda_package\" { bucket = \"fmk-lambda-deployments\" key = \"tf-aws-lambda/lambda-func.zip\" source = data.archive_file.zip_the_lambda_code.output_path etag = data.archive_file.zip_the_lambda_code.output_md5 } resource \"aws_lambda_function\" \"tf_lambda_func\" { s3_bucket = aws_s3_object.lambda_package.bucket s3_key = aws_s3_object.lambda_package.key function_name = \"fmk-tf-lambda-function\" role = aws_iam_role.lambda_role.arn handler = \"main.lambda_handler\" runtime = \"python3.10\" depends_on = [aws_iam_role_policy_attachment.attach_iam_policy_to_role] } Why real pipelines use this even under 50 MBBeyond the size ceiling, S3-based deployment means the artifact exists as a named, addressable object — a CI pipeline can build the zip once, upload it, and reference the exact same S3 object from multiple environments (dev/stg/prod) without re-zipping or re-uploading. etag = data.archive_file...output_md5 is what makes Terraform notice the code actually changed: without it, updating the local zip wouldn't trigger a new aws_s3_object upload at all, and the function would silently keep running the old code. 250 MB unzipped is still a hard ceiling either wayS3-based deployment raises the limit, it doesn't remove it — AWS caps the unzipped deployment package (code plus every dependency) at 250 MB regardless of upload method. A function approaching that size is usually a sign it's bundling more than it needs (16.6's over-abstraction warning, applied to a dependency tree instead of a module) — worth checking whether a Lambda Layer, or splitting into more than one function, fits better than pushing the ceiling."},{"page":"week4.html","pageLabel":"Week 4","id":"d18-10","title":"Provisioning a Jenkins Controller","body":"A Jenkins controller (week5.html Day 20) is itself just a resource — an EC2 instance, a security group, and a user_data script that installs and starts it, the same shape as every other compute resource in this file. #!/bin/bash dnf update -y dnf install -y java-17-amazon-corretto wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io-2023.key dnf install -y jenkins systemctl enable --now jenkins dnf, not apt-get — same lesson as 18.8.1This script assumes an Amazon Linux AMI, matching the exact AMI-family reasoning already covered for user_data scripts in general: the package manager and package names have to match the actual OS the instance boots from, not whatever was copied from a different example. resource \"aws_ebs_volume\" \"jenkins_home\" { availability_zone = aws_instance.jenkins_controller.availability_zone size = 50 tags = { Name = \"jenkins-home\" } } resource \"aws_volume_attachment\" \"jenkins_home\" { device_name = \"/dev/xvdf\" volume_id = aws_ebs_volume.jenkins_home.id instance_id = aws_instance.jenkins_controller.id } JENKINS_HOME does not belong on the root volumeEvery job config, build history, and credential Jenkins holds lives under JENKINS_HOME (/var/lib/jenkins by default). Mounting it on a separate EBS volume, not the instance's root volume, means replacing the controller instance itself — a new AMI, a resize, a region move — doesn't threaten that data at all: detach the volume, attach it to the new instance, done. Same principle as 19.7's prevent_destroy on a critical resource — the data that actually matters shouldn't share a lifecycle with the compute that happens to be running it this week."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-1","title":"Drift Detection","body":"Drift is state disagreeing with reality — someone widened a security group rule in the console during an incident, or an ASG's desired_capacity moved because a scaling policy fired (17.1 introduced the concept; this is catching it in practice). # Refresh Terraform's view of reality and show any drift, without touching anything terraform plan -refresh-only # Older/equivalent: refresh state, then a normal plan will show the same drift terraform apply -refresh-only # aws_security_group.app has changed ~ resource \"aws_security_group\" \"app\" { ~ ingress = [ + { + cidr_blocks = [\"0.0.0.0/0\"] + from_port = 22 + to_port = 22 + protocol = \"tcp\" }, ] } This is a refresh-only plan, so Terraform will not take any actions. Schedule it, don't wait to trip over itA weekly plan -refresh-only run in CI, posting its output somewhere visible, catches console-made changes within a week instead of at the next unrelated apply, when the drifted resource shows up mixed in with changes nobody intended to make. Detecting drift isn't fixing it-refresh-only only updates what Terraform believes state to be — it does not push your config back onto AWS, and it does not pull the drifted value into your .tf files either. Deciding whether the console change or the code is correct is still a human call every time."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-2","title":"Targeted Apply, and When It's a Mistake","body":"# Apply changes to just this one resource, ignoring other pending diffs terraform apply -target=aws_launch_template.app Legitimate use: an emergency, isolated fixProduction is down because of one resource, the fix is known, and a full plan right now would also apply three unrelated in-flight changes nobody's reviewed yet. -target applies only the fix. The mistake: using it as a habitRepeated -target applies let a config and its state quietly drift apart from each other in a different way than 19.1 — not AWS vs. state, but \"the parts of config that get applied\" vs. \"the parts that don't.\" The next full, untargeted apply can produce a surprisingly large plan, because everything skipped over every targeted run finally shows up at once. Terraform itself warns about this every time -target is used — treat that warning as accurate, not boilerplate."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-3","title":"moved Blocks","body":"Renaming a resource in code, or moving it into a module, changes its address (aws_instance.app → module.compute.aws_instance.app). Terraform matches state by address, not by intent — without help, it reads that as \"the old one was deleted, a new one must be created,\" and plans a destroy-and-recreate of something that never actually changed on AWS. # after moving aws_instance.app into a module, add this alongside it moved { from = aws_instance.app to = module.compute.aws_instance.app } $ terraform plan Note: Objects have changed outside of Terraform # aws_instance.app has moved to module.compute.aws_instance.app resource \"aws_instance\" \"app\" { id = \"i-0aaa1111aaaa11111\" } No changes. Your infrastructure matches the configuration. This is exactly what 16.6's repo layout refactor needsExtracting an inline resource into a module — the \"third time\" moment from 16.6's rule of three — is precisely when a moved block matters: it lets the refactor be a no-op against real infrastructure, instead of an accidental destroy-and-recreate of something already running in production. This happened for real, on this exact mistakeSwitching an aws_iam_user resource from count to for_each — without a moved block — produced a plan reading Plan: 4 to add, 0 to change, 3 to destroy. Typing yes anyway destroyed three real IAM users, then failed to recreate two of them (a race between the parallel destroy and create for the same username). The fix was mechanical, not scary: since the destroyed users genuinely no longer existed, a follow-up apply recreated them cleanly with no conflict. The lesson is the plan line itself — count → for_each always shows as unrelated destroy+create unless a moved block (or state mv, 19.4) tells Terraform the two addresses are the same object — and \"3 to destroy\" in a plan is worth stopping on every single time, not just reading past."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-4","title":"state mv and state rm","body":"moved blocks (19.3) are the modern, declarative way to record a rename — they live in code and survive a fresh checkout. terraform state mv does the same rewrite imperatively, once, from the CLI — useful when you need it done immediately and won't remember (or won't bother) to leave a moved block behind. # Same effect as the moved block in 19.3, done directly against the state file terraform state mv aws_instance.app module.compute.aws_instance.app # Remove a resource from Terraform's management without destroying it in AWS -- # for handing a resource off to another team's config, or before a manual takeover terraform state rm aws_instance.legacy_bastion # List what's actually in state right now terraform state list state rm doesn't delete anything in AWSIt only removes the resource from Terraform's bookkeeping — the real EC2 instance keeps running, untouched. The easy mistake is expecting state rm to be a lightweight destroy; it's the opposite, a way to stop managing something while leaving it exactly as it is."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-5","title":"Recovering a Half-Applied State","body":"An apply can be interrupted mid-run — a killed terminal, a lost network connection, a lock timeout — after some resources succeeded and before others did. State reflects whatever finished before the interruption; nothing about it is inherently corrupt, but it needs deliberate steps to get back to normal, not blind re-running. # 1. If a lock was left behind by the interrupted run, confirm nothing else is actually applying, then: terraform force-unlock <LOCK_ID> # 2. Reconcile state with what's actually on AWS before touching anything else terraform plan -refresh-only # 3. A normal plan now shows only the resources still pending from the interrupted apply -- # read it like any other plan before applying terraform plan terraform apply force-unlock is a last resort, not a first reactionRun it while the original process might still be applying, and two applies now race against the same state with no lock protecting either — the exact failure mode locking (17.2) exists to prevent. Confirm the original process is actually dead (check who holds the lock, ask the team) before force-unlocking. Half-applied is recoverable precisely because state is incrementalEach resource is recorded as it succeeds, not all-or-nothing at the end — that's why plan after an interruption shows only what's left to do, rather than an all-or-nothing do-over of the entire config."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-6","title":"Machine-Readable Plans for Automation","body":"The human-readable plan output from 15.5 is for a person reading a terminal. A CI pipeline (Week 5) needs to act on a plan programmatically — post it as a PR comment, gate an approval on whether anything destructive is in it — which means parsing it as structured data instead of text. # Save the plan to a binary file, then export it as JSON terraform plan -out=tfplan.binary terraform show -json tfplan.binary > tfplan.json # Pull out just the destructive actions -- exactly the check Week 5's # approval gate runs before anyone clicks \"approve\" jq '.resource_changes[] | select(.change.actions | index(\"delete\"))' tfplan.json Why -out matters hereterraform show -json without a saved plan file re-runs plan against current state — which can differ from the plan a human or a pipeline already reviewed, if anything changed in between. Saving with -out and applying that exact file (terraform apply tfplan.binary) guarantees the plan that was reviewed is the plan that gets applied, with nothing able to slip in between the two. This is the actual mechanism behind Week 5's PR gate\"terraform plan posted as a comment\" and \"the approval gate\" (week5.html Day 21) are both built on exactly this JSON output — a script reads tfplan.json, formats a summary, and posts it; the approval step re-checks it for anything destructive before allowing apply to run the saved tfplan.binary."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-7","title":"Importing Existing Resources","body":"Something created by hand in the console — or by anyone else, outside Terraform entirely — has no entry in state. Terraform doesn't know it exists, so plan/apply assumes it needs to be created, and AWS rejects the create because the real thing is already there: │ Error: creating IAM User (nsl-media-vault-app-s3-access): operation error IAM: CreateUser, │ https response error StatusCode: 409, EntityAlreadyExists: User with name │ nsl-media-vault-app-s3-access already exists. terraform import fixes this — and only this. It does not create, modify, or delete anything in AWS; it just writes an entry into the state file saying \"this resource address already corresponds to this real object.\" # Syntax: terraform import <resource address> <the resource's real ID> terraform import 'aws_iam_user.example[\"nsl-media-vault-app-s3-access\"]' nsl-media-vault-app-s3-access The ID on the right isn't universalWhat counts as \"the ID\" is resource-type-specific — for aws_iam_user it's the username, for aws_instance it's the instance ID (i-0aaa1111aaaa11111), for aws_s3_bucket it's the bucket name. Always check the specific resource's page on the Terraform Registry — it documents the exact import ID format, usually right at the bottom of the page. After importing, run plan — it should show 0 to change if your config's arguments already match the real object's actual settings. If they don't match, plan shows exactly what it would change to bring the real object in line with your code — read that like any other plan before applying. Importing into a count index is fragileImporting into aws_iam_user.example[3] ties the import to a position in a list (16.1's gotcha, again). If that list ever reorders, index 3 can silently point at a different resource next apply. Importing into a for_each-keyed address instead — aws_iam_user.example[\"nsl-media-vault-app-s3-access\"], as above — ties the import to a stable name that can't shift underneath it. Worked example: importing a resource that must never be destroyed Bringing a genuinely important resource under Terraform's control — a long-running production instance, not a throwaway learning one — deserves an extra step before anything else touches it. resource \"aws_instance\" \"critical_app\" { ami = \"ami-0333333333333333\" instance_type = \"t3.micro\" tags = { Name = \"critical-app\" } lifecycle { prevent_destroy = true # add this in the same edit as the import, not after } } terraform import aws_instance.critical_app i-0aaa1111aaaa11111 terraform plan # confirm 0 to change, or read exactly what doesn't match yet With prevent_destroy already in place, a routine, safe change — updating tags, say — applies normally: # aws_instance.critical_app will be updated in-place ~ resource \"aws_instance\" \"critical_app\" { id = \"i-0aaa1111aaaa11111\" ~ tags = { \"Name\" = \"critical-app\" + \"Owner\" = \"fahad\" } } Plan: 0 to add, 1 to change, 0 to destroy. prevent_destroy is the actual safety net hereBefore this instance was imported, nothing about it could be \"destroyed by Terraform\" — Terraform didn't know it existed. The moment it's imported, that protection disappears unless something replaces it — which is exactly prevent_destroy's job. Add it in the same edit as the import itself, not as a follow-up step: any change that would force replacement (18.8.1's -/+ from 15.5) now fails outright with an error instead of silently destroying and recreating a resource that was never meant to be disposable."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-8","title":"Debug Logging: TF_LOG and TF_LOG_PATH","body":"When a plan or apply fails with an error too vague to act on, Terraform's own internal logging — normally silent — can be turned on to see exactly what it's doing: every provider RPC call, every HTTP request to AWS, every internal decision. export TF_LOG=DEBUG # TRACE, DEBUG, INFO, WARN, or ERROR -- TRACE is the noisiest terraform plan # unset when done -- it stays exported for every future command in this shell otherwise unset TF_LOG TRACE vs DEBUGTRACE is the most verbose level — it includes the exact HTTP requests and responses between the provider and AWS's API, useful for tracking down a provider bug or an unexpected API response. DEBUG is one level down: Terraform's own internal decisions and provider RPC calls, usually enough for \"why did it plan to do that\" without the sheer volume of raw HTTP traffic. Left on, TF_LOG prints directly to the terminal, mixed in with normal plan/apply output — usable for a quick look, unmanageable for anything longer. TF_LOG_PATH redirects it to a file instead: export TF_LOG=DEBUG export TF_LOG_PATH=\"/home/fahad/logs/debug.log\" terraform apply The directory has to already existTerraform does not create missing directories in TF_LOG_PATH — pointing it at /home/fahad/logs/debug.log when /home/fahad/logs/ doesn't exist yet fails to write the log at all (checked directly: a fresh logs/ directory genuinely doesn't exist until something creates it). mkdir -p the directory first, the same requirement as any other tool writing to a path you haven't set up yet. The log file is appended to, not overwrittenEvery subsequent command with the same TF_LOG_PATH set keeps adding to the same file rather than starting fresh — a log from an hour ago and one from just now sit in the same file, back to back. Delete or rotate it manually between debugging sessions, or it grows indefinitely and makes finding the relevant run harder each time. Unset both when doneBoth variables persist for every Terraform command run in that shell session afterward, not just the one that needed debugging — exactly the same \"stray environment variable\" class of surprise as 15.2's credential-precedence gotcha. A stray TF_LOG=TRACE left set makes every future plan print pages of noise nobody asked for; unset TF_LOG TF_LOG_PATH (or just open a fresh terminal) once the debugging is done avoids that."},{"page":"week4.html","pageLabel":"Week 4","id":"d19-9","title":"Beyond the Checkpoint: Terraform Associate Certification","body":"An external, formal validation of everything Week 4 covers — HashiCorp's own Terraform Associate (004) exam, current as of late 2026 (the prior 003 version retired January 2026). Format1 hour, online proctored, ~57 questions (multiple choice / multiple answer / true-false) Cost~$70.50 USD + local taxes Validity2 years PrerequisitesNone Tests againstTerraform 1.12 The 8 official objectives Infrastructure as Code with Terraform — what IaC is, its advantages, multi-cloud/hybrid/service-agnostic workflows Terraform Fundamentals — installing/versioning providers, multi-provider configs, how state is used (15.2, 17.1) Core Terraform Workflow — init, validate, plan, apply, destroy, fmt (15.4, 15.5) Terraform Configuration — resource vs data, cross-resource references, variables/outputs, complex types, expressions/functions, resource dependencies, custom validation conditions, sensitive data handling including Vault (15.3, 15.6, 16.1, 16.4, 16.10) Terraform Modules — sourcing, variable scope, using, versioning (16.4, 16.5, 16.6, 18.8.2) Terraform State Management — local backend, state locking, remote state via backend, drift (17.1, 17.2, 19.1) Maintain Infrastructure with Terraform — import, inspecting state via the CLI, verbose logging (19.4, 19.7, 19.8) HCP Terraform — creating infrastructure via HCP Terraform, collaboration/governance features, workspaces/projects, integrations Objectives 1–7: already in strong shapeEvery objective above except the last has a real, hands-on match somewhere in this week — not just reading about the concept, but having hit its actual failure modes against a real account (drift, a corrupted lock, a botched import, a missing depends_on). That's a meaningfully stronger position than most exam preparation, which is usually reading-only. Objective 8 (HCP Terraform) is the one clean gapNothing in this project has touched HCP Terraform specifically — its workspaces/projects, collaboration and governance features, or run-integration model. That's roughly one-eighth of the exam, covering a genuinely different product surface (a hosted service, not the CLI/provider mechanics everything else here is built on) — worth deliberate, separate study before sitting the exam, rather than assuming it's covered by the remote-backend/state concepts already learned."},{"page":"week4.html","pageLabel":"Week 4","id":"d15","title":"From Clicks to Code","body":"Everything in Weeks 1–3 was built by clicking in the AWS console or typing one-off aws CLI commands. Neither leaves behind a record of the intended state of the infrastructure — only whatever state it happens to be in right now. Terraform's job is to make that intended state a file you can read, diff, review and re-apply."},{"page":"week4.html","pageLabel":"Week 4","id":"d16","title":"Iteration and Modules","body":"The language features that shape everything downstream, then packaging it for reuse."},{"page":"week4.html","pageLabel":"Week 4","id":"d17","title":"State, Environments and Regions","body":"What state actually holds, and how one codebase serves multiple environments and regions safely."},{"page":"week4.html","pageLabel":"Week 4","id":"d18","title":"The Whole Platform in Code","body":"Every resource from Weeks 1–3, declared. Nothing new conceptually — this is Day 15–17's language and workflow, applied to the actual shape of the platform."},{"page":"week4.html","pageLabel":"Week 4","id":"d19","title":"Operating Terraform, Checkpoint 2","body":"Keeping state healthy once real changes start landing, then the checkpoint. Checkpoint 2Given a diagram, produce working, readable, reproducible code inside the session, and perform state operations without notes — then hand over the repository."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-1","title":"Controller & Agent Model","body":"Jenkins splits into two roles. The controller (formerly \"master\") serves the web UI, stores every job's configuration, schedules builds, and holds the plugin/credential state — it orchestrates, it doesn't build. An agent (formerly \"slave\"/\"node\") is a separate machine or container that connects to the controller and actually runs the pipeline's steps. Never build directly on the controllerThe controller has no isolation from what runs on it — a build that exhausts memory, hangs, or gets compromised takes down the one thing coordinating every other job too. Agents exist specifically to keep build workloads (arbitrary code, effectively) away from the process that everything else depends on staying up. An agent advertises labels — arbitrary tags like linux, docker, or gpu — and a pipeline's agent directive picks one by label, the same way a Kubernetes pod picks a node by nodeSelector: pipeline { agent { label 'linux && docker' } stages { stage('Build') { steps { sh 'make build' } } } } Why not just one big controllerSeparate agents mean a Windows build and a Linux build can run on the same Jenkins instance without one machine needing both toolchains installed, and adding build capacity is \"add another agent,\" not \"resize the box everything else depends on.\" The controller stays a stable, mostly-idle coordination point regardless of how much build traffic the team generates. Why agents exist, beyond just isolation ReasonWhat it actually buys IsolationArbitrary, untrusted build code never runs on the one process everything else depends on (the fact-fail above). Horizontal scalingMore build capacity is \"provision another agent,\" not \"give the controller a bigger instance\" — capacity and coordination scale independently. Environment matchingA label picks the toolchain a build actually needs — a specific OS, a GPU, a licensed tool only installed on one machine — instead of forcing every build to fit whatever the controller happens to have. ParallelismMultiple agents mean multiple builds genuinely running at once, not queued behind each other on a single executor. Network/resource proximityAn agent can live inside the same VPC, region, or on-prem network as whatever it needs to reach fast — an internal artifact repo, a private database, a specific compliance boundary — without routing the controller itself into that network. Team/security segregationFolder-scoped agents (20.8) let one team's builds run on infrastructure another team can't touch, without needing a separate Jenkins instance per team. Types of agents TypeHow it connects / how long it lives Static (permanent) agentAdded once by hand via Manage Jenkins → Nodes and left running indefinitely — one real, named machine (20.3's EC2 example). Simple, but it's idle capacity paid for even when no build is using it. SSH (outbound) agentThe controller initiates the connection, over SSH, to a host it already knows the address of (20.3). Needs the agent host reachable from the controller, not the other way around. Inbound (JNLP/WebSocket) agentThe agent initiates the connection to the controller instead — the fix when the agent sits behind NAT or a firewall the controller can't reach inbound, e.g. a build machine on a laptop or in a restricted network. Cloud / ephemeral agentProvisioned on demand by a plugin (EC2 Fleet, Docker, Kubernetes) when a build needs one, then torn down afterward. No idle cost, and every build starts from a known-clean environment instead of accumulating drift on a long-lived machine. Docker agentDeclared directly in a Jenkinsfile — agent { docker { image 'node:20' } } — a fresh, disposable container per build or per stage, with the exact toolchain version pinned in code instead of installed once on a static machine. Built-in nodeThe controller's own, hidden executor pool — technically able to run builds itself, which is exactly what the fact-fail above says not to rely on. Usually set to 0 executors deliberately on any real controller."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-2","title":"Installing Jenkins & First-Time Setup","body":"Before anything gets provisioned as code (20.4), it's worth knowing what \"installing Jenkins\" actually involves by hand — the same principle as week4.html starting from ssh-keygen and a plain resource block before ever reaching a module. Jenkins is a Java application; everything below assumes Java is present first. MethodCommandGood for Native package (Amazon Linux/RHEL)dnf install jenkins, after adding the Jenkins repoA long-lived controller on a real instance — this project's approach (20.4) Native package (Debian/Ubuntu)apt-get install jenkins, after adding the Jenkins apt repo and signing keySame idea, different AMI family — the exact dnf/apt-get distinction from 18.8.1, applied to Jenkins itself now Dockerdocker run -p 8080:8080 -v jenkins_home:/var/jenkins_home jenkins/jenkins:ltsTrying Jenkins locally, or running it as a container on ECS/EKS instead of a bare EC2 instance WAR filejava -jar jenkins.warQuick, throwaway testing — nothing installed system-wide, nothing left behind but the process #!/bin/bash # Amazon Linux / RHEL family dnf install -y java-17-amazon-corretto wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io-2023.key dnf install -y jenkins systemctl enable --now jenkins #!/bin/bash # Debian / Ubuntu family apt-get update apt-get install -y fontconfig openjdk-17-jre wget -O /usr/share/keyrings/jenkins-keyring.asc https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key echo \"deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian-stable binary/\" \\ | tee /etc/apt/sources.list.d/jenkins.list apt-get update apt-get install -y jenkins Java version is a real compatibility constraintRecent Jenkins releases require Java 17 or 21 — an older Java 11 install fails to even start the service, with the actual reason buried in systemctl status jenkins or /var/log/jenkins/jenkins.log rather than shown anywhere obvious. Check the specific Jenkins version's requirements before assuming whatever Java happens to already be on an AMI is new enough. First-time setup wizard Jenkins starts on port 8080 by default and, on first boot, is locked behind a one-time password only readable from the server itself: cat /var/lib/jenkins/secrets/initialAdminPassword Unlock Jenkins Paste that password into the setup wizard at http://<controller-ip>:8080 — proves whoever's completing setup actually has access to the server itself, not just the network port. Install plugins \"Install suggested plugins\" is the reasonable default for a first controller — a curated set covering Git, pipelines, and the essentials. \"Select plugins to install\" is the deliberate route once 20.5's plugin-hygiene principle actually matters — smaller footprint, chosen instead of inherited. Create the first admin user Skipping this and continuing as the default admin account is explicitly offered but shouldn't be taken for anything beyond a five-minute local test — the same reasoning as never leaving a database on its default credentials. Instance configuration Confirms the URL Jenkins believes it's reachable at — matters later for webhook payload URLs (20.10) and any notification that links back to a build, since a wrong URL here means links that go nowhere. Operating Jenkins as a systemd service The native package installs Jenkins as a real systemd unit, not just a process someone starts by hand — the same start/stop/status vocabulary as any other Linux service applies directly: systemctl start jenkins # start it now systemctl stop jenkins # stop it now systemctl restart jenkins # e.g. after a config or plugin change that needs a restart systemctl status jenkins # is it actually running, and since when systemctl enable jenkins # survive a reboot -- already done by --now during install journalctl -u jenkins -f # follow the service's own logs live Two files control how the service actually behaves, separate from anything configured in the Jenkins UI itself: /etc/sysconfig/jenkins (RHEL family) / /etc/default/jenkins (Debian family) Environment variables read by the systemd unit before Jenkins even starts — JENKINS_PORT (default 8080), JENKINS_HOME (where job data actually lives), JENKINS_USER (which OS user the process runs as). Changing the port here, not in the Jenkins UI, is what actually takes effect — the UI has no setting for its own listen port. JAVA_OPTS / JENKINS_JAVA_OPTIONS JVM flags passed to the underlying Java process — heap size (-Xmx2g), most commonly, since Jenkins is a long-running JVM and the default heap can be too small for a controller running many jobs. A config file change needs a restart, not a reloadNeither of the files above is watched live — editing JENKINS_PORT or JAVA_OPTS and expecting it to take effect immediately is the same mistake as expecting a changed environment variable to reach an already-running process anywhere else. systemctl restart jenkins is what actually applies it. Verifying it actually workedsystemctl status jenkins showing active (running), plus the setup wizard actually loading in a browser on port 8080, is the full confirmation — no separate health check exists beyond \"does the service report running and does the UI respond.\" Provisioning this with Terraform lives in the Terraform weekWrapping this exact install script into an aws_instance's user_data, with JENKINS_HOME on its own EBS volume for durability, is covered in week4.html 18.10 — that's fundamentally a Terraform/infrastructure topic (provisioning compute), not a Jenkins-specific one, so it lives with the rest of Week 4's real resource patterns rather than here."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-3","title":"Connecting an Agent Node via SSH","body":"A controller with no agents attached still has nowhere to actually run a build (20.1). Manage Jenkins → Nodes → New Node creates one — \"Permanent Agent\" for a real, long-lived machine like an EC2 instance, as opposed to a cloud-provisioned, one-build-and-gone agent. The SSH launch method needs nothing pre-installed on the target beyond SSH access and a working Java — the controller pushes its own agent jar over the connection itself. A permanent agent node's Configure screen — SSH launch method, pointed at one EC2 instance Name The node's identity inside Jenkins — shown in build history and node listings, independent of the label used to target it. Remote root directory Where the agent jar and every job's workspace get written on the remote machine — /home/ec2-user above, which is also why the earlier pipeline output showed Running on agent-one in /home/ec2-user/workspace/CICD. Labels What a pipeline's agent directive (20.1) actually matches against — set to agent-one here, matching the node's own name, but the two are independent; a label can be shared across several nodes, or differ from the node's name entirely. Usage \"Use this node as much as possible\" lets the controller schedule any matching job here; the alternative, \"Only build jobs with label expressions matching this node,\" reserves it for jobs that explicitly ask for it and nothing else. Launch method: Launch agents via SSH Host and Credentials (an SSH keypair, added to Jenkins' credentials store — 20.8) are all this method needs. Jenkins opens the connection itself on save, or whenever the node is manually relaunched. What actually happens when it connects [SSH] Opening SSH connection to <agent-host>:22. [SSH] Authentication successful. [SSH] Starting sftp client. [SSH] Copying latest remoting.jar... [SSH] Starting agent process: cd \"/home/ec2-user\" && java -jar remoting.jar -workDir /home/ec2-user Agent successfully connected and online Underneath the UI, this is exactly the SSH-file-transfer-plus-remote-command pattern from week4.html's own ssh-keygen/scp fundamentals — Jenkins just automates it: connect, copy its agent jar over SFTP, then run it with java -jar on the other end. \"java: command not found\" here means Java is missing, not Jenkins misconfiguredThe SSH connection and the SFTP copy can both succeed — the log shows authentication working and remoting.jar transferring cleanly — and the launch can still fail at the very last step, bash: line 1: java: command not found / Agent JVM has terminated. Exit code=127, because there's simply no java on the agent host's PATH. A common real cause: a boot-time install script (this project's own 20.2/week4.html 18.10 pattern) that ran dnf install/yum install without -y — harmless interactively, but silently skipped during a non-interactive user_data boot with no terminal to answer its confirmation prompt. Fix on the agent host directly (install Java, or relaunch the instance with the corrected script) — nothing about this is a Jenkins-side problem. Referencing the node from a pipeline pipeline { agent { label 'agent-one' } stages { stage('Hello') { steps { echo 'Hello World' } } } } An unquoted label is Groovy math, not a stringagent { label agent-one } — no quotes — doesn't fail because of anything wrong with the label. Groovy parses the bareword agent-one as the expression agent - one, subtracting two undefined variables, which throws groovy.lang.MissingPropertyException: No such property: agent from inside Jenkins' own ModelInterpreter.inDeclarativeAgent — a genuinely confusing error for what's really just a missing pair of quotes. label 'agent-one' is the fix, same as quoting any other Groovy string. \"Still waiting to schedule task ‘agent-one’ is offline\"A pipeline whose agent label matches only a currently-disconnected node queues indefinitely — there's no default timeout. Manage Jenkins → Nodes → that node shows its live status and log; clicking Launch agent retries the SSH connection without touching the pipeline itself, and an already-queued build picks up automatically the moment the node comes back online. On a memory-constrained instance (a t3.micro's 1GB strained by a JVM plus the remoting process), a node can connect successfully and then drop again shortly after — dmesg | grep -i kill on the agent host is worth checking for an OOM kill if that keeps happening. A node that's \"online\" but still fails every build Two more failure modes worth knowing, both diagnosed on this same real agent — neither one looks like a connection problem at first, and neither one is fixed by the \"Launch agent\" click above. A fix applied after the agent connected doesn't apply until it reconnectsAdding ec2-user to the docker group (usermod -aG docker) fixes the account immediately — a brand-new SSH login, or even id ec2-user, shows it right away. But Linux fixes a process's supplementary groups at the moment it's created, not continuously — the agent's already-running remoting.jar process keeps failing with permission denied on /var/run/docker.sock forever, because it was forked before the fix existed. Confirmed here directly: ps -o etime= -p <pid> showed the process had been alive for 48 minutes, well before the group was added. The fix isn't re-running the build — it's forcing a new process: Manage Jenkins → Nodes → agent-one → Disconnect, wait for it to show offline, then Launch agent. Clicking \"Launch agent\" while Jenkins still considers the node connected is often a no-op, since Jenkins has no reason to kill a channel it doesn't believe is broken. The identical symptom, from a completely different cause, on a freshly-launched instanceThe same \"permission denied\" error can reappear on a brand-new instance too — even though usermod -aG docker is baked into user_data (20.2) and should have already run. The reason: user_data executes asynchronously in the background after boot, and SSH typically becomes reachable before it finishes — especially with a yum update -y running first, which alone can take over a minute. If Jenkins' agent connects and starts its process during that window, it's fixed at the pre-usermod state, for a boot-timing reason rather than a manual-fix-timing reason. Same fix either way: Disconnect, wait, Launch agent — by the time you're re-attempting, user_data has almost always finished. \"Disk space is below threshold\" when the disk isn't actually fullJenkins' built-in Free Temp Space monitor took this same agent offline with \"Only 456.59 MiB out of 456.62 MiB left on /tmp\" — which reads like a full disk, but df -h on the box told a different story: /tmp was a tmpfs (RAM-backed) at 0% used, completely empty, while the real root disk had gigabytes free. The catch: this t3.micro's /tmp is sized to roughly half its 1GB of RAM — a hard ceiling around 457MB that can never satisfy the monitor's default 1GiB-free requirement, no matter how empty it is. Not a leak, not real pressure — an instance-sizing mismatch against a monitor default. Fixed via the small gear/settings icon on the Manage Jenkins → Nodes list (or Manage Jenkins → System's \"Free Temp Space\" section): lower the threshold to something the instance can actually reach, e.g. 100MiB."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-4","title":"Job Types: What \"New Item\" Actually Offers","body":"Clicking New Item in Jenkins shows every kind of thing it can create, in one screen — worth knowing the full list before defaulting to whichever one a tutorial happened to use. An overview for now; each gets its own use-cases/pros-cons/step-by-step treatment later. Item typeWhat it's for PipelineBuild, test, and deploy using a Jenkinsfile (20.9). Supports stages, parallel work, and running steps across multiple agents. Freestyle projectThe classic, UI-configured job type — checks out from up to one SCM, runs build steps serially, then post-build actions like archiving artifacts or sending email. Predates Pipeline-as-code, and still its own CJE exam section (20.10). Multi-configuration projectOne job definition run across many combinations automatically — multiple OSes, multiple environments, multiple platform targets — without hand-creating a separate job per combination. FolderA real namespace, not just a filter — two items can share a name if they live in different folders. The same folder that scopes credentials and permissions in 20.5. Multibranch PipelineScans one repository and creates a Pipeline job per detected branch and pull request automatically (20.9) — no manually creating a job per branch. Organization FolderOne level up from Multibranch Pipeline — scans an entire GitHub org/Bitbucket project for repositories, and creates a Multibranch Pipeline for each one it finds. \"Duplicate an existing item\" isn't a typeIt's the seventh option on the same screen, but it's a shortcut action, not a job type of its own — it just copies an existing item's configuration into a new one, for any type above, as a starting point instead of configuring from scratch."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-5","title":"Pipeline Job Configuration: General, Triggers & the Script Definition","body":"Choosing \"Pipeline\" as the item type (20.4) opens this configuration screen — the same shape every Pipeline job shares, regardless of what its stages actually do. General OptionWhat it actually does Discard old buildsA retention policy — keep only the last N builds or the last N days. Without it, build history (20.8) grows forever. Do not allow concurrent buildsSerializes runs of this same job — a second trigger waits instead of running in parallel. For a job that touches shared state it can't safely share with itself. Do not allow the pipeline to resume if the controller restartsJenkins normally tries to resume an in-progress pipeline after a controller restart. Disabling this makes a restart abort the run instead — the right choice when a resumed run risks replaying a non-idempotent step (5.6/week2.html territory: retries are only safe when the operation tolerates repetition). GitHub projectJust a display link back to the repo's GitHub page — cosmetic, not functional. Pipeline speed/durability overrideTrades how often Jenkins persists pipeline state to disk against I/O overhead — safer and slower, or faster and more state lost if the controller crashes mid-run. Preserve stashes from completed buildsA stash (files passed between stages/agents) is normally discarded once the build finishes — this keeps it around afterward for inspection. This project is parameterizedThe UI equivalent of a Jenkinsfile's parameters {} block (20.10) — configured by clicking instead of by code. Throttle buildsRate-limits how often, or how many concurrently, this job (or a named category of jobs) can run — for protecting a shared resource several different jobs compete for. Concurrent-build prevention and throttling solve different problems\"Do not allow concurrent builds\" only stops this one job from overlapping with itself. \"Throttle builds\" limits a whole category of jobs against each other — useful when five unrelated jobs all hit the same rate-limited API and none of them individually looks like the problem. Triggers TriggerWhat starts the build Build after other projects are builtUpstream/downstream chaining — this job runs automatically once a named other job finishes. Build periodicallyA cron-syntax schedule — the same five-field format as week2.html 5.6's scheduled scaling policies, evaluated in the controller's own timezone. GitHub hook trigger for GITScm pollingThe real webhook (20.11) — GitHub pushes an event to Jenkins the moment something happens, no delay. Poll SCMThe older mechanism 20.11 contrasts against webhooks directly — also cron-syntax, but Jenkins checks the repo on a timer instead of being told immediately. Trigger builds remotelyAn authenticated URL with a token, callable by an external script or system to start a build without needing full Jenkins API credentials. The script definition itself Further down the same screen, \"Pipeline script\" is one of two ways to tell Jenkins what to actually run: pipeline { agent any stages { stage('Hello') { steps { echo 'Hello World' } } stage('Create Folder') { steps { sh \"mkdir -p devops\" } } stage('Bye') { steps { echo 'Bye' } } } } This is the opposite of what 20.9/20.10 recommend\"Pipeline script\" types the Groovy directly into this one field, saved only in Jenkins' own job configuration — invisible to git history, code review, or branch-per-Jenkinsfile discovery (20.11's Multibranch Pipeline). \"Pipeline script from SCM\" is the other option on the same dropdown — pointing at a Jenkinsfile checked into the repository, which is what every example in 20.9 onward assumes. Fine for a five-minute \"does Jenkins work at all\" test; the wrong choice for anything meant to last. Use Groovy Sandbox Checked by default — restricts which Java/Groovy APIs a script can call, since arbitrary unrestricted Groovy can do real damage running on the controller. Unchecking it requires an administrator to manually approve the script before it can run. Pipeline Syntax (link) A built-in generator that produces correct step syntax by filling out a form, instead of memorizing every step's exact arguments from documentation."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-6","title":"Real Pipeline Walkthrough: Git, Credentials & Docker","body":"Everything from 20.1–20.5 in one real, working pipeline: cloning a private repository with a scoped credential, checking out a specific branch, and building a Docker image from the result. Every failure quoted below actually happened, in that order, on a real agent. What the agent host needs installed first Neither git nor docker ships on a base Amazon Linux 2023 AMI — both have to be installed explicitly, in the same user_data script that installs Java (20.2/18.10): #!/bin/bash sudo yum update -y sudo yum install -y java-21-amazon-corretto git docker sudo systemctl enable --now docker sudo usermod -aG docker ec2-user Package names still don't transfer across distrosThe same lesson as 20.2's openjdk-21-jre: that name is Debian/apt's convention and doesn't exist on Amazon Linux. Docker has the identical trap — docker.io is the Debian/Ubuntu apt package; Amazon Linux 2023's yum/dnf repos just call it docker. Running yum install docker.io here fails immediately with \"No match for argument,\" the same failure shape as the Java naming mismatch, just one distro-naming gotcha away from a different tool. usermod -aG docker ec2-user adds the agent's user to the group that owns /var/run/docker.sock (normally root:docker, mode 660) — without it, every docker command fails with permission denied while trying to connect to the Docker daemon socket, even though the daemon itself is running fine. Group membership doesn't apply to an already-running processLinux fixes a process's supplementary groups at the moment it's created — editing /etc/group afterward doesn't retroactively update a process that's already running. If the Jenkins agent's java -jar remoting.jar process started before usermod -aG docker ran, that specific process keeps failing with \"permission denied\" on the docker socket forever, even though a brand-new SSH login to the same box (and even id ec2-user) correctly shows docker in its group list. The account is fixed; the already-running process isn't. Confirmed here by checking the process's own uptime (ps -o etime= -p <pid>) — 48 minutes old, well before the fix. The actual fix: kill the old process, don't just re-run the buildOn the node's own page (Manage Jenkins → Nodes → agent-one), click Disconnect first — this is easy to miss, and re-running a build without it just reuses the same stale, already-connected process. Only after it shows offline does Launch agent actually start a brand-new remoting.jar process, one that reads /etc/group fresh and correctly picks up the new membership. Authenticating to a private GitHub repository Cloning a public repo needs no credential at all — the earlier \"No credentials specified\" line in a build log is completely normal for those. A private repo fails that same anonymous clone outright, so a credential has to exist in Jenkins before the git step can succeed. 1. Generate a GitHub Personal Access Token, scoped narrowly GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token. Repository access: only the one repo needed. Permissions: Contents: Read-only — a clone needs nothing more. 2. Store it in Jenkins' credentials store, never in the pipeline script Manage Jenkins → Credentials → System → Global credentials → Add Credentials. Kind: Username with password. Username: the GitHub username. Password: the token. ID: a memorable string (github-pat below) referenced from the pipeline. 3. Reference it from the git step by that ID credentialsId: 'github-pat' — Jenkins resolves the actual token at checkout time and masks it out of console logs; the pipeline script itself never contains the secret value (20.8's credentials-store principle applies here directly). An SSH deploy key is the alternative: generate a keypair, add the public half under the repo's Settings → Deploy keys (read-only), add the private half to Jenkins as an \"SSH Username with private key\" credential, and use a git@github.com:... URL instead of HTTPS. More setup, but scoped to exactly one repo rather than an account-wide token — worth it across several private repos. The git step: url, credentialsId, branch — and beyond ParameterWhat it does urlThe remote to clone — HTTPS or SSH form, matching whichever credential type is in use. credentialsIdThe Jenkins credentials-store ID to authenticate with. Omitted entirely for a public repo, as the earlier \"No credentials specified\" log line showed. branchA plain branch name — slashes included, e.g. holding/ui, a perfectly normal git branch name — resolved against the remote. Also accepts a tag ref directly, e.g. refs/tags/v1.2.3, for building from a specific release point instead of a moving branch. changelog / pollBooleans controlling whether this checkout contributes to Jenkins' build-changelog UI and SCM-polling baseline — usually left at their defaults unless a job deliberately checks out more than one repository. After checkout, Jenkins exposes what it just cloned as environment variables — env.GIT_COMMIT (the full SHA) and env.GIT_BRANCH — usable straight in a later stage, e.g. tagging a Docker image by commit instead of only by build number: sh \"docker build -t investor-pro:${env.GIT_COMMIT.take(7)} .\" The full commit message isn't exposed as an env var, but a plain git log in a sh step captures it just as easily, once the repo is checked out: def msg = sh(script: \"git log -1 --pretty=%B\", returnStdout: true).trim() The pipeline, line by line The actual working pipeline this section is built from: pipeline{ agent {label 'agent-one'} stages{ stage(\"Code\"){ steps{ echo \"This is cloning the code\" git credentialsId: 'github-pat', url: 'https://github.com/Fahad-Md-Kamal/investor-pro.git', branch:\"holding/ui\" echo \"Code cloned successfully\" } } stage(\"Build\"){ steps{ echo \"This is building the code\" sh \"docker build -t investor-pro:${env.BUILD_NUMBER} -t investor-pro:latest .\" } } stage(\"Test\"){ steps{ echo \"This is testing the code\" } } stage(\"Deploy\"){ steps{ echo \"This is deploying the code\" } } } } pipeline { ... } The declarative-syntax root block (20.10) — everything inside it is Jenkins-managed structure, not arbitrary Groovy. agent {label 'agent-one'} Pins this entire pipeline to the one node advertising the label agent-one (20.1, 20.3) — every stage below runs on that same machine, in the same workspace. stages { ... } The container for every named phase of the build — purely organizational, holds one or more stage blocks. stage(\"Code\") { ... } One named phase, shown as its own box in Jenkins' pipeline visualization. The name is just a label — \"Code\" could as easily be \"Checkout\". steps { ... } The actual commands run inside a stage — every stage needs exactly one. echo \"...\" Prints a line to the build's console log — used here purely for readability while watching a run, no functional effect. git credentialsId: ..., url: ..., branch: ... The simplified checkout step (this section, above) — clones url at branch, authenticating with the named credential. sh \"...\" Runs a shell command on the agent, in the current stage's workspace — here, the actual docker build. ${env.BUILD_NUMBER} A Jenkins built-in environment variable — the current job's build number, incrementing every run. Used here as an image tag so every build produces a distinct, traceable image, alongside a floating latest tag for convenience. The Docker build itself docker build ... . uses . — the workspace root — as its build context, which works because the git step above clones directly into the workspace root rather than a subdirectory; the real Dockerfile at the repo's top level is found without needing a -f path. Two tags in one command (-t investor-pro:${env.BUILD_NUMBER} -t investor-pro:latest) apply both at once, from a single build — no need to build twice or run a separate docker tag. What actually running this looked likeCode stage: clone succeeds, credential accepted, correct branch checked out. Build stage: multi-stage apt-get/uv sync output streams through exactly as it would locally, image exported and tagged. Test and Deploy stages (still placeholder echos here) run last. Finished: SUCCESS — the same pipeline shape 20.10's declarative Jenkinsfile builds on next, just with real steps instead of placeholders."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-7","title":"Hardening the Deploy Stage: Frontend Builds, Dockerfile Gotchas & Cleanup","body":"20.6's pipeline reaches Finished: SUCCESS — but a green build is not the same as a working deployment. Everything below is a real gap between the two, found by actually opening the deployed URL rather than trusting the pipeline's own exit code. A stage can only have one steps block stage(\"Code\"){ steps{ /* clone */ } steps{ /* copy .env */ } // invalid -- a second steps block } \"Expected one steps block, but got 2\"A declarative stage allows exactly one steps block. This fails before a single step runs — Jenkins rejects the whole script at validation time, not partway through execution. The fix is just merging both step lists into the one block the stage is allowed to have. Building a frontend on the same agent that's already tight on resources A t3.micro agent (1GB RAM, an 8GB disk already shared with Jenkins itself and every Docker image built) is a genuinely hard place to run npm install and npm run build — this project hit disk pressure, and separately investigated (and rejected) nvm as the way to get Node onto the box at all. nvm installs Node for the wrong user, in a way Jenkins can't see anywayuser_data runs as root, not ec2-user — so curl ... | bash followed by nvm install 24 puts Node under /root/.nvm, invisible to the account Jenkins actually builds as. Fixing that user mismatch still wouldn't be enough: nvm works by having an interactive shell's ~/.bashrc source nvm.sh and rewrite PATH — Jenkins' SSH-launched agent (20.3) and every pipeline sh step run a non-interactive, non-login shell, which never sources ~/.bashrc. The same shape of bug as the docker group issue below: works fine when you SSH in by hand, silently doesn't apply in the actual automated context. A distro package (NodeSource's yum/dnf repo) avoids both problems at once — the binaries land on the system PATH for every user and every shell type, no sourcing required. The fix that actually held: don't build the frontend on the agent at allRather than fighting a tiny instance's resources for every build, the frontend gets built once (locally, or wherever's convenient), and the built dist/ output — index.html plus a hashed assets/ folder, Vite's default output — is committed directly into the repo under src/webapp/frontend_dist/. Since the Dockerfile already does COPY src ./src, the built assets ride along automatically, with zero Node.js on the agent, zero npm install, zero frontend build step in the pipeline at all. The backend's own path resolution matches this exactly: FRONTEND_DIST_ROOT = APP_ROOT / \"frontend_dist\", i.e. right next to app.py inside src/webapp/ — the same directory the Docker image already contains. A container that \"runs\" but never actually starts the app STATUS: Restarting (0) 3 seconds ago COMMAND: \"python3\" No CMD in the Dockerfile means the base image's default runs insteadExit code 0, not a crash — the tell. With no CMD/ENTRYPOINT in the Dockerfile, docker run falls back to python:3.12-slim's own default command: a bare python3 interpreter. Detached (-d), its stdin is closed immediately, the REPL hits EOF and exits cleanly, and --restart unless-stopped loops that forever. No application code ever runs, which is also why docker logs came back completely empty — there was nothing to log. Fix: CMD [\"python3\", \"main.py\"], pointed at the repo's actual ASGI entrypoint. Dockerfile CMD vs docker-compose's command Once a docker-compose.yml exists alongside the Dockerfile, its command: key fully overrides the Dockerfile's CMD — it doesn't merge with it. Precedence, highest to lowest: a command passed directly on the CLI, then a compose service's command:, then the Dockerfile's own CMD as the last-resort fallback for a bare docker run (only different if the Dockerfile uses ENTRYPOINT instead — then CMD/command: become arguments appended to the entrypoint rather than replacing it). \"It works under docker-compose but not under a bare docker run\" is a real, common gapA compose file can quietly fix a problem a bare docker run still has — here, docker-compose.yml pointed DATABASE_URL at postgres:5432 (the Postgres service's name, resolved over compose's own Docker network), while the app's own .env still says @localhost:5432. Inside any container, localhost means that container itself, not a sibling container and not the host. A Jenkins Deploy stage doing a bare docker run (this project's own pipeline, for simplicity) never gets that compose-level override, so the app starts, tries to reach a database on its own loopback, and fails — a problem invisible until someone actually checks whether the deployed app works, not just whether the pipeline went green. The actual fix: let a Makefile target run docker-compose, not the Jenkinsfile itself Rather than re-implementing docker-compose.yml's networking logic inside the pipeline, the Deploy stage was rewritten to call the project's own Makefile: sh \"make start\" # Makefile start: docker compose up -d --build This fixes the DATABASE_URL/localhost problem for gooddocker compose up builds the app image and starts it alongside Postgres on compose's own network, with compose's own environment override taking effect (DATABASE_URL: postgresql://...@postgres:5432/...) — the exact override a bare docker run could never see. One Makefile target now matches whatever the project's maintainers already use locally, instead of a second, parallel deployment recipe hand-written inside the Jenkinsfile that can quietly drift from it. echo \"make start\" runs nothing — it just prints the wordsA one-character-category mistake with a completely silent failure mode: writing echo \"make start\" instead of sh \"make start\" prints the literal text make start to the console log and does nothing else. The build still reaches Finished: SUCCESS, because nothing in an echo step can fail — there's no command being run to fail. The tell in the log is structural, not textual: a real shell step shows up as [Pipeline] sh followed by a line starting with + (the shell echoing what it's about to run); an echo step shows only [Pipeline] echo and the string itself, with no + line anywhere. A green pipeline that changed nothing is a strong sign to check for exactly this. Yet more tools a base AL2023 AMI doesn't haveRunning make start surfaced two more gaps in the same \"assume nothing is preinstalled\" pattern as 20.2's Java, this section's git/docker, and 20.7's Node: make itself isn't on the base image (make: command not found, fixed with yum install -y make), and even once Docker Compose's CLI plugin is installed, docker compose build separately requires the Buildx plugin — \"compose build requires buildx 0.17.0 or later\" — which is a different plugin binary, not bundled with Compose itself. Neither AL2023's yum repos nor Docker's own package ship it directly; the fix is downloading the plugin binary from Buildx's GitHub releases into the same /usr/libexec/docker/cli-plugins/ directory Compose's own plugin lives in. Since Buildx's release asset name embeds its version number (buildx-v0.37.1.linux-amd64, not a stable filename), resolving the latest tag via GitHub's API first avoids hand-typing a version number into the script that will eventually go stale. Every build leaves behind a full image, forever, unless something removes it sh \"\"\" docker image prune -f docker images investor-pro --format '{{.Tag}}' \\\\ | grep -vE '^(latest|${env.BUILD_NUMBER})\\$' \\\\ | xargs -r -I {} docker rmi investor-pro:{} || true \"\"\" Disk usage that grows by one full image every single builddocker build -t investor-pro:${env.BUILD_NUMBER} -t investor-pro:latest . creates a brand-new ~1.3GB image on every run, and nothing removes the previous numbered tag automatically. By build 18, if none of the earlier 17 images had ever been cleaned up, that alone is many times the agent's entire 8GB disk — exactly the kind of accumulation that trips a low-disk-space monitor (or the Free Temp Space false-positive in 20.3) and takes the agent offline, with the actual cause being old, unused images rather than anything about the current build. The snippet above runs right after the new container is already live, so there's no window where the image actually in use gets deleted out from under it — it keeps only latest and the build currently deployed, and reclaims everything else, every time. Deploying the wrong branch entirely A Jenkinsfile pointed at a branch that never got the fix merged into itEvery fix above — the Dockerfile CMD, the frontend_dist restructuring — happened on a feature branch. Pointing the git step's branch: at main instead brought back the exact same \"bare python3, exit 0\" crash loop from earlier, because main's own Dockerfile still had no CMD at all — the two branches had genuinely diverged (git merge-base --is-ancestor confirmed neither contained the other), each carrying real, non-overlapping work. There's no Jenkins-side fix for this — it's a git problem wearing a deployment failure's clothes. git log --oneline branchA..branchB shows what one branch has that the other doesn't; merging (not rebasing, once a branch may already be deployed from) brings both sets of changes together before pointing the pipeline back at whichever branch is meant to be the source of truth."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-8","title":"Plugin Hygiene, the Credentials Store & Folder-Level Security","body":"Three separate concerns that all show up under \"Manage Jenkins,\" easy to conflate. Plugin hygiene Every installed plugin is both an attack surface and a maintenance burden — each one can introduce its own vulnerabilities, and each one needs updating independently. Install only what a pipeline actually needs, keep an inventory of what's installed and why, and pin versions rather than auto-updating blindly (the same reasoning as 15.2's provider version pinning, applied to Jenkins' own plugin ecosystem). The credentials store Jenkins has a built-in, encrypted credential store (Manage Jenkins → Credentials) — AWS keys, SSH keys, tokens are added there once and referenced by ID from a Jenkinsfile, never hardcoded into pipeline code: withCredentials([usernamePassword(credentialsId: 'ecr-creds', usernameVariable: 'USER', passwordVariable: 'PASS')]) { sh 'docker login -u $USER -p $PASS ...' } A credential ID in a Jenkinsfile is not the credentialcredentialsId: 'ecr-creds' is just a lookup key — the actual secret value never appears in the pipeline source, never gets committed to the repo, and Jenkins actively masks it out of build console logs. Anyone who can edit the Jenkinsfile can use the credential, but they can't read its value from the code itself. Folder-level security Folders aren't just organization — they're a permission and credential-scoping boundary. A credential added inside a folder is only visible to jobs inside that same folder, and role-based/matrix security can grant a team full control over their own folder's jobs without touching anyone else's. This is what makes multi-team Jenkins viable on one shared controller instead of needing a separate instance per team."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-9","title":"Backing Up JENKINS_HOME","body":"JENKINS_HOME holds everything: every job's configuration, full build history, encrypted credentials, and every installed plugin's binary. Losing it is losing the entire Jenkins instance's identity, not just the compute it happened to run on. EBS snapshotThinBackup (plugin) What it capturesThe entire volume, byte for byteJob configs, build records, and credentials specifically — Jenkins-aware Restore unitThe whole volume, as one point in timeSelectable — restore just job configs without touching build history, or vice versa Where it livesInfra-level (week4.html 18.10's EBS volume) — same mechanism as any other point-in-time recovery (week2.html 7.2's RDS backups)Jenkins-level, scheduled from inside the Jenkins UI itself An untested backup is a hope, not a backupThe same principle from every other backup strategy in this project applies here without modification: a snapshot that's never been restored is unverified. Actually restoring a JENKINS_HOME snapshot onto a fresh controller at least once, before it's needed for real, is what turns \"we take backups\" into \"we know recovery actually works.\""},{"page":"week5.html","pageLabel":"Week 5","id":"d20-10","title":"The Declarative Jenkinsfile","body":"A Jenkinsfile is Jenkins' equivalent of a Terraform config — a checked-in file describing a pipeline as code, instead of clicking through the UI to define a job. pipeline { agent { label 'linux' } parameters { choice(name: 'ENVIRONMENT', choices: ['dev', 'stg'], description: 'Target environment') } environment { AWS_REGION = 'ap-south-1' } stages { stage('Test') { steps { sh 'npm test' } } stage('Deploy') { when { branch 'main' } steps { sh \"./deploy.sh ${params.ENVIRONMENT}\" } } } post { failure { echo 'Notify the team -- build failed' } always { cleanWs() } } } agent Where this pipeline runs — a label (20.1), a specific Docker image, or none if each stage declares its own. parameters Build-time user input — a dropdown, a string field, a checkbox — collected before the pipeline starts, referenced as params.NAME. environment Environment variables available to every step in the pipeline (or scoped to just one stage if declared inside it instead). when Guards a stage — here, the Deploy stage only actually runs on the main branch, though it's still evaluated (and skipped) on every other branch's run. post Runs after the pipeline finishes, regardless of outcome — always, success, failure, and others let cleanup or notification logic depend on how the run ended, the closest Jenkins equivalent to a try/finally."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-11","title":"Multibranch Pipelines, Webhooks & Shared Libraries","body":"Three pieces that turn \"one Jenkinsfile\" into \"a real CI setup serving a whole repository.\" Multibranch pipeline Instead of one job per branch created by hand, a multibranch pipeline job scans a repository and automatically creates (and destroys) a pipeline run per branch and per pull request — each one running the same Jenkinsfile, checked into that specific branch, so a feature branch can even modify its own pipeline before merging. Webhooks Without a webhook, Jenkins has to poll the repository on a timer to notice new commits — slow, and wasteful when nothing's changed. A webhook has GitHub/GitLab push an event to Jenkins the moment a commit or PR happens, triggering the build immediately instead of waiting for the next poll. Shared libraries A shared library is a separate git repository of reusable Groovy pipeline code (conventionally under vars/ and src/), loaded into any Jenkinsfile that needs it: @Library('platform-shared-lib') _ pipeline { agent { label 'linux' } stages { stage('Deploy') { steps { deployToEcs(service: 'order-service', environment: 'dev') } } } } The same reasoning as a Terraform moduledeployToEcs(...) above is a function defined once in the shared library and called from every pipeline that needs to deploy to ECS — the exact same motivation as extracting a Terraform module (16.6): once the same handful of steps shows up in a third Jenkinsfile, that's the signal to stop copy-pasting Groovy and put it in one place every pipeline calls instead."},{"page":"week5.html","pageLabel":"Week 5","id":"d20-12","title":"Certified Jenkins Engineer (CJE)","body":"The external validation for Jenkins specifically, the way week4.html 19.9 covers Terraform's — CloudBees' Certified Jenkins Engineer (CJE) exam, last updated September 2023. Worth knowing before searching further: CloudBees retired a similarly-named CCJE (Certified CloudBees Jenkins Engineer) credential back in 2022 — CJE, without the extra \"C,\" is the one still active. Format60 multiple-choice questions, 90 minutes Passing score66% PrerequisitesNone formally, though CloudBees recommends real hands-on Jenkins experience first Tests againstOpen-source Jenkins core (not CloudBees CI/Operations Center specifically) The four exam domains Jenkins Fundamentals — core CI/CD concepts, jobs and builds, source code management integration, plugins, security (authentication/authorization), the REST API Jenkins Administration — installation, distributed builds, controller/agent configuration, credentials, artifact and fingerprint management, notifications Pipeline (Build Technologies) — declarative pipeline syntax, stages and steps, multibranch pipelines, shared libraries, promotion strategies, upstream/downstream triggering Freestyle (Build Technologies) — the older, UI-configured job type predating Pipeline-as-code — still a real exam section even though this project skips straight to declarative pipelines Public breakdowns of the exact weighting disagreeUnlike week4.html 19.9's HashiCorp source (a single official, current objectives page), CloudBees doesn't publish one canonical up-to-date percentage breakdown the way HashiCorp does — third-party prep sites list different weightings and even different section names for the same four domains. Treat the domain list above as directionally reliable, but verify specifics against CloudBees' own current study guide before relying on any one source's exact percentages. Where 20.1–20.11 already landController/agent architecture (20.1), installation itself (20.2 — literally named in the Administration domain's topic list), connecting a distributed agent (20.3 — \"distributed builds, controller/agent configuration\" in the Administration domain, almost word for word), job types and Pipeline job configuration (20.4, 20.5), source code management integration and credentials (20.6 — named explicitly in the Fundamentals domain), real-world pipeline/deployment troubleshooting (20.7 — not a named exam topic, but exactly the kind of \"why did the green build not actually work\" judgment the Pipeline domain expects), plugin/credential/folder security (20.8), the declarative Jenkinsfile (20.10), and multibranch/shared libraries (20.11) map directly onto the Administration and Pipeline domains — the two domains carrying the most exam weight in every breakdown found. Freestyle jobs get a mention in 20.4 but no deep-dive yet — worth a dedicated look before sitting this exam, since it's still a full CJE section."},{"page":"week5.html","pageLabel":"Week 5","id":"d20","title":"Jenkins Provisioned, Pipeline as Code","body":"Standing up Jenkins itself, then writing pipelines as code from day one."},{"page":"week5.html","pageLabel":"Week 5","id":"d21","title":"CI and CD, End to End","body":"The full path from commit to running service, plus the two operations jobs the customer names explicitly. Lint and test stages Container build and push to ECR tagged by commit terraform fmt, validate and plan posted onto the pull request The approval gate, terraform apply ECS service update with wait-for-stable, smoke tests A one-click rollback job, notifications Safe SQL execution inside a transaction, and log extraction for a time window"},{"page":"week5.html","pageLabel":"Week 5","id":"d22","title":"Progressive Delivery, Runbook, Readiness Review","body":"Morning: finishing the pipeline. Afternoon: the readiness review itself. Blue/green and canary driven from the pipeline Migration step ordering around a deploy OIDC and IAM roles instead of static credentials Timeouts, retries and idempotency The operations runbook, written from what's been built Readiness review: live demo, architecture defence, customer-style questioning Known thinnessThree days is enough to build and operate this pipeline, not to design a delivery platform from nothing. Jenkins is targeted at L2 on 30 September and finishes during October's knowledge transfer, on the customer's own Jenkins, with the tech lead pairing."}];
